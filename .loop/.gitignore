# Commit the config + docs; ignore mutable runtime state.
state.json
STOP
PAUSE
findings-inbox.json
opportunities-inbox.json
opportunities-digest.md
# Phase 5 business-opportunity scout: the machine ledger (biz-opportunities-ledger.json)
# is COMMITTED on purpose — it's the durable dedupe memory. The per-run digest + raw
# run dumps are scratch.
biz-opportunities-digest.md
bizscout-runs/
proposals.json
outbox.txt
*.log
# machine-specific channel binding — contains a personal chat handle, never commit
channel.local.json
*.local.json
# pending-actions.md and dismissed-findings.md are committed as templates;
# their runtime content is fine to track (it's an audit trail), but ignore
# any rotated copies.
pending-actions.*.md
dismissed-findings.*.md
# Phase 9 daily radar: commit the config (radar-sources.json), the install tooling
# (radar-install/) and the human-readable briefs (radar-briefs/ — an audit trail like
# gsc-audits/). Ignore the mutable per-run dumps + incremental state.
radar-runs/
radar-state.json
radar-cron.err
# Per-run strategist + Ratan-Tata score dumps — intermediate scoring output. The
# durable merged memory is biz-opportunities-ledger.json (committed).
strategist-scores-*.json
tata-scores-*.json
