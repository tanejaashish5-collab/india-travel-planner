# Pending actions — awaiting your approval

> This file is where the loop writes anything it wants to **write, commit, or deploy**.
> It NEVER acts on these without your "go". When you see entries here, the loop is paused waiting for you.
>
> **To approve:** reply `go` (all) or `go 1 3` (just those numbers) on the approval channel, or delete the lines you don't want and reply `go`.
> **To reject everything:** reply `skip` or just delete this file's entries.

_(empty — nothing pending)_
