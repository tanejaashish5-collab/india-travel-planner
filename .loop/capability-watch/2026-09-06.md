# Capability-refresh watch — 2026-09-06

Window: 2026-08-30 → 2026-09-06.

## (a) Releases this week

- **Sept 1 — Claude Fable 5.1 / Mythos 5.1 launch**: 1M context (default+max), 128k max output, always-on adaptive thinking, same $10/$50 pricing as Fable 5, **cache reads cut to $0.25/Mtok (0.025x base input vs 0.1x on other models — a real 4x cache-read cost cut)**. Capability gains concentrate in long-session agentic coding, document/spreadsheet/slide work, multistep research, vision (dense charts/tables in PDFs), 1M-context reasoning, computer use. [platform.claude.com/docs/en/models/fable-5-1/whats-new-fable-5-1](https://platform.claude.com/docs/en/models/fable-5-1/whats-new-fable-5-1)
- **Sept 1 — same release**: per-message effort mid-conversation (beta), turn-scoped system messages (beta), progress-updates-only thinking display (beta) — all reduce prompt-cache invalidation from mid-session tweaks. [same URL]
- **Sept 2 — Claude Code v2.1.259**: `managedMcpServers` (org-wide HTTP/SSE MCP servers); `--permission-prompts none` for unattended headless hosts — auto-denies instead of hanging. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 3 — v2.1.260**: `/diff` panel; `/cost` + status line now show the likely cause of a prompt-cache miss. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 3 — `ant` CLI 1.30.0**: `ant apply` — declarative agents/environments/skills/memory-stores/deployments defined in repo files, applied via a `claude-lock.json` lockfile so reapplies update instead of duplicating. [platform.claude.com/docs/en/cli-sdks-libraries/cli/apply](https://platform.claude.com/docs/en/cli-sdks-libraries/cli/apply)
- **Sept 4 — v2.1.261**: `/skill-doctor` — shows which loaded skills go unused and what they cost in context; `bashOutputMaxChars`/`taskOutputMaxChars` raise inline output caps to 128K. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 6 — v2.1.263**: bug fixes only. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)

## (b) Flips to retest

**Repo-read failure, 4th consecutive week.** `research-wiki/wiki/ideas/rejected-ideas.md` remains outside this cloud session's GitHub scope (moved 2026-08-10 to the founder's local `claude-ai-os` repo). Fallback: re-ran the keyword grep across all 101 `.loop/biz-opportunities-ledger.json` entries (`updatedAt` still 2026-06-26 — 4th straight week static). 9 text hits for capability/cost-kill language ("too expensive", "compute cost", "not feasible", etc., across 8 entries: disclosureforge, creatorcostmap, franchiselens-india, foundonai, jobping, tokentriage, petrace, fogoledger) — every one sits inside a **market-structure** verdict (no defensible moat, single-regulation dependency, thin TAM), not an actual capability gate. Same finding as the prior 3 weeks; no flip candidates in the accessible ledger.

Needs a founder action to unblock: GitHub scope added for `claude-ai-os`, or `rejected-ideas.md` synced into this repo.

## (c) Adoption candidates

- **Fable 5.1's cheaper cache reads + long-session agentic gains**: candidate for this org's long research/backfill sessions currently run on Sonnet/Opus (state-cluster eateries/stays sweeps, if reopened). Cheapest test: run one bounded research sub-agent on `claude-fable-5-1` and compare cost + output quality against the current Haiku-research/Sonnet-synthesis split.
- **`/skill-doctor` (Sept 4)**: direct fit — this repo's `CLAUDE.md` plus the loaded skill roster is large. Run it once in a live session and prune whatever shows unused.
- **`ant apply` (Sept 3)**: worth checking whether JobAgent / radar-pipeline / autoposter agent configs are still hand-managed and could move to declarative files + lockfile.
- **`--permission-prompts none`**: fits unattended cron-style sessions (this watcher included, the GA4/GSC audit crons) — auto-deny beats a hang. Cheapest test: check whether any scheduled task here currently risks stalling on an unanswered prompt.
