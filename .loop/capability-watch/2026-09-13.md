# Capability-refresh watch — 2026-09-13

Window: 2026-09-06 → 2026-09-13.

## (a) Releases this week

- **Sept 6 — Claude Code v2.1.263**: bug fixes/reliability only, no listed details. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 8 — v2.1.265**: `user.email`/`user.groups` in gateway telemetry; `--plugin-dir` for a folder of plugins; 1 GB cap on disk-saved tool results; prompt-cache/resume fixes; model switching no longer breaks cache reuse. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 8 — v2.1.266**: same-day regression fix — restored `CLAUDE_CODE_USE_GATEWAY` behavior broken in .265. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 9 — v2.1.267**: `maxEffortLevel` setting caps effort across all providers; `--system-prompt-snapshot off`; fixed Cowork scheduled tasks failing for orgs requiring sandboxing (**this watcher is a Cowork scheduled task**). [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 10 — v2.1.268**: gateway pricing support with managed settings; fixed WebFetch hanging indefinitely (now a 300s timeout — likely why some fetches this run failed clean instead of hanging); fixed high idle-session CPU. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 10 — Claude Platform**: Managed Agents permission policies gain `auto` — server evaluates each agent/MCP tool call and runs/denies/pauses it, reported via `evaluation`/`evaluated_permission` fields; `ant beta:sessions connect` attaches a terminal (or `--web` console view) to a live Managed Agents session. [platform.claude.com/docs/en/release-notes/overview](https://platform.claude.com/docs/en/release-notes/overview)
- **Sept 11 — Claude Code v2.1.269**: `claude plugin eval` (scored, reproducible plugin eval runs); `/output-style [name]`; Bash-command file diffs now attached to tool results; `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1–256); OpenTelemetry repo attributes; several permission/terminal/plugin-MCP-resume bug fixes. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **Sept 12 — v2.1.270**: fixed a regression where read-only git commands in Bash started asking for permission again on long-running sessions. [code.claude.com/docs/en/changelog](https://code.claude.com/docs/en/changelog)
- **UNVERIFIED, exclude from adoption**: multiple secondary outlets (Bleeping Computer, MindStudio, TestingCatalog, daily.dev — all blocked by this session's egress proxy, so only search-snippet text was seen) report Anthropic/@ClaudeDevs announced Claude Code's weekly usage limits go from a temporary +50% boost (since May) to a **permanent +25%** on Sept 14 — a net ~17% cut from today's elevated limits, though still above the pre-May baseline. No anthropic.com/docs.claude.com/support.claude.com primary page was reachable to confirm wording or exact date; flagging because it changes next week's usage budgeting regardless.
- Budget controls / advisor / `inference_geo` / GitHub-repo skills for Managed Agents (surfaced by search as "September news") actually shipped **Aug 7**, outside this window — not counted here (already would've been due in the 08-09 brief).

## (b) Flips to retest

**Repo-read failure, 5th consecutive week.** `research-wiki/wiki/ideas/rejected-ideas.md` stays outside this cloud session's GitHub scope (moved 2026-08-10 to the founder's local `claude-ai-os` repo; `find /` confirms no local copy exists here). Fallback unchanged: `.loop/biz-opportunities-ledger.json` `updatedAt` is still **2026-06-26** — 5th straight week static, 101 entries. Re-ran the capability/cost-kill keyword grep: 8 hits (foundonai, jobping, tokentriage, petrace, act-direct-source-readiness-desk, fogoledger, spiceroute-direct, marbleway) — same set as prior weeks. Read each `surviving_wedge` directly this run: all 8 are market-structure or competitive-moat kills (existing incumbents, thin/fragmented TAM, regulatory narrowness), not "model can't do X" — no flip candidates.

Still needs a founder action to unblock: GitHub scope added for `claude-ai-os`, or `rejected-ideas.md` synced into this repo. This has now been the identical ask for 5 weeks running.

## (c) Adoption candidates

- **`maxEffortLevel` (Sept 9)**: this watcher and the other Cowork scheduled tasks (GA4/GSC audits) run unattended — a hard effort cap is a cheap guardrail against a runaway-cost session. Cheapest test: set it on one low-stakes cron task and confirm output quality holds.
- **Cowork sandboxing fix (Sept 9, v2.1.267)**: directly relevant — this session *is* a Cowork scheduled task. If any scheduled task here previously failed silently, this may be why; worth a one-time check of recent cron logs for that failure signature.
- **Managed Agents `auto` permission policy + `ant beta:sessions connect` (Sept 10)**: fits JobAgent-style unattended agents better than today's binary allow/deny — `auto` lets the server route only genuinely risky calls to a human, and `connect` gives a live intervention path instead of waiting for the next scheduled check-in. Cheapest test: point one non-critical Managed Agents session at `auto` and watch the `evaluation` field for a week before trusting it broadly.
- **Weekly usage-limit change (Sept 14, UNVERIFIED)**: if confirmed, next week's brief should re-check actual weekly Claude Code usage against the new ceiling — cost-aware operating rules in this repo already exist for a reason.
