# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-21 (AEST Thursday)

Audit run shortly after **06:35 AEST May 21** (≈20:35 UTC May 20). Covers the AEST May 20 posting day plus the overnight roll into May 21 — i.e. it picks up exactly where `audit_2026-05-20.md` (run 09:13 AEST May 20) deliberately stopped, since every May 20 posting slot was still in the future at that point.

**Sandbox note:** the `gh` CLI is **not installed** in the audit sandbox (same as the last 3 audits). GitHub Actions run conclusions and `event` types (schedule vs `workflow_dispatch`) cannot be queried directly. Runs are reconstructed from the `origin/autoposter-state` branch — each completed run pushes one `chore: autoposter state YYYY-MM-DD HH:MM UTC` commit, and the audit reads the per-commit diffs of `autoposter.log`, `data/post_log.jsonl`, `state.json` and `analytics.json`. A fresh `git fetch` was performed before this report.

## 📊 GitHub Actions activity

Audit window: 23:13 UTC May 19 → 20:35 UTC May 20 (09:13 AEST May 20 → 06:35 AEST May 21). **8 state-push commits** = 8 completed runs.

| # | UTC commit | AEST | Run | Result |
|---|---|---|---|---|
| 1 | `74496b10` 08:01 | 18:01 May 20 | Post (MORNING mode) | ✅ mumbai · arrival_intel · IG `Mgu9C` + FB `i6KpB` |
| 2 | `21eed716` 10:06 | 20:06 May 20 | Post (MORNING mode) | ✅ chennai · arrival_intel · IG `D7Nso` + FB `x3rm8` |
| 3 | `8f7dbc51` 12:50 | 22:50 May 20 | Post (MORNING mode) | ✅ havelock-island · cost_index_card · IG `IhnsX` + FB `vSrhg` |
| 4 | `7165ac87` 17:36 | 03:36 May 21 | Analytics sync | ✅ ran (225 published / 46 failed cumulative) — ⚠️ output later clobbered |
| 5 | `f0762f2f` 17:48 | 03:48 May 21 | Post (MORNING mode) | ✅ chopta · arrival_intel · IG `Ih9RX` + FB `vSBsg` |
| 6 | `7aaf8ca2` 18:15 | 04:15 May 21 | Post (MORNING mode) | ⚠️ chopta · v2_pov_slow_morning · IG `l5k1K` only — FB skipped |
| 7 | `fda9d64c` 18:46 | 04:46 May 21 | Post (MORNING mode) | ⚠️ bhandardara · cost_index_card · IG `MgPJC` **Queued** + FB `nH3Al` ✅ |
| 8 | `c9ade096` 20:34 | 06:34 May 21 | State rollback / housekeeping | ⚠️ no posts — reverted state.json + truncated log (see Issue §4) |

**8 runs · 6 content-post runs · 1 analytics sync · 1 housekeeping.** 11 platform posts landed (5 IG+FB pairs + 1 IG-only). 0 outright crashes.

## 📅 Schedule reference

**The SKILL.md schedule narrative is stale** (morning feed 6:30–9 PM, reel slots 1/2/3, tourist map Tue/Thu/Sat, canva, infographic Mon/Wed/Fri, YT Short 1/2/3). Those slots were **paused on 2026-05-16** as part of the cadence cut (user diagnosis: 215 IG posts → 2 organic followers; sub-1K accounts get algorithm-penalised for posting >2–3×/day). Today is a Wednesday-equivalent posting day, so SKILL.md would literally expect an infographic — but the infographic cron has been commented out for 5 days. **Not a miss.** This is the 2nd consecutive audit to flag the stale task file (see Issue §6).

Active crons in `.github/workflows/autoposter.yml` (verified against `origin/main` this run):

| Slot | Cron (UTC) | AEST | May 20 status |
|---|---|---|---|
| Morning post (IG+FB) | `17 3 * * *` | 13:17 | ✅ Posted (mumbai) |
| YT Short slot 1 | `17 4 * * *` | 14:17 | ⚠️ Ran — posted IG+FB feed content, **no YT short** |
| YT Short slot 2 | `47 11 * * *` | 21:47 | ⚠️ Ran — posted IG+FB feed content, **no YT short** |
| Evening post (IG+FB) | `17 14 * * *` | 00:17 May 21 | ✅ Posted (chopta) |
| Analytics sync | `17 18 * * *` | 04:17 May 21 | ✅ Ran 03:36 (41 min early — recurring) |
| Engagement pull | `17 21 * * *` | 07:17 May 21 | ⏳ Not yet due (read-only, no commit) |
| Weekly digest | `17 22 * * 0` | Mon 08:17 | N/A (Sunday only) |
| Reel 1, Visual, Reel-map, Flow-story, UGC, Infographic, Tourist map, Canva | — | — | ⏸️ Paused 2026-05-16 — not a miss |

## ✅ Cross-reference — scheduled vs actual

- **Morning post** → ✅ Published. mumbai/arrival_intel, IG `Mgu9C` + FB `i6KpB`.
- **YT Short slot 1** → ⚠️ The slot ran but delivered an IG+FB feed post (chennai/arrival_intel), **not a YouTube Short**. Zero `youtube` posts and zero `yt_short.*`-format posts across the whole of May 20 — contrast May 18–19, which both shipped `yt_short.listicle` to YouTube. Most likely intentional: `yt_shorts_gen.py` is mid-rewrite under Phase 2 (`origin/main` 370468d4 "wire CSV YT formats into yt_shorts_gen, v2_yt_silent_pov fires"). Flagged, not re-triggered — see Issue §2.
- **YT Short slot 2** → ⚠️ Same as slot 1 — IG+FB feed post (havelock-island/cost_index_card), no YT short.
- **Evening post** → ✅ Published. chopta/arrival_intel, IG `Ih9RX` + FB `vSBsg`.
- **Analytics sync** → ✅ Ran 17:36 UTC (`7165ac87`), reported 225 published / 46 failed cumulative. Ran 41 min ahead of the `17 18 * * *` cron — same early-fire offset noted in the last two audits, no publishing impact. **But** its `analytics.json` write was reverted 12 min later — see Issue §3.
- **Engagement pull** → ⏳ Not yet due at audit time (cron 21:17 UTC; latest commit 20:34 UTC). Read-only — never pushes a commit. Expected absence, not a miss.
- **Two extra post runs** (`7aaf8ca2` 18:15, `fda9d64c` 18:46) → beyond the 4 scheduled content slots. See Issue §1.

**Verdict: no slot is ❌ Missing.** Every active content slot produced a post, the evening post and analytics sync completed. The concerns are over-posting, two soft-failed posts, and state/analytics integrity — not missed runs.

## 🚨 Issues Found

**1. Over-posting — 6 content posts on May 20 vs the 2–3/day cadence cap.**
Eleven platform posts went out (mumbai, chennai, havelock-island, chopta ×2, bhandardara). The 2026-05-16 cadence cut exists specifically to keep this ≤2–3/day. Run-start times (07:59, 10:04, 12:48, 17:46, 18:13, 18:44 UTC) do **not** align with the 4 content crons (03:17, 04:17, 11:47, 14:17 UTC) — drifts run +1 h to +5h45m, and three runs fired inside a single 58-minute window (17:46 / 18:13 / 18:44 UTC). That cadence is impossible for GitHub's scheduler and points to **manual `workflow_dispatch` triggering** — almost certainly Phase 2 format testing: `origin/main` shows ~10 Phase 2 format-wiring commits dated May 20, and the brand-new `v2_pov_slow_morning` format appears in the 18:15 post. Cannot be confirmed (no `gh` to read run `event` types). If the runs were instead scheduled, the cron schedule is over-firing and the May-16 cadence cut has regressed — either way it warrants a look.

**2. Zero YouTube Shorts published May 20.** Both YT Short slots delivered IG+FB feed content instead of Shorts; no `youtube` post all day. Likely intentional during the Phase 2 `yt_shorts_gen` rewrite, but worth a conscious confirm so it doesn't silently become a permanent gap.

**3. `analytics.json` clobbered by a concurrent run.** The 17:36 analytics sync (`7165ac87`) appended +802 lines to `analytics.json`. The very next commit, the 17:48 post run (`f0762f2f`), **removed those same 802 lines** — it pushed a stale `analytics.json` checked out before the sync. Branch HEAD `analytics.json` is 3611 lines and does **not** contain the May 20 sync's data. The autoposter's concurrency-merge step covers `post_log.jsonl` but not `analytics.json`. Bounded impact (the next daily sync re-pulls), but the sync genuinely did not persist.

**4. `state.json` rollback recurrence (now the live HEAD state).** The 20:34 housekeeping commit (`c9ade096`) reverted `state.json` — it removed the 18:44 run's `posted_today` entries (bhandardara, chopta, cost_index_card) and re-ran the `post_log.jsonl` bootstrap migration, meaning it started from a stale checkout and force-pushed over newer state. Branch HEAD `state.json` now reads `posted_today: {}`, `last_sync: 2026-04-30`, `strategy_history: null`. This is the same regression flagged as Issue §1 in `audit_2026-05-20.md`, still unfixed. **Impact is bounded:** `data/post_log.jsonl` (the authoritative append-only dedup log) retains all 11 May 20 entries, and the runtime dedup reads from the post-log fingerprint snapshot ("8 dests in 7d · 24 (dest,fmt) pairs in 30d" in the logs), not from `state.json.posted_today` — so dedup is protected. But `state.json` is effectively dead/stale on the branch.

**5. Two soft-failed posts.**
   - `7aaf8ca2` 18:15 — `v2_pov_slow_morning` published to IG (`l5k1K`) but the **FB render skipped**: `v2_pov_slow_morning: no eligible dest at render time — SKIPPING` → `[facebook] No content generated — skipping`. IG/FB asymmetry on a brand-new Phase 2 format — a render-eligibility bug worth fixing before this format goes live on cadence.
   - `fda9d64c` 18:46 — bhandardara IG post returned `⚠️ Queued (ID=MgPJC)`, **not** a confirmed `✅ Published`. FB published cleanly (`nH3Al`). "Queued" usually self-resolves on Outstand's side, but it should be spot-checked.

**6. SKILL.md audit task file is stale.** It still describes the pre-May-16 schedule (morning feed 6:30 PM, reel slots, tourist map, canva, infographic, 3 YT Short slots, 3 AM/6 AM crons). None match the live workflow YAML. Step 7's re-trigger modes (`tourist-map`, `canva-visual`, `reel`, `infographic`) all target **paused** slots — blindly re-triggering them would reintroduce the exact over-posting the cadence cut fixed. The task file should be rewritten to mirror the 6 active crons + the paused list.

**7. `gh` CLI unavailable in the audit sandbox.** Run conclusions, `event` types, and `gh workflow run` re-triggers are all impossible here. The audit relies entirely on the autoposter-state commit history — a run that crashed before its state-push step would leave no trace and be invisible to this method.

## 🛠 Actions Taken

**No re-triggers performed.** Reasons:
1. **Nothing is ❌ Missing** — every active content slot posted; the day in fact ran *over* cadence, not under.
2. **`gh` CLI is unavailable** in this sandbox — `gh workflow run` cannot be executed here regardless.
3. The two soft-failures (§5) would only be made worse by a re-trigger: re-running a post mode adds *more* posts on a day that already exceeded the 2–3/day cap, and "Queued" typically resolves on its own.
4. SKILL.md step 7's modes (`reel`, `canva-visual`, `infographic`, `tourist-map`) are all **paused** content types — re-triggering them would violate the 2026-05-16 cadence cut and risk re-triggering the algorithm penalty.

## ⚠️ Status: 7 issues found, 0 re-triggers

Posting pipeline is **healthy and publishing** — 11 posts landed, evening post and analytics sync both completed, no crashes. The concerns are cadence and data hygiene, not availability.

**Recommended follow-ups (no autonomous action taken — solo-founder review):**
- **Confirm the 6 May 20 posts were intentional Phase 2 manual tests** (Issue §1). If yes, no action. If the schedule is over-firing, the cadence cut has regressed and the cron list needs re-checking.
- **Decide the YouTube Shorts posture** (Issue §2) — paused-for-Phase-2 vs unintended gap.
- **Fix the concurrency clobbers** (Issues §3 + §4): extend the autoposter-state merge step to cover `analytics.json`, and confirm whether `state.json` can be retired in favour of `post_log.jsonl` now that dedup no longer reads it. The 17:36 analytics sync overlapping the 17:48 post run is the direct trigger.
- **Fix `v2_pov_slow_morning` FB render eligibility** before it ships on cadence (Issue §5), and spot-check that bhandardara IG `MgPJC` cleared from Queued.
- **Rewrite the SKILL.md audit task file** to the 6 active crons + explicit paused list (Issue §6) — pending for 2 audits running.
