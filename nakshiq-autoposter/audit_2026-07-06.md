# 🔍 Nakshiq Autoposter Daily Audit — 2026-07-06

Run: Monday 2026-07-06, ~11:08 PM AEST. Covers the **Jul 6 UTC posting cycle so far** (00:00 → 13:08 UTC = 10:00 AM → 11:08 PM AEST). Ground truth: `origin/autoposter-state` (HEAD `16b1a3a3e`) + GitHub's public REST API for Actions runs (`api.github.com/.../actions/workflows/autoposter.yml/runs`, unauthenticated — reachable directly from this session, no `gh` CLI needed). Local `autoposter.log`/`state.json` on `main` are stale (frozen 2026-04-30 / 2026-06-04) and were ignored, per the pattern the last several audits already established.

📊 **GitHub Actions: 3 runs today (3 succeeded, 0 failed, 0 in progress)** — all `schedule` events (run #784, #785, #786). Correct count for a Monday: YT Short 1 + Reel Studio (Mon/Wed/Fri day) + Carousel. YT Short 2 hasn't fired yet as of report time — see Issues.

## Content Published Today (AEST)

| Slot | AEST | Status | Detail |
|---|---|---|---|
| YT Short 1 | 6:45 PM | ✅ Published | kargil `this_vs_that` (hi) — YT `voLNg` confirmed; IG `cXrBL` queued-unconfirmed (known 40s-poll false-alarm class — see Issues) |
| Reel Studio | 7:50-7:51 PM | ✅ Published | leh `itinerary` (route: ladakh-express-7d) — YT `CySsF` + IG `VrB2N`, both platform-confirmed. Mon/Wed/Fri cadence, correct day |
| Carousel | 9:49 PM | ✅ Published | lamayuru `persona`, 7 slides — IG `gcNOx` + FB `ODLdv`, both platform-confirmed |
| YT Short 2 | — | 🕐 Not yet fired | Target ~9:47 PM AEST (11:47 UTC); as of this report (11:08 PM AEST) it's ~1h20m past target. See Issues — not treating as missing yet |
| Evening post | — | 🕐 Upcoming | Target ~12:17 AM AEST Tue (14:17 UTC) — not due yet |
| Analytics sync | — | 🕐 Upcoming | Target ~4:17 AM AEST Tue (18:17 UTC) — not due yet |
| Engagement pull | — | 🕐 Upcoming | Target ~7:17 AM AEST Tue (21:17 UTC) — not due yet |

`posted_today` cross-check (autoposter-state `state.json`): `GAh5p_yt_short`, `GAh5p_reel_studio`, `m8EAd_reel_studio`, `m8EAd_carousel`, `PdMu0_carousel` all stamped `2026-07-06`. `post_log.jsonl` has 6/6 entries for today (2 legs × YT Short 1, 2 legs × Reel Studio, 2 legs × Carousel). Consistent with the log and GHA run IDs.

### Paused / not currently scheduled (not misses)

Per `.github/workflows/autoposter.yml`'s own cron comments, these are intentionally dark right now — do not flag as missing in future runs: Morning feed post (paused 2026-07-01, replaced by daily Carousel), Reel slot 1 (paused 2026-05-16), Reel-map/Tourist map (paused 2026-05-16, was Tue/Thu/Sat), Visual slot/Canva (paused 2026-05-16), Infographic (paused 2026-05-16, was Mon/Wed/Fri), Flow story (paused 2026-05-16, was Sun only), Reel slot 2 (removed 2026-05-10). Only 2 YT-Short crons exist — there is no "slot 3."

## Issues Found

**1. The scheduled-task file's schedule block is stale and should be updated.** This is a repeat flag — the 2026-07-05 audit already noted it and it's still unfixed. The task instructions list 11 slots (morning feed 6:30-9PM, reel 1/2/3, tourist map Tue/Thu/Sat, canva visual, infographic Mon/Wed/Fri, YT short 1/2/3 at fixed clock times, analytics sync 9:30AM) — none of the times match the live workflow, and tourist-map/canva/infographic/reel-slot-1/reel-slot-2/flow-story are paused entirely (5 of 11 listed items don't run at all). Mechanically grading against the task file's list would incorrectly flag 8 of 11 slots as "missing" every single day. I graded against the actual cron schedule in `autoposter.yml` instead (same approach prior audits used). Recommend the task definition get updated to the real 7-slot cadence (YT Short ×2, Carousel, Reel Studio M/W/F, Evening, Analytics, Engagement) — happy to do this via the scheduled-tasks tool next time you're here to confirm the rewrite.

**2. Weekly digest has a confirmed commit-path bug — root-caused this run.** `digest_weekly.py` writes its only output to `data/research/social-engagement-week-{date}.md` (confirmed in source, line ~394). Neither the "Sync latest autoposter state" step's file loop nor the "Commit updated state" step's file loop in `.github/workflows/autoposter.yml` (the two per-file `for f in ...` lists, ~lines 189-196 and ~584-591/629-636) include this path — only `state.json`, `autoposter.log`, `analytics.json`, `post_engagement.json`, `post_outcomes.jsonl`, `post_log.jsonl`, `posted_today.jsonl`, `theme_usage.jsonl` are synced/committed. Evidence this bit this week: GH Actions run #783 (created 2026-07-05 23:19 UTC / 9:19 AM AEST Jul 6 — the delayed `--digest-weekly` fire, queued behind run #782's Engagement Pull in the shared concurrency group) completed with **conclusion=success**, but produced **zero commit** to `autoposter-state` and zero matching log lines. The report almost certainly generated correctly inside the ephemeral runner and was discarded when the job ended — it never reached the repo. Fix: add the digest path (or its directory) to both file lists in the workflow.

**3. Confirmed: the 3 "queued_unconfirmed" posts flagged by yesterday's audit did reconcile.** `N4X8a` (YT, turtuk), `8VoDI` (IG, turtuk), `gcDmx` (YT, umlingla) all show `"status": "published"` in the latest `analytics.json`, no errors. This validates the "known 40s-poll false alarm, reconciles at next analytics sync" pattern noted in prior audits. Today has one new instance in the same class: `cXrBL` (IG leg, kargil, YT Short 1) — not yet in `analytics.json` (expected; tonight's analytics sync hasn't run). Next audit should confirm it reconciled too.

**4. All 3 of today's runs drifted ~4-4.5 hours late** (targets 04:17 / 05:47 / 07:17 UTC; actual fires 08:40 / 09:45 / 11:43 UTC). This is larger than the 30min-2h drift the workflow's own comments treat as normal/expected. No failures resulted (dedup + windows absorbed it fine), so this isn't an active problem, but it's a bigger drift than the last few days (Jul 5 ranged 1-3.5h) and worth a glance if GHA scheduling delay keeps growing — check tomorrow whether it's a one-day blip or a trend.

No ERROR/Traceback lines in today's log (only the one expected `queued but NOT confirmed` warning, class already covered above). No git push failures — checked the full `autoposter-state` history for the workflow's own "(re-merge attempt N)" retry-commit marker; zero occurrences ever, so the push-retry path has never been needed.

## Actions Taken

None. No genuine misses or failures — everything that should have posted by now did (3/3 success). YT Short 2 is ~1h20m past its nominal target but that's well inside today's own observed 4-4.5h drift band on the 3 slots that already fired; re-triggering it now would be premature and risks either duplicate content churn or a wasted manual dispatch right before the natural delayed cron lands. Holding off — next audit should confirm it fired (look for a 4th commit/run dated today with a `yt_short` post_log entry).

⚠️ **2 process-level issues to review (not blocking, nothing failed today):** the stale task-file schedule (repeat flag) and the newly root-caused weekly-digest commit-path bug. Content posting itself: ✅ all clear.
