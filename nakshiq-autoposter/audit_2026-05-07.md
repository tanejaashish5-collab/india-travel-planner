# Nakshiq Autoposter Daily Audit — 2026-05-07 (Thu, AEST)

Audit generated at **10:30 AEST (00:30 UTC)** on Thu 7 May 2026.
Audit covers the **UTC operating day 2026-05-06** (≈ AEST Wed 13:30 → Thu 04:00) — that is the cron day that has just completed.
All times below are **UTC** unless explicitly marked AEST or IST. The cron in `.github/workflows/autoposter.yml` is keyed to **IST/UTC** (NOT AEST), so this audit reports against actual cron windows.

## GitHub Actions

10 scheduled runs initiated on 2026-05-06 UTC (1 also fired on 2026-05-05 19:48 UTC for Tue's analytics sync). 8 succeeded, 1 failure-cancelled, 1 cancelled.

| Time UTC | Time AEST | Conclusion | Window (TOTAL_MINS) | Mode workflow would have selected | Run ID |
|---|---|---|---|---|---|
| 06:16 | 16:16 | ✅ success | 376 (320–400) | `--reel` (Reel slot 1) | 25419649261 |
| 07:14 | 17:14 | ✅ success | 434 (400–440 → off-schedule) | `--sync-only` ⚠️ but log shows MOAT (mode-step ran ~07:20 = 440+) | 25421674165 |
| 07:54 | 17:54 | 💥 failure / cancelled | 474 (440–520) | `--moat` (DOW=Wed) | 25423195854 |
| 09:42 | 19:42 | ✅ success | 582 (530–590) | `--ugc` ⚠️ HeyGen 401 (disabled) | 25427901717 |
| 11:44 | 21:44 | ✅ success | 704 (680–760) | `--yt-short` (slot 2) | 25433231179 |
| 12:14 | 22:14 | ✅ success | 734 (680–760) | `--yt-short` (slot 2 retry) | 25434589607 |
| 13:12 | 23:12 | ✅ success | 792 (770–860) | `--evening` | 25437410499 |
| 15:22 | 01:22 +1 | ✅ success | 922 (890–970) | `--reel` (slot 2) | 25444510667 |
| 16:45 | 02:45 +1 | ⚠️ cancelled | 1005 (off-schedule) | `--sync-only` | 25448684111 |
| 19:56 | 05:56 +1 | ✅ success | 1196 (1070–1200) | `--analytics` | 25457797942 |

## Content Published Today

Cross-referenced against the `autoposter-state` branch (last commit `5a4b8352` at 07:18 UTC). The remote log has no entries after 07:18 UTC, meaning runs from 09:42 UTC onwards either ran in sync-only/dedup-skip mode OR posted but failed to commit state. The workflow only commits when `state.json`/`autoposter.log` actually changed.

| Scheduled cron (UTC / IST) | Mode | Status | Detail |
|---|---|---|---|
| 03:30 / 09:00 IST | Morning post | ❌ **Missing** | No GH Actions run fired between 03:30 and 06:16 UTC. The `30 3 * * *` cron silently dropped — first run of the day was 06:16 UTC (~2h46m late, mode resolved to `--reel`). |
| 04:30 / 10:00 IST | YT Short slot 1 | ❌ **Missing** | Same drop window. No `30 4 * * *` cron firing. |
| 05:30 / 11:00 IST | Reel slot 1 | ✅ **Published** | 06:16 UTC run produced `seasonal_shift` reel for **Bhaderwah** — published to YouTube (`8SRCI` → `yiPk2-bAges`), Instagram (`HFqn5` → `18044400302783384`), Facebook (`IJHuX` → `1871378490210689`). |
| 07:30 / 13:00 IST | Visual slot (Wed → MOAT) | ✅ **Published** | 07:14 UTC run (with mode-step running just past the 440-min boundary) produced `data_provenance` MOAT for **Guptkashi** — published to Instagram (`TqXA1` → `18150917497468400`) and Facebook (`SLFHJ` → `1362905502526183`). YouTube skipped (no video for moat). |
| 07:54 cron retry | MOAT | 💥 **Failed/Cancelled** | Concurrency-cancelled (group `autoposter` with `cancel-in-progress: true`). Likely overlap with the 07:14 run still finishing. No content gap — 07:14 already filled the slot. |
| 09:00 / 14:30 IST | UGC (Wed/Sat) | 💥 **Failed (HeyGen 401)** | Cron is **commented out** in workflow. The 09:42 UTC run (closest to UGC window) may have triggered UGC mode internally; the 2026-05-05 09:31 UTC run had identical 401. **Pre-existing issue:** HeyGen API key is unauthorized; needs re-issue. |
| 10:00 / 15:30 IST | Reel-map | ❌ **Missing** | No GH Actions run in the 590–640 window (09:50–10:40 UTC). The 09:42 UTC run was 8 min early and resolved to `--ugc` mode, not `--reel-map`. |
| 10:30 / 16:00 IST | Flow story | ❌ **Missing** | No GH Actions run in the 640–680 window (10:40–11:20 UTC). The `30 10 * * *` cron silently dropped. |
| 11:30 / 17:00 IST | YT Short slot 2 | 💥 **Failed (no state push)** | 11:44 UTC run completed "success" but no commit landed on `autoposter-state`. Either posted-and-skipped-commit, or failed silently mid-run. **Suspect 1:** YouTube OAuth still 401'ing (see audit_2026-04-20 Issue #4). 12:14 UTC retry also no-op. |
| 13:00 / 18:30 IST | Evening post | 💥 **Failed (no state push)** | 13:12 UTC run completed "success" but no commit. Should have produced a `score_card`/`reality_check`-style feed post — none visible. Worth re-triggering. |
| 15:00 / 20:30 IST | Reel slot 2 | ⏭️ **Skipped (dedup, expected)** | 15:22 UTC run completed "success" — almost certainly hit `Already posted reel today` from the 06:16 Bhaderwah reel. No content gap. |
| 18:00 / 23:30 IST | Analytics sync | ⏭️ **Likely skipped** | 19:56 UTC run completed "success" — analytics may have synced but produced no `analytics.json` diff to commit. State branch shows `last_sync` still at 07:17 UTC, so analytics didn't run cleanly. |

### Confirmed published (May 6 UTC)
- 1 reel (`seasonal_shift` / Bhaderwah) → YT + IG + FB ✅
- 1 MOAT (`data_provenance` / Guptkashi) → IG + FB ✅

### Local runs (not from CI)
- 09:58 UTC `MORNING · sync-only` — fired locally on user's machine, not CI; only present in local log, not on `autoposter-state`.

## Issues Found

1. **🚨 GitHub Actions silently dropped 3 cron triggers (Morning, YT-short slot 1, Flow story).** No GH Actions run created for `30 3 * * *`, `30 4 * * *`, or `30 10 * * *` on 2026-05-06 UTC. This is the third consecutive day showing GH Actions cron drops (see audit_2026-04-20 Issue #2 — that was a single 02:00 UTC drop; this is a wider pattern). Likely contributors: heavy commit activity on `main` (~20 commits on May 6 UTC including pomelli waves 1/2/3 and cinematic round 2/3/4), which is known to deprioritize scheduled workflows in GH Actions.

2. **💥 4+ runs completed "success" but pushed no state changes (09:42, 11:44, 12:14, 13:12, 15:22, 19:56 UTC).** Only 06:16 and 07:14 UTC produced commits to `autoposter-state`. The other runs either:
   - Hit `Already posted today` dedup (expected for the 15:22 reel slot 2 retry).
   - Failed silently mid-`autoposter.py` (most likely for YT-short slot 2 if YT OAuth is still 401'ing, and for evening post).
   - Produced no diff to `state.json` (analytics, sync-only).
   Without `gh run view --log` access from the audit sandbox, we can't distinguish silent-failure from clean-skip. **Recommend the user pull logs for runs 25433231179 (YT short 2) and 25437410499 (evening post) to confirm.**

3. **⚠️ Concurrency cancellation at 07:54 UTC.** The 07:14 MOAT run was still pushing state when the next cron fired; `cancel-in-progress: true` cancelled the 07:54. No content gap (the 07:14 run had already published), but logging this because if it happens repeatedly it could mask real failures.

4. **🔁 HeyGen 401 (UGC).** The 09:42 UTC run (TOTAL_MINS=582, UGC window) and the prior day's 09:31 UTC run both hit `401 UNAUTHORIZED` at `https://api.heygen.com/v3/users/me`. The cron line for UGC is **already commented out** in the workflow ("DISABLED: HeyGen balance too low"), but mode-detection still routes to `--ugc` for the 530–590 window — meaning any other cron landing in that window will attempt UGC and fail. **Recommend either (a) topping up HeyGen + re-issuing key, or (b) gating mode-detection on the same comment-out as the cron.**

5. **📜 `last_sync` stuck at 07:17 UTC.** Despite the 19:56 UTC analytics run "succeeding", the `autoposter-state` `state.json` still shows `"last_sync": "2026-05-06T07:17:09.718914+00:00"` — meaning analytics didn't actually run or didn't write its sync timestamp. Worth investigating after the YT-OAuth/HeyGen fixes are in.

6. **📁 Local `state.json` is stale.** The local copy at `/Users/ashishtaneja/Desktop/India Travel Planner/nakshiq-autoposter/state.json` (mtime 2026-05-06 08:24 UTC) has only 38 `post_log` entries; the canonical `autoposter-state` branch has 62. This isn't a real issue — CI pulls from the side branch each run — but if the user manually invokes the autoposter locally (as happened at 09:58 UTC on May 6), it will run with stale state and risk re-publishing.

## Actions Taken

**No re-triggers were dispatched from the audit sandbox.** The sandbox has no `gh` CLI installed and no GitHub auth token, so `gh workflow run autoposter.yml -f mode=…` cannot run from here. (Same constraint as audit_2026-04-20.) Read-only audit was completed via the unauthenticated GitHub REST API.

**Suggested manual commands (run from the user's Mac where `gh` is authenticated):**

```bash
cd ~/Desktop/India\ Travel\ Planner

# Pull logs for the suspect-silent-failure runs to confirm if they posted or skipped
gh run view 25433231179 --log | tail -200   # 11:44 UTC YT short slot 2
gh run view 25437410499 --log | tail -200   # 13:12 UTC evening post

# Re-trigger the 3 cleanly-missed slots from May 6 UTC (no -force, respects dedup)
# These will post fresh content for 2026-05-07's date keys.
gh workflow run autoposter.yml -f mode=yt-short
gh workflow run autoposter.yml -f mode=flow-story
# Morning post has no dedicated mode; use `evening` or rely on the next 03:30 UTC cron

# If the 11:44 run silently failed and YT-short slot 2 didn't post:
# (Don't re-trigger if you confirm via logs above that it actually posted to YouTube/IG.)
```

**Re-triggers NOT recommended:**
- Reel slot 2 (15:22 was correct dedup-skip)
- MOAT/visual (07:14 already filled the slot)
- Reel-map (the 09:42 cron landed in UGC window — separate issue)
- Analytics (low priority; the next 18:00 UTC cron will catch up)

## Summary

```
🔍 Nakshiq Autoposter Daily Audit — 2026-05-07 (covers UTC 2026-05-06)

📊 GitHub Actions: 10 runs (8 succeeded, 1 failed/cancelled, 1 cancelled)

Content Published Today:
✅ Reel (Bhaderwah / seasonal_shift) → YT + IG + FB
✅ MOAT (Guptkashi / data_provenance) → IG + FB
❌ Morning post — cron dropped
❌ YT Short slot 1 — cron dropped
❌ Flow story — cron dropped
💥 Reel-map — wrong-window cron, ran as UGC and 401'd
💥 YT Short slot 2 — ran but no state push (suspect YT OAuth)
💥 Evening post — ran but no state push
⏭️ Reel slot 2 — correctly dedup-skipped
⏭️ Analytics — ran but didn't update last_sync

Issues Found:
1. 3 cron drops (03:30, 04:30, 10:30 UTC) — third day in a row
2. 4+ "success" runs with no state-branch commit — likely silent failures
3. HeyGen 401 still blocking UGC window
4. last_sync stuck at 07:17 UTC despite 19:56 analytics run
5. Local state.json drift (low priority)

Actions Taken:
None — audit sandbox has no gh CLI / GitHub auth.
Suggested manual re-trigger commands listed above.

⚠️ 5 issues found — needs user action this morning before today's slots fire.
```

Saved to `nakshiq-autoposter/audit_2026-05-07.md`.
