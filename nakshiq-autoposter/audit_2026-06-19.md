🔍 **NakshIQ Autoposter Daily Audit — 2026-06-19** (generated ~23:05 AEST)

**Method note.** Ground truth read from `origin/autoposter-state` (the local `main` copies of `state.json` / `autoposter.log` are stale — local log last written June 8, local state last posted Apr 30). `gh` CLI is **not available in this sandbox**, so GitHub Actions run counts are *inferred from dated `chore: autoposter state …` commits* (one commit ≈ one successful run). All times are **AEST (UTC+10; no DST in June)**; log timestamps are UTC and were converted (+10h). The poster self-reports `weekday=4` = Friday ✓.

This audit covers **June 19 progress to date**. At 23:05 AEST we are early in the Friday AEST posting cycle — only the first two slots are due; the rest fire later in UTC (the live pattern lands slots ~14:00 / 17:30 / 20:45 / 23:10 UTC) and are **not yet due**.

---

📊 **GitHub Actions (inferred from state commits)**

- **June 19: 2 runs, 2 succeeded, 0 failed** — morning feed (`d1f8f86b`, 18:48 AEST) + YT-short slot 1 (`199a415d`, 19:22 AEST). Same early-cycle stage as the June-18 audit at this hour.
- **Git push: 0 failures.** Both state commits landed cleanly. No `rejected` / `failed to push` entries in today's log.

---

**Content Published — June 19 (so far)**

- ✅ **Morning feed** — fired 18:45 AEST · format `v2_tourist_trap_split` · dest **roopkund**.
  - **FB Reel — ✅ Published** (Outstand `cIaGL` · Platform `985926810931948`) · 18:48 AEST.
  - **IG Reel — ⚠️ Queued-Unconfirmed** (`vDIrg`) · 18:47 AEST — polling timed out after 40s; known false-alarm pattern, analytics sync typically reconciles to `published`.
  - Note: the FB leg's `v2_tourist_trap_split` pool was again exhausted (all candidates cooldown-blocked or no current-month row) → fell back to `score_card`. **Unlike June 18 it still posted**, because `roopkund` was locked as the shared best-destination and was not cooldown-blocked. So the morning feed delivered to both platforms today.
- ✅ **YT-short slot 1** — fired 19:18 AEST · format `nakshiq_score` · dest **khandala** · music `edm-1001`.
  - **IG cross-post — ✅ Published** (Outstand `lMykK` · Platform `17880397215458655`) · 19:22 AEST.
  - **YouTube — ⚠️ Queued-Unconfirmed** (`QZSUy`) · 19:21 AEST — same 40s polling timeout; usually reconciles. FB leg correctly skipped (YT-shorts → YouTube + IG only).

**Not yet due (live cadence, drift-adjusted):** YT-short slots 2 & 3, evening feed, analytics sync (~09:30 AEST Jun 20), engagement pull.

**Paused / not applicable today:**

- ⏭️ **Tourist map** — Tue/Thu/Sat only → not scheduled Friday. Correctly absent.
- ⏸️ **Reels / Canva visual / Infographic** — these dispatch modes are paused pending 1k followers and are **not part of the live daily cadence** (last fired: infographic Jun 9, tourist-map Jun 15, reel-map Jun 13). Their absence is expected, not a gap. *(The task-file schedule lists these as daily/MWF — that schedule is stale; audited against the live cadence per prior runs.)*

---

**Issues Found**

1. ⚠️ **Two queued-unconfirmed posts today** — IG morning-feed reel (`vDIrg`, roopkund) and YouTube YT-short (`QZSUy`, khandala). Both are the known Outstand polling-timeout pattern (accepted by Outstand, no platform confirmation within 40s). They historically reconcile to `published` at the analytics sync. **Watch the ~09:30 AEST Jun 20 analytics sync flips both to published.** Side-effect: because YouTube (`GAh5p`) wasn't confirmed, it didn't get its same-day guard, so the next YT-short run could re-attempt YouTube — minor, self-correcting.
2. ⚠️ **`v2_tourist_trap_split` pool still thin** — FB leg fell back to `score_card` again (same pool-exhaustion family as the June-18 morning-feed failure). It posted today only because the shared-best-dest happened to be eligible. The underlying fix (re-pick a fresh *eligible* dest / widen the anti-trap in-month pool before falling back) from the June-18 audit still stands.

**Actions Taken**

- **No re-triggers performed.** No slot is ❌ missing or 💥 failed — both due slots (morning feed, YT-short 1) posted; the remaining slots are not yet due. The two queued-unconfirmed items are warnings (not confirmed failures) and aren't re-triggerable. `-force` was **not** used (and `gh` is unavailable in this sandbox regardless).

**Recommended (founder / dev)** — carried from June-18, not blocking:

- Feed fallback fix: when the chosen format's pool is exhausted, re-pick a fresh *eligible* destination (or alternate format) before skipping, and widen/refresh the `v2_tourist_trap_split` in-month pool.
- If queued-unconfirmed keeps recurring on the IG/YT legs, consider lengthening the publish-confirmation poll window beyond 40s/8 polls to cut false-alarm noise.

---

✅ **All clear on slots due so far** — morning feed + YT-short 1 both delivered (FB + IG reels, IG short). **2 queued-unconfirmed warnings to watch** (expected to reconcile at next analytics sync). The June-18 morning-feed no-post did **not** recur. Remaining Friday slots pending / not yet due.
