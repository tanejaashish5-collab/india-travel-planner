# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-27 (Wednesday, AEST)

**Audit run at:** 23:03 AEST (13:03 UTC), 2026-05-27
**Data source:** `origin/autoposter-state` branch (latest commit `9fd18a51` at 18:18 AEST / 08:18 UTC)

> **Note on `gh` availability:** This run was executed from a sandboxed environment without the `gh` CLI installed and no GitHub PAT in the env, so GitHub Actions run history could not be queried directly. All audit findings below are derived from the `autoposter-state` branch (where each GHA run pushes the updated `state.json`, `autoposter.log`, and `post_log.jsonl`). Cron-skip detection is therefore inferred from the *absence* of expected state commits rather than from a direct `gh run list`.

---

## 📊 Today's runs (from `autoposter-state` branch)

| Time (UTC) | Time (AEST) | Slot | Result |
|---|---|---|---|
| 07:20 → 07:22 | 17:20 → 17:22 | **MORNING** (cron 03:17 UTC scheduled) | ✅ Published (4 h late) |
| 08:16 → 08:18 | 18:16 → 18:18 | **YT SHORT #1** (cron 04:17 UTC scheduled) | ✅ Published (4 h late) |
| 11:47 | 21:47 | **YT SHORT #2** | ⚠️ No state commit yet (≈ 1 h 15 min past due — within typical GHA delay window) |
| 14:17 | 00:17 (Thu) | **EVENING** | ⏳ Future |
| 18:17 | 04:17 (Thu) | **Analytics sync** | ⏳ Future |
| 21:17 | 07:17 (Thu) | **Engagement pull** | ⏳ Future |

GHA scheduler is again running ~4 h behind the cron, consistent with the documented "heavy minute mark" delay pattern. The workflow file already lands every cron on `:17` or `:47` to mitigate this; the delay is upstream-GHA, not a workflow bug.

---

## ✅ Content actually published today

From `post_log.jsonl` on `autoposter-state` (4 entries, all today):

1. **17:22 AEST** — Instagram (`m8EAd` / nakshiq) · `v2_women_solo_brief_video` · **Kabini** · post_id `OSzRv` · media `kabini.mp4`
2. **17:22 AEST** — Facebook (`PdMu0` / Nakshiq) · `v2_women_solo_brief_video` · **Pahalgam** · post_id `yV4Zj` · media `pahalgam.mp4`
3. **18:17 AEST** — YouTube (`@naksh-iq`) · `yt_short.this_vs_that` · **Kasauli** · post_id `esO2r` · platform id `6hpoBn0h8qs`
4. **18:18 AEST** — Instagram (`nakshiq`) · `yt_short.this_vs_that` · **Kasauli** · post_id `C8afF` · platform id `18102238267840545`

`posted_today` in `state.json` confirms IG (`m8EAd`), FB (`PdMu0`), YT (`GAh5p_yt_short`), and IG (`m8EAd_yt_short`) have all checked in for 2026-05-27.

---

## ⚠️ Issues found

### 1. ⚠️  Instagram morning post "queued unconfirmed" — Kabini (OSzRv)

`2026-05-27 07:22:14  WARNING   [instagram/nakshiq] ⚠️  Queued unconfirmed (ID=OSzRv)`

The `wait_for_publish` polling exhausted 8 attempts in 40 s without platform confirmation. Outstand accepted the upload, but Instagram never returned a confirmed publish event during the polling window. **The post may still have gone live afterwards — verify manually on @nakshiq IG profile.** Facebook companion (Pahalgam, `yV4Zj`) confirmed cleanly.

This is the same `queued_unconfirmed` warning that hit `cPl6L` on 2026-05-26 (dont_go_here YT short → IG) — looks like an intermittent Outstand/Instagram polling timeout, not a publish failure.

### 2. ⚠️  `post_outcomes.jsonl` write failed — UTF-8 surrogate encoding

`2026-05-27 08:18:15  WARNING   Could not write post_outcomes entry: 'utf-8' codec can't encode characters in position 110-111: surrogates not allowed`

The YT Short Kasauli entry could not be written to `post_outcomes.jsonl` because the caption contains surrogate pair characters (likely an emoji that Python's default codec rejects). The main `post_log.jsonl` write succeeded, so analytics will still see this post — but the outcomes tracker is missing this row. Reproducible bug: `data/post_log.jsonl` uses `ensure_ascii=False` correctly; `post_outcomes.jsonl` writer probably doesn't pass `errors='surrogatepass'` or `errors='replace'`. Worth a one-line fix later, not urgent.

### 3. ⚠️  Cron drift (≈ 4 h late) — not a defect, just GHA reality

Morning post was scheduled for 03:17 UTC (13:17 AEST), fired at 07:20 UTC (17:20 AEST) — same pattern as YT Short #1 (04:17 → 08:16 UTC). This is consistent with the 4-hour GHA scheduling delay that's been recurring throughout May. No action required; the workflow already lands every cron on `:17`/`:47` per the 2026-05-10 rewrite to avoid heavy minute marks.

### 4. ℹ️  YT Short #2 (cron 11:47 UTC) — not yet observed

Cron fired ~1 h 15 min ago at 21:47 AEST; no state commit since 08:18 UTC. Given today's 4-hour GHA delays on the morning and YT #1 crons, this slot is **most likely still inside the normal delay window** (would push it out to ~01:47 AEST Thursday in the worst case). Re-classify after midnight AEST if it still hasn't appeared.

### 5. ℹ️  No git push failures detected

No "rejected" or "failed to push" tokens in today's log section of `autoposter.log`. State commits at 07:22 UTC and 08:18 UTC both landed on `autoposter-state` cleanly.

---

## ❗ SKILL.md schedule is stale — DO NOT use as the source of truth

The schedule embedded in this scheduled-task SKILL.md still lists:

- Morning feed post 6:30 PM – 9:00 PM AEST
- Reel slots 1/2/3 at 9 PM / 2 AM / 6 AM
- Tourist map (Tue/Thu/Sat)
- Canva visual 12:30 AM
- Infographic (Mon/Wed/Fri)
- YT Short slots 1/2/3
- Analytics sync 9:30 AM

These slots were all **removed or paused on 2026-05-16** in the "cadence cut" (founder note in `apps/web/CLAUDE.md` / commit history: pivoting from 6 → 2 IG+FB posts/day until 1k followers crossed). The **current actual schedule** is what's in `.github/workflows/autoposter.yml`:

```
03:17 UTC  →  13:17 AEST  ·  Morning post   (daily IG+FB)
04:17 UTC  →  14:17 AEST  ·  YT Short #1    (daily YT only)
11:47 UTC  →  21:47 AEST  ·  YT Short #2    (daily YT only)
14:17 UTC  →  00:17 AEST  ·  Evening post   (daily IG+FB)
18:17 UTC  →  04:17 AEST  ·  Analytics sync (internal)
21:17 UTC  →  07:17 AEST  ·  Engagement pull (internal)
22:17 UTC Sun → Mon 08:17 AEST · Weekly engagement digest (internal)
```

**Tourist map, Canva visual, Infographic, Reel slots, and YT Short slot 3 no longer exist in the workflow.** Wednesday-specific check (infographic) returns N/A. The SKILL.md should be updated by the founder so future audits don't keep listing phantom slots.

---

## 🔧 Actions taken

None.

- Cannot re-trigger via `gh workflow run` because `gh` is not installed in this sandbox and no `GH_TOKEN` / `GITHUB_TOKEN` is available in the environment.
- Even if a re-trigger were possible, **no slot has crossed the "definitively missed" threshold yet** — today's two scheduled-but-not-yet-observed slots (YT Short #2 at 11:47 UTC, Evening at 14:17 UTC) are still inside the typical 4-hour GHA delay window observed on this account.
- Re-triggering early would risk duplicate content (per the SKILL's own "NEVER use `-force`" rule and the cost-aware operating rules in `CLAUDE.md`).

### Recommended manual follow-up (for the founder, not auto-executed)

1. Verify on Instagram that **Kabini reel `OSzRv`** is actually live (the `queued_unconfirmed` warning means we can't tell from logs alone).
2. After ~00:00 AEST Thursday, glance at `gh run list --workflow=autoposter.yml --limit 8` on the local machine — if YT Short #2 (cron 11:47 UTC) and Evening (cron 14:17 UTC) both show `success`, all clear. If either shows `failure` or is missing, re-trigger with `gh workflow run autoposter.yml -f mode=yt-short` or `mode=reel` (NEVER the `-force` variant).
3. Optional follow-up bug fix: add `errors='surrogatepass'` (or move to `json.dump(..., ensure_ascii=False)` with proper byte writes) to whichever writer emits `post_outcomes.jsonl`, to stop swallowing posts whose captions contain emoji surrogate pairs.

---

## 🩺 Summary

- ✅ **2 / ? scheduled slots confirmed published** (Morning + YT Short #1).
- ⚠️ **1 unconfirmed publish** (Instagram Kabini reel `OSzRv` — manual verification advised).
- ⏳ **YT Short #2 & Evening still inside delay window** — not yet declared missed.
- 🐛 **1 minor bug** (UTF-8 surrogate write failure in `post_outcomes.jsonl`).
- 📌 **SKILL.md schedule is out of date** — must be updated to match current `autoposter.yml` (6 daily slots, not 11+).
- 🚫 **No auto re-triggers performed** (no `gh` in sandbox + nothing definitively missed yet).

**⚠️ 1 confirmed issue + 1 stale SKILL.md to refresh.**
