# Nakshiq Autoposter Daily Audit — 2026-04-26 (Sunday)

Audit run time: **2026-04-26 14:50 AEST** (04:50 UTC)
Schedule reference: AEST (Sydney)
Prior audit run: 2026-04-25 ~23:18 AEST

---

## GitHub Actions

10 scheduled runs since the audit window opened (Sat 14:00 UTC); **most recent run: 2026-04-25 19:05 UTC = 05:05 AEST Sun**. **No GH Actions activity for the last 9h45m.**

| AEST time today | UTC time | Cron slot | Result |
|---|---|---|---|
| 01:44 Sun | 2026-04-25 15:44 UTC | Reel slot 2 (20:30 IST) | success — IG+FB Reel published (Outstand=zIhdP / 9YFkQ) |
| 05:05 Sun | 2026-04-25 19:05 UTC | Analytics sync (23:30 IST) | success — analytics summarised (175 published / 46 failed across 230 tracked) |
| 13:30 Sun | 2026-04-26 03:30 UTC | Morning post (09:00 IST) | **❌ DID NOT FIRE on GitHub Actions** |
| 14:30 Sun | 2026-04-26 04:30 UTC | YT Short slot 1 (10:00 IST) | ⏳ should have fired ~20 min ago — no run yet |

GH Actions cron has been silent since the workflow file was edited at 10:26 AEST today (commit `2e5e1a1 perf(vercel): isolate autoposter to side branch + tighten ignore regex`). GitHub commonly suppresses scheduled triggers for several hours after a `.github/workflows/*.yml` change.

---

## Local Cowork Scheduled Tasks

The Mac runs its own duplicate set of autoposter triggers via Cowork scheduled tasks (independent of GH Actions). Today's runs:

| Cowork task | Last run (AEST) | Outcome |
|---|---|---|
| `nakshiq-autoposter-daily` (morning) | 2026-04-26 13:32 | **💥 Failed mid-upload.** Started rendering data_carousel for verinag/binsar/dharamshala/gopeshwar/lansdowne. Uploaded 5 of 7 slides to R2, then log abruptly stops. No "Published" / ✅ confirmation, no state-save line. |
| `nakshiq-autoposter-evening` | yesterday 20:09 | not due yet (next: tonight 20:09 AEST) |
| `nakshiq-autoposter-moat` | 2026-04-24 16:38 | skipped (Mon/Wed/Fri only) |
| `autoposter-daily-audit` | 2026-04-25 23:18 | running now (this audit) |

Two additional local invocations after the failed morning attempt also crashed:

```
2026-04-26 13:43:47  ERROR  OUTSTAND_API_KEY not set. Exiting.
2026-04-26 13:45:24  ERROR  OUTSTAND_API_KEY not set. Exiting.
```

The Mac's `nakshiq-autoposter/.env.local` does contain `OUTSTAND_API_KEY=…` so these were almost certainly invoked from a shell that didn't auto-load `.env.local`. Worth investigating whether the Cowork task wrapper started exec'ing without env propagation after the recent `.env auto-load` change in commit `a8f36e9`.

---

## What was actually published today

Cross-checking `state.json` (last_sync = 2026-04-25T23:59 AEST):

```
posted_today entries dated 2026-04-26: NONE
```

So **zero content has been confirmed published on Sunday Apr 26 yet**. The 01:44 AEST Reel slot 2 IG+FB run from GH Actions is on the platform side (Outstand confirmed) but its state-save commit has not propagated to the local checkout — last autoposter chore commit on `main` is `70bf2c0 chore: autoposter state 2026-04-25 19:06 UTC`.

---

## Schedule status (per task description, AEST)

| Slot | Time AEST | Schedule | Today's status |
|---|---|---|---|
| Canva visual | 12:30 AM Sun | daily | ⚠️ no run found in window — but Sat already covered Canva via 13:00 IST visual rotation |
| Reel slot 2 | 02:00 AM Sun | daily | ✅ Published (GH Actions 01:44 AEST → IG+FB) |
| YT Short slot 3 | 03:00 AM Sun | daily | ❌ no GH Actions run, no state entry |
| Reel slot 3 | 06:00 AM Sun | daily | ❌ no GH Actions run, no state entry |
| Analytics sync | 09:30 AM Sun | daily | ✅ Ran (GH Actions 05:05 AEST — early but valid) |
| YT Short slot 2 | 12:00 PM Sun | daily | ❌ no GH Actions run, no state entry |
| Morning feed | 6:30–9:00 PM Sun | daily | ⏳ pending (also: local 13:32 attempt failed mid-upload) |
| Tourist map | 8:30 PM | Tue/Thu/Sat | ⏭️ correctly skipped (Sunday) |
| Reel slot 1 | 9:00 PM Sun | daily | ⏳ pending |
| YT Short slot 1 | 9:00 PM Sun | daily | ⏳ pending |
| Infographic | 11:00 PM | Mon/Wed/Fri | ⏭️ correctly skipped (Sunday) |

**Note on schedule mismatch:** the actual `.github/workflows/autoposter.yml` cron grid is keyed on IST (UTC+5:30), not the AEST schedule listed in the task. Active IST slots are 09:00, 10:00, 11:00, 13:00, 15:30, 17:00, 18:30, 20:30, 23:30 — none of which line up cleanly with the "12 PM AEST YT Short slot 2" or "12:30 AM AEST Canva" entries in the audit task spec. The Sunday visual slot in the actual cron is **pomelli-visual at 13:00 IST** (17:30 AEST tonight), not Canva. Recommend reconciling the audit-task spec with `autoposter.yml` cron windows before next audit cycle.

---

## Issues found

1. **GH Actions cron stopped firing** at 19:05 UTC Apr 25 — likely tied to today's 10:26 AEST workflow edit (`2e5e1a1`). Expect natural recovery within a few hours; if 04:30 UTC slot continues to be skipped beyond ~05:30 UTC, manually nudge with a workflow_dispatch.
2. **Local morning post failed mid-render** at 13:32 AEST — 5/7 carousel slides uploaded to Outstand R2, then process died before publication. State unchanged. The slides are now in Outstand limbo (uploaded but never attached to a post).
3. **OUTSTAND_API_KEY missing in 2 local invocations** at 13:43 and 13:45 AEST. Process and origin unknown — env loader may have been broken by recent `.env auto-load` refactor (commit `a8f36e9`).
4. **`autoposter-state` branch does not exist on origin** — workflow change at 10:26 AEST expects to push state there, with self-bootstrap. Will resolve itself on the first successful GH Actions run, but until then state continues to be written to `main`.
5. **Stale audit verifier** — pid 53081 is `osascript … sleep 120 && … gh run list` still hung from a previous audit's "wait 90s and verify" step (running for at least an etime of 6m). Harmless but consider terminating.

---

## Actions taken

**No re-triggers fired this run.** Reasoning:

- The "morning post" content type is **not** in the allowed re-trigger mode list (`tourist-map`, `canva-visual`, `reel`, `infographic`, `yt-short`). The local Cowork task already attempted it at 13:32; partial slide uploads sit in Outstand. Re-triggering via a different content type would compound the mess.
- The OUTSTAND_API_KEY failures point to an environment / env-loader bug, not a missed slot — re-triggering won't solve the root cause.
- The actual GH Actions cron is likely paused post-edit; manually firing now would put noise on top of an already-degraded picture. Better to let cron recover and audit again at 23:00 AEST.

If the user wants to push the morning post manually after reviewing this report, the cleanest path is `gh workflow run autoposter.yml -f mode=force` from the Mac (which uses the existing time-window logic without the `-force` slot variants flagged as duplicate-risk).

---

## ⚠️ 5 issues found — needs attention

Top priorities for the user:

1. Verify GH Actions cron resumes by ~05:30 UTC (15:30 AEST). If not, manually dispatch.
2. Investigate the OUTSTAND_API_KEY error — likely env loader regression from commit `a8f36e9`.
3. Decide what to do with the orphaned 5 carousel slides uploaded to Outstand at 13:32 AEST (verinag/binsar/dharamshala/gopeshwar carousel — slide 5 was the last successful upload).
4. Confirm `autoposter-state` branch bootstraps correctly on the next successful GH Actions run.
5. Consider killing pid 53081 (stale `osascript` from previous audit's verifier).
