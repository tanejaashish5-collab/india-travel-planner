# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-25 (Mon)

_Generated 2026-05-26 ~01:10 AEST. All times AEST (Sydney, UTC+10). Covers the
2026-05-25 run-day; the last two internal slots (analytics sync, engagement
pull) were not yet due at audit time._

## 📊 GitHub Actions: UNAVAILABLE THIS RUN

`gh` CLI is **not installed** in this audit sandbox and no GitHub token is
present, so run-level Actions history (success/failure counts, git-push errors)
could **not** be retrieved. This audit was reconstructed instead from the
`autoposter-state` git branch — the workflow commits `state.json` there after
runs — plus `origin/main` commit history. Treat publish findings below as
best-effort from state snapshots, not confirmed run conclusions.

## ⚠️ The task schedule is stale — audited against the real workflow instead

The schedule block in this task's SKILL.md (morning feed, 3 reel slots, tourist
map, canva visual, infographic, 3 YT slots) describes the **pre-2026-05-16**
cadence. `.github/workflows/autoposter.yml` underwent a **cadence cut on
2026-05-16** (6 IG+FB posts/day → 2; reels, visual slot, tourist-map, canva,
infographic, reel-map and flow-story all **PAUSED** until the account crosses
1k followers). The reel/visual/map/infographic modes the task asks me to
re-trigger no longer run on a schedule — re-triggering them would push content
the user deliberately paused.

**Actual active schedule (UTC cron → AEST):**

| Slot | Cron (UTC) | AEST | Cadence |
|---|---|---|---|
| Morning post (IG+FB) | 03:17 | 13:17 Mon | daily |
| YT Short slot 1 | 04:17 | 14:17 Mon | daily |
| YT Short slot 2 | 11:47 | 21:47 Mon | daily |
| Evening post (IG+FB) | 14:17 | 00:17 Tue | daily |
| Analytics sync | 18:17 | 04:17 Tue | daily, internal |
| Engagement pull | 21:17 | 07:17 Tue | daily, internal |
| Weekly digest | Sun 22:17 | Mon 08:17 | Sun only — N/A today |

## Content Published Today

Evidence: `posted_today` snapshots across 8 `state.json` commits on
`origin/autoposter-state` (18:45 → 00:27 AEST).

- ✅ **YT Shorts** — published. A snapshot at 18:45 AEST recorded **2** YT shorts
  (accounts `GAh5p`, `m8EAd`); the latest snapshot (00:27 AEST) shows 1
  (`GAh5p`). At least one, likely both YT slots fired.
- ✅ **Morning post (IG `m8EAd` + FB `PdMu0`) + IG story** — content published;
  recorded in `posted_today` between 18:56–22:12 AEST.
- ⚠️ **Evening post (00:17 AEST)** — **unconfirmed**. The latest state snapshot
  (00:27 AEST) shows `posted_today` rewound to a stale baseline with only the YT
  short — no evening IG/FB entry.
- ⏭️ Reel slots / Tourist map / Canva visual / Infographic / Reel-map —
  **paused since 2026-05-16**, correctly did not run (Infographic would be
  Mon-scheduled under the old cadence; Tourist map is Tue/Thu/Sat anyway).
- ⏳ Analytics sync (04:17 Tue) & Engagement pull (07:17 Tue) — not yet due.

## Issues Found

1. **state.json churn / rewind on `autoposter-state` branch.** Across today's 8
   commits, `last_sync` flip-flops between live `2026-05-25` values and a stale
   `2026-04-30` baseline, and `posted_today` was wiped to `{}` twice (22:53,
   23:52 AEST) then repopulated from the old baseline. The latest committed
   snapshot (`0363a8f0`, 00:27 AEST) carries the **2026-04-30 baseline** with a
   near-empty `posted_today`. A wiped `posted_today` breaks the same-day dedup
   guard and risks duplicate / empty posts on the next scheduled run — the exact
   failure mode in prior incident history (3× duplicate publish).

2. **Live incident today: empty-placeholder posts.** `origin/main` shows an
   URGENT fix — `b8eee273` "stop empty-placeholder posts shipping live
   (kalimpong v4 incident)" (18:27 AEST) — plus `ae830304` rewriting 6 orphan
   CSV formats (18:39 AEST). Empty-placeholder content was reaching live feeds
   before the fix landed.

3. **Morning / YT scheduled fires not independently verifiable.** No state
   commit sits near the 13:17 / 14:17 AEST cron times; today's 8 state commits
   cluster 18:45–00:27 AEST, matching the user's evening dev/test session
   (code commits at 18:27 & 18:39 AEST). Content did publish, but whether via
   the scheduled crons or manual test dispatches cannot be told from git alone —
   GH Actions run logs (unavailable) would be needed to confirm.

4. **GH Actions visibility gap.** No `gh` / token in this sandbox → could not
   count run successes/failures or scan Actions output for `rejected` /
   `failed to push` git errors.

5. **Local `autoposter.log` has no 2026-05-25 entries** (last entry
   2026-05-24 17:37). Expected, not a defect — the log is not git-tracked, so
   cloud GH Actions runs never write to this local copy.

## Actions Taken

**None — report-only, by design.**

- No workflow re-trigger is possible (`gh` unavailable).
- Re-triggering would be wrong even if possible: the modes the task lists
  (`reel`, `tourist-map`, `canva-visual`, `infographic`) are paused — firing
  them violates the 2026-05-16 cadence strategy. The active modes (morning, YT)
  already published, and re-triggering during the current state.json churn would
  risk duplicate posts.
- Per the scheduled-task guardrail, a report is the correct output here.

## Recommended Follow-ups (for Ashish)

1. **Resolve the `autoposter-state` state.json churn.** The branch tip
   (`0363a8f0`) has `last_sync` rewound to 2026-04-30 and `posted_today` nearly
   empty. Reconcile it with the correct live state before the next morning cron
   (13:17 AEST), or the dedup guard may misfire.
2. **Confirm the 00:17 AEST evening post** actually published — it is not
   visible in state.
3. **Update this task's SKILL.md schedule** to the post-2026-05-16 workflow
   (drop reel/tourist-map/canva/infographic; the re-trigger mode list in step 7
   is also stale).
4. **Restore GH Actions visibility** — install + authenticate `gh` in the audit
   environment, or hand the task a token, so future audits can report real run
   conclusions and push errors.

---

⚠️ **5 issues found** — 1 state-integrity risk, 1 live incident (fix shipped),
3 visibility/process gaps. No content gap requiring a re-trigger; YT shorts and
the morning IG+FB post published, evening post unconfirmed.
