# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-26 (Tue)

_Generated 2026-05-26 23:09 AEST. All times AEST (Sydney, UTC+10).
Supersedes the morning version of this file (10:10 AEST), which ran before
the day's first slot had fired._

## 📊 GitHub Actions: 2 runs today (2 succeeded, 0 failed)

Pulled via the GitHub public REST API (no auth needed for this public repo —
`gh` CLI still missing in the sandbox but the unauthenticated `/runs` endpoint
worked, removing the visibility gap that's affected the last two audits).

| Run ID | Started (AEST) | Duration | Trigger | Conclusion | What slot |
|---|---|---|---|---|---|
| 26437298139 | 2026-05-26 16:56 | 12m04s | schedule | ✅ success | Morning post (cron 13:17, fired 3h39m late) |
| 26440018083 | 2026-05-26 18:00 | 5m37s  | schedule | ✅ success | YT Short slot 1 (cron 14:17, fired 3h43m late) |

Late dispatch is the documented GHA-load behaviour the workflow already warns
about (see comment block in `.github/workflows/autoposter.yml`). Both runs
completed all 12 steps green, including step 11 *Run autoposter* and step 12
*Commit updated state to autoposter-state branch*.

## Content Published Today — 2026-05-26 cron-day (from autoposter-state tip `f1f8fa1b`)

Pulled from `state.json` + `post_log.jsonl` + `post_outcomes.jsonl` on the
autoposter-state branch tip (commit `f1f8fa1b`, 2026-05-26 18:06 AEST).

- ✅ **Morning IG post (m8EAd)** — `kalimpong` · format `v3_tl_city_neighborhood` · Outstand `8hNJI` · published 17:06 AEST
- ✅ **Morning IG story (m8EAd_story)** — recorded in `posted_today.m8EAd_story: 2026-05-26` · published in the same run
- ✅ **Morning FB post (PdMu0)** — `mathura` · format `v3_tl_city_neighborhood` · Outstand `bmnmT` · published 17:08 AEST
- ✅ **YT Short slot 1 — YouTube (@naksh-iq, GAh5p)** — `doodhpathri` · format `yt_short.did_you_know` · Outstand `9cASQ` · Platform `ivCsXhtP4C0` · published 18:05 AEST
- ✅ **YT Short slot 1 — IG cross-post (m8EAd)** — `doodhpathri` · Outstand `Xw1ne` · Platform `18587741689012745` · published 18:05 AEST
- ⏭️ **FB cross-post for YT Short** — skipped by design (`[facebook/Nakshiq] Skipping (YT Shorts post to YouTube + Instagram only).` in log) ✓ expected
- ⏳ **YT Short slot 2 (cron 21:47 AEST / 11:47 UTC)** — **NOT YET FIRED** (1h22m past cron time, still inside the day). No GHA dispatch visible yet — see "Issues".
- ⏳ **Evening post (cron 00:17 AEST Wed / 14:17 UTC Tue)** — not yet due (fires in ~1h)
- ⏳ **Analytics sync (cron 04:17 AEST Wed / 18:17 UTC Tue)** — internal, not yet due
- ⏳ **Engagement pull (cron 07:17 AEST Wed / 21:17 UTC Tue)** — internal, not yet due

**Paused since 2026-05-16 (no scheduled cron, will NOT fire and should NOT be
re-triggered):** reel, tourist-map (Tue/Thu/Sat), canva-visual, infographic
(Mon/Wed/Fri), reel-map, pomelli-visual, flow-story, UGC avatar. These appear
in this task's SKILL.md schedule block but the workflow file no longer has the
corresponding cron lines — they're commented out.

## Issues Found

1. **⚠️ YT Short slot 2 is 1h22m past its 21:47 AEST cron time with no GHA
   dispatch.** Slot 2 of the YT-short pair was due at 21:47 AEST (11:47 UTC).
   It's now 23:09 AEST (13:09 UTC). The previous two slots today landed
   3h39m and 3h43m late, so a similar delay would still bring slot 2 in
   before midnight AEST — i.e. inside the day. Not a confirmed miss yet, but
   **leaning toward late-but-coming.** Watch for it in the next ~2h. If it
   still hasn't run by ~02:00 AEST (16:00 UTC), it's effectively missed.

2. **⚠️ Encoding warning in YT-short post_outcomes write.** The log at
   2026-05-26 08:05:55 UTC: `WARNING Could not write post_outcomes entry:
   'utf-8' codec can't encode characters in position 99-100: surrogates not
   allowed`. The publish itself succeeded (state was committed), but the
   outcomes-log entry didn't write — `post_outcomes.jsonl` on the tip carries
   no row for the YT short, only for the morning IG + FB posts. Likely a
   surrogate-pair leak from caption emoji into the JSON encoder. Low blast
   radius (engagement pull reads from `post_log.jsonl` not `outcomes`), but
   the durable analytics audit trail is missing today's YT short.

3. **ℹ️ Task SKILL.md schedule and re-trigger block are stale (recurring,
   now reinforced).** The SKILL.md still lists Reel slot 1/2/3, Tourist map,
   Canva visual, Infographic, plus `-force` re-triggers. As of today's
   `ab63b4d8` (12:23 AEST Phase C ship), **all `-force` variants are
   actively rejected by the workflow** (`case "$INPUT" in *-force|force)
   echo "::error::--force dispatch has been disabled (Phase C)..."; exit 1
   ;;`). Following the SKILL.md as written would now hard-fail. The plain
   mode names still exist but would fire paused content. The audit task
   needs a rewrite to reflect the post-2026-05-16 cadence and the
   2026-05-26 Phase C lockdown.

4. **ℹ️ State.json `last_sync` field lag.** The branch-tip `state.json` has
   `last_sync: 2026-05-26T07:06:06Z` even on the 08:06 UTC commit — the
   field is only stamped by the morning sync step and isn't refreshed for
   the YT-short slot. Cosmetic; `posted_today` and `post_log` are the
   authoritative truth.

## Git push health: ✅ clean

Both runs committed and pushed to the autoposter-state branch successfully:
- `96f7547b` — 2026-05-26 07:08:54 UTC (morning run)
- `f1f8fa1b` — 2026-05-26 08:06:04 UTC (YT short slot 1 run)

No `rejected` / `failed to push` markers; the new Phase C "track push success,
hard-fail on 3 attempts" wrapper has nothing to flag yet.

## Actions Taken

**None — report-only is the correct outcome here.**

- No slot is irrecoverably missed; YT Short slot 2 is delayed but still
  inside its possible-fire window. Manual re-trigger would risk a
  double-publish if the scheduled run lands shortly after.
- `gh` CLI is still not installed in this sandbox, so `workflow_dispatch`
  isn't possible from here regardless. The unauthenticated REST API only
  exposes read endpoints.
- Re-triggering paused content types (tourist-map, infographic, reel,
  canva-visual, pomelli-visual, flow-story, reel-map) would directly
  contravene the 2026-05-16 cadence cut and is not done.
- The `-force` variants the task suggests as fallback no longer exist —
  Phase C explicitly rejects them with an `::error::` exit.

## Recommended Follow-ups (for Ashish)

1. **Update this audit task's SKILL.md** to the post-2026-05-26 reality:
   active schedule = Morning + YT slot 1 + YT slot 2 + Evening + Analytics +
   Engagement pull; remove tourist-map / canva-visual / reel / infographic /
   reel-map / flow-story / pomelli-visual / UGC slots; remove all `-force`
   re-trigger examples; replace with the plain `yt-short`, `evening`,
   `analytics` modes for the only re-trigger cases that make sense today.

2. **Fix the YT-short post_outcomes encoding bug.** Surrogate-pair guard on
   `caption_preview` before JSON-encoding — likely a `.encode('utf-8',
   errors='surrogatepass').decode('utf-8', errors='replace')` pass, or
   `json.dumps(..., ensure_ascii=False)` with an explicit surrogate strip.
   The fix is small but missing rows will accumulate silently.

3. **Watch the YT-short slot 2 between 23:09 → ~02:00 AEST.** If it
   doesn't land by 02:00 AEST, it's a genuine miss and tomorrow's audit
   should re-trigger via `gh workflow run autoposter.yml -f mode=yt-short`
   (this mode is alive and respects dedup).

4. **Install `gh` CLI + provide a token to the audit sandbox.** Three
   audits in a row have either hit the visibility gap entirely or worked
   around it via the unauthenticated REST endpoint (which doesn't expose
   logs, only run metadata).

---

⚠️ **4 issues found** — 1 in-progress (YT slot 2 delayed, not yet missed),
1 low-severity bug (post_outcomes encoding), 1 stale process doc (SKILL.md),
1 cosmetic (state.json `last_sync` lag). 4 published items on the 2026-05-26
cron-day so far: morning IG (kalimpong) + IG story + morning FB (mathura) +
YT short (doodhpathri) cross-posted to IG. Evening post + analytics sync +
engagement pull still pending later in the cron-day.
