🔍 NakshIQ Autoposter Daily Audit — 2026-06-18 (generated ~23:10 AEST)

**Method note.** Ground truth read from `origin/autoposter-state` (local `main` copies of `state.json`/`autoposter.log` are stale). `gh` CLI is **not available in this sandbox**, so GitHub Actions run counts are *inferred from dated state commits* (one `chore: autoposter state …` commit ≈ one successful run). All times below are **AEST (UTC+10; no DST in June)**. Log timestamps are UTC and were converted.

This audit covers the **last complete posting cycle (June 17)** plus **June 18 progress to date** — most June-18 slots have not yet landed (GHA scheduling drift is currently +5 to +6.5 h after cron, so the evening/analytics/engagement slots are not yet due).

---

📊 **GitHub Actions (inferred from state commits)**

- **June 17:** 6 successful runs — morning feed, 3× YT-short, analytics sync, engagement pull. **One expected evening-feed run left no trace** (no banner, no commit, no post).
- **June 18 (so far):** 2 successful runs — morning feed (published nothing — see below) and YT-short slot 1.
- **Git push: 0 failures.** State commits are landing normally (latest 06-18 19:04 AEST). The only `rejected` log entries are old **Facebook content** rejections (Jun 2 / 8 / 10), not git pushes.

---

**Content Published — June 17 cycle**

- ✅ **Morning feed** — IG Reel (`pangong-lake`) + FB Reel (`nubra-valley`) · 18:41 AEST. IG Story correctly skipped (Reel day — by design).
- ✅ **YT-short 1** — YouTube (`har-ki-doon`) + IG cross-post · 19:16 AEST.
- ✅ **YT-short 2** — YouTube (`great-himalayan-np`) + IG cross-post · 00:15 AEST. The IG "queued but NOT confirmed" warning (`GnLJp`) is a **false alarm** — analytics reconciled it `published`.
- ✅ **YT-short 3** — IG cross-post (`umlingla`) · 00:46 AEST. YouTube correctly skipped (2/day cap).
- ✅ **Analytics sync** — 06:33 AEST (synced 8 new posts; 477 tracked).
- ✅ **Engagement pull** — ~08:57 AEST (state-only commit, no banner — normal).
- ❌ **Evening feed — MISSING.** No evening run fired on June 17 (June 16 had one at 04:43 AEST; June 15 had two). A full daily content slot was lost.

**Content Published — June 18 (so far)**

- 💥 **Morning feed — FAILED, nothing posted.** Format `v2_tourist_trap_split` pool fully exhausted — every candidate dest either had no current-month row or was in 14-day cooldown. Fallback to `score_card` still resolved to `sangla`, which is cooldown-blocked → `COOLDOWN VIOLATION` on **both** IG and FB legs → both skipped. No morning feed post to either platform · 18:24 AEST.
- ✅ **YT-short 1** — YouTube (`amboli`, `5Jq3dIWLXqk`) + IG cross-post (`17979339636034229`) · 19:01 AEST. Both confirmed in-log.
- ⏳ **Pending (not yet due / drift):** YT-short 2, evening feed, analytics sync, engagement pull.

---

**Issues Found**

1. 💥 **June 18 morning feed produced no post** — cooldown / pool-exhaustion hard-skip on both platforms. Same family as the known morning-feed no-fallback gap. Risk is rising: the `v2_tourist_trap_split` anti-trap pool has too few in-month, out-of-cooldown destinations left.
2. ❌ **June 17 evening feed never ran** — no banner, no commit, no post. Root cause isn't visible from state alone; GHA run history would show whether the evening cron failed to fire or failed at runner setup.

**Actions Taken**

- **No re-triggers performed.** Both gaps are **feed** posts, and the workflow exposes no feed/morning/evening dispatch mode — only `reel`, `tourist-map`, `canva-visual`, `infographic`, `yt-short`, of which all but `yt-short` are paused until 1k followers. The one healthy daily content slot (YT-short) needs nothing. (`gh` is also unavailable in this sandbox.) Re-triggering was neither possible nor appropriate; `-force` was not used.

**Recommended (founder / dev)**

- **Feed fallback fix:** when the chosen format's pool is exhausted, re-pick a fresh *eligible* destination (or alternate format) before skipping — don't hand a cooldown-blocked dest to the fallback format. Also refresh/widen the `v2_tourist_trap_split` in-month pool.
- **Check GHA history** for the June 17 evening cron to see if it failed vs never dispatched. If evening keeps no-showing, adjust its cron minute-mark.
- Standing open bug (not applicable today — Sunday only): weekly digest output still not committed to `autoposter-state`.

---

⚠️ **2 issues found** — 1 failed feed run (June 18 morning) and 1 missing feed run (June 17 evening). YT-shorts, analytics, engagement pull, and git push are all healthy.
