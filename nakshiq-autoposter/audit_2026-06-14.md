🔍 Nakshiq Autoposter Daily Audit — 2026-06-14

_Run at Sun 14 Jun 2026, 12:02 PM AEST (02:02 UTC). All times AEST (UTC+10)._

## Audit window

Run fired Sunday noon AEST. June 14's posting slots (Morning feed 1:17 PM AEST, YT shorts, Evening) are all still **in the future** as of this run — nothing for June 14 has come due yet. This audit therefore reviews the **most recently completed cycle: the June 13 posting day** (06-13 IST/AEST run-day, UTC 03:17→22:17), plus confirms June 14 has no overdue slots.

Data source note: `gh` CLI is **not available in this run environment** and no GitHub token is present, so GitHub Actions run history could not be queried directly. Findings below are reconstructed from the authoritative runner artifacts on the `origin/autoposter-state` branch (`autoposter.log` + `state.json`), which the runner commits after every run. State branch is current through commit `d55d2097` (2026-06-13 22:23 UTC).

## ⚠️ Schedule correction (important)

The schedule in the audit task file is **stale** — it predates the **2026-05-16 cadence cut**. The live workflow (`.github/workflows/autoposter.yml`) now runs only:

| Slot | Cron (UTC) | AEST | Cadence |
|------|-----------|------|---------|
| Morning feed (IG+FB) | `17 3 * * *` | 1:17 PM | daily |
| YT Short slot 1 (YT) | `17 4 * * *` | 2:17 PM | daily |
| YT Short slot 2 (YT) | `47 11 * * *` | 9:47 PM | daily |
| Evening feed (IG+FB) | `17 14 * * *` | 12:17 AM | daily |
| Analytics sync (internal) | `17 18 * * *` | 4:17 AM | daily |
| Engagement pull (internal) | `17 21 * * *` | 7:17 AM | daily |
| Weekly digest (internal) | `17 22 * * 0` | Sun 8:17 AM | Sundays |

**Tourist map, Canva visual, infographic, and all reel slots are PAUSED (since 2026-05-16)** — pivot from 6→2 IG/FB posts/day for the sub-1k-follower algorithm penalty. Their absence is **expected, not a gap.** Future audits should not flag them as missing.

## 📊 Runs — June 13 cycle: 5 content/sync runs observed (no failed GHA runs in artifacts)

## Content Published (June 13 cycle)

- ❌ **Morning feed (IG+FB)** — MISSED. Run fired (05:11 PM AEST) but selected format `confidence_intel` for **Lolab Valley, which has 0 confidence-card rows** → "No content generated — skipping" on both Instagram and Facebook. No feed post went out. State `m8EAd`/`PdMu0` still dated 2026-06-12.
- ✅ **YT Short slot 1** — published to YouTube (`hrKHe4xRtnc`) + Instagram (`18105274043057801`), format nakshiq_score.
- ⚠️ **YT Short slot 2** — YouTube ✅ (`tj9R85R7z3A`); Instagram **queued but not confirmed** (post_id `Trbi1`, 40s poll timeout).
- ✅ **Evening slot** — variety pick delegated to **reel-map** pipeline (Himachal roads stability scores). YouTube ✅ (`5_4qwVEJ0Og`), Facebook ✅ (`2222560581874489`); Instagram **queued but not confirmed** (post_id `mYrFd`).
- ✅ **Analytics sync** — synced 8 new posts (cumulative 440 tracked; 380 published / 51 failed lifetime).
- ⏸️ **Tourist map / Canva / infographic / reels** — paused by design (see correction above).

## June 14 (today) so far

No overdue slots. First slot (Morning feed, 1:17 PM AEST) was ~1h out at run time. No June 14 entries in the log yet — expected.

## Issues Found

1. **Morning feed missed (June 13)** — root cause is content selection, not infra: the format picker assigned `confidence_intel` to a destination (Lolab Valley) with zero confidence-card rows, then skipped the whole post instead of falling back to an eligible destination. This is the day's primary IG+FB post and it did not publish. **Recurrence risk** until the picker gates `confidence_intel` on `card_rows > 0` (or falls back).
2. **2× Instagram "queued but not confirmed"** (YT short `Trbi1`, reel `mYrFd`) — Outstand accepted both; platform confirmation polling exhausted in 40s. Per the known-pattern note in memory, IG-unconfirmed warnings are **usually false alarms** (the post lands; confirmation just lagged the 40s window). Low priority — spot-check the two IG posts actually appeared.
3. **Account allowlist hotfix is working as intended** — log shows repeated "Ignoring non-NakshIQ account in shared Outstand workspace: youtube/@chanakyasutra-t8o (xS5s8)". The June 12 wrong-channel leak fix (`bfb00485`) is correctly blocking the shared-workspace foreign account. No action.
4. **No git push / "rejected" / "failed to push" errors** found in the runner artifacts. State branch committing normally through 06-13 22:23 UTC.

## Actions Taken

- **None auto-executed.** Re-trigger could not be performed: `gh` CLI is unavailable in this run environment (no binary, no token, no sudo to install). The prescribed `gh workflow run` mechanism is not reachable here.
- **Re-trigger judgment:** the only genuine miss (June 13 morning feed) is now ~19h stale and its cause is a content-data gap, not a transient failure — a blind re-fire risks (a) re-hitting the same skip and (b) posting off the 2-posts/day cadence right after the 06-12 wrong-channel incident. Recommended as a founder decision rather than an autonomous write.

## Recommended (founder, from your Mac)

1. **Fix root cause** (preferred over re-trigger): gate `confidence_intel` (and similar data-dependent formats) on `confidence_card rows > 0`, or add a destination fallback so a 0-data pick doesn't kill the whole feed post.
2. If you want today's missed slot backfilled manually: `cd ~/Desktop/India\ Travel\ Planner && gh workflow run autoposter.yml` (no `-f mode`, no `-force`) — but note it'll publish off-cadence.
3. Quick eyeball: confirm IG posts behind `Trbi1` and `mYrFd` actually rendered on the @nakshiq Instagram.

## ⚠️ 1 real issue (missed morning feed) + 2 known-pattern warnings; infra healthy, allowlist fix confirmed working
