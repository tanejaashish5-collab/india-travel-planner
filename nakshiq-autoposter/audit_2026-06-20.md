# 🔍 Nakshiq Autoposter Daily Audit — 2026-06-20 (Saturday)

_Audit run: 2026-06-20 23:00 AEST (13:00 UTC). All times AEST (Sydney = UTC+10, no DST in June)._

## 📊 GitHub Actions

**gh CLI / Actions run-history was not reachable from this run's environment**, so run conclusions are reconstructed from the **authoritative `autoposter-state` branch** (CI commits the live `state.json`, `data/*.jsonl`, and `autoposter.log` there after every run) — a more reliable source than the local Desktop checkout, which is stale (local log last written Jun 8, local `state.json` last synced Apr 30).

- Method note: `gh` is not installed/authenticated in this sandbox; the GitHub public-API URL is provenance-blocked for `web_fetch`. Verification was done via `git ls-remote` + a blob-less partial fetch of `origin/autoposter-state`.
- **State commits today: 2** — `07:14 UTC` (17:14 AEST) and `08:04 UTC` (18:04 AEST). Both pushed cleanly to `origin/autoposter-state` → **no git-push failures**. Current remote HEAD `9a3bd91`.
- **Runs evidenced today: ≥2** (Morning post, YT Short slot 1). Both ran to completion without crashing. No `Traceback`/crash/`rejected`/`failed to push` in the committed log.

## Schedule reconciliation (important)

The schedule in the audit task file is **stale**. The live `.github/workflows/autoposter.yml` (post the 2026-05-16 cadence cut: 6→2 IG/FB posts/day) runs a much leaner set. Slots the task file expects but that are **PAUSED/REMOVED** — their absence today is correct, not a failure:

- **Reel slots 1/2/3** — Reel slot 1 PAUSED 2026-05-16; Reel slot 2 REMOVED 2026-05-10.
- **Tourist map** (task expects it on Sat) — PAUSED 2026-05-16 (part of the "visual slot").
- **Canva visual** — PAUSED 2026-05-16 (visual slot).
- **Infographic** (Mon/Wed/Fri) — visual-slot family, dark; also not a Saturday slot.
- **Reel-map / Flow story** — PAUSED 2026-05-16.
- The task file also lists a **YT Short slot 3** that no longer exists (only 2 YT slots are live) and **omits the Evening IG/FB post** that IS live.

**Live active schedule (UTC → AEST):** Morning post `03:17→13:17` · YT slot 1 `04:17→14:17` · YT slot 2 `11:47→21:47` · Evening post `14:17→00:17⁺¹` · Analytics sync `18:17→04:17⁺¹` · Engagement pull `21:17→07:17⁺¹` · Weekly digest Sun only. (GHA cron drift is routine — today slot 1 fired ~4h after its cron.)

## Content Published Today

| Slot | Scheduled (AEST) | Status | Detail |
|---|---|---|---|
| Morning feed post (IG+FB) | 13:17 | 💥 **Ran but posted nothing** | `COOLDOWN VIOLATION` on both IG & FB — destination pool exhausted |
| YT Short slot 1 (YouTube+IG) | 14:17 | ✅ **Published** | Sambalpur · Hindi · `nakshiq_score` · YT `YP1l0IjCFbc` + IG `18047509223786372` (fired 18:03 AEST) |
| YT Short slot 2 (YouTube+IG) | 21:47 | ⏳ **Pending / likely delayed** | No state commit or log entry yet as of 23:00 AEST; slot 1 ran ~4h late, so drift is expected |
| Evening feed post (IG+FB) | 00:17⁺¹ | ⤵️ Not due yet | Window opens after audit time |
| Analytics sync (internal) | 04:17⁺¹ | ⤵️ Not due yet | — |
| Engagement pull (internal) | 07:17⁺¹ | ⤵️ Not due yet | — |

## Issues Found

**⚠️ 1 real issue — Morning feed post published 0 posts (content-pool exhaustion, not infra).**

The morning run (`07:13 UTC`/`17:13 AEST`) executed normally, selected format `v2_local_knows`, then failed the final gate on both platforms:

```
ERROR [instagram/nakshiq] COOLDOWN VIOLATION: sangla is in the 14-day window
      but was selected for v2_local_knows — skipping (pool likely exhausted...)
ERROR [facebook/Nakshiq]  COOLDOWN VIOLATION: sangla ... skipping.
```

Root cause: the eligible destination pool is empty. Only **20 destinations** synced with a current-month (June) row; the rest log `"no current-month row — SKIPPING"` (nainital, ooty, gangtok, hampi, munnar, manali, shimla, mussoorie…), and nearly every dest that *does* have a June row is inside the **14-day cooldown** (pangong-lake, nubra-valley, leh, kaza, gulmarg, sonamarg, sangla…). The picker locked "Sangla Valley" as best dest, then the cooldown gate correctly rejected it → 0 posts. Same gap occurred **2026-06-18** (also YT-only that day), so this is recurring, not a one-off.

- This is a **data/content problem, not a CI failure** — re-running the workflow hits the identical exhausted pool and reproduces the skip.
- **Fix (founder/data-side):** add June current-month score rows for more destinations (or widen the feed-eligible pool / relax the cooldown when the pool drops below a threshold). The pipeline itself is healthy — yesterday's morning (06-19 08:47–08:48 UTC) and evening (06-19 16:46–16:47 UTC) feed posts published fine.

No platform/Outstand errors, no R2 upload errors, no git-push failures.

## Actions Taken

**No re-triggers performed**, by design:

1. **Morning feed post** — *not* re-triggered. It's a content-pool exhaustion skip, not a transient infra failure; a re-trigger would reproduce the same `COOLDOWN VIOLATION`. The actionable fix is data-side (above). _(Also: `gh workflow run` is not executable from this sandbox — gh CLI absent/unauthenticated.)_
2. **YT Short slot 2** — *not* re-triggered. It is most likely just delayed by GHA cron drift (slot 1 fired ~4h late today). Re-triggering a slot that may still fire risks a **duplicate post**, which the task explicitly prohibits. Recheck on the next audit; if no `*_yt_short` entry for 2026-06-21 appears by tomorrow's slot-1 window, investigate then.

⚠️ **Environment limitation to fix for this scheduled task:** GitHub Actions run history and the re-trigger step both depend on an authenticated `gh` CLI, which isn't available where this task ran. Until that's wired up (install + `GH_TOKEN`), audits rely on the `autoposter-state` git branch and cannot auto-re-trigger.

## Verdict

⚠️ **1 issue found** — Morning IG/FB feed post published nothing (destination pool exhausted: too few June rows + 14-day cooldowns). Recurring (also 06-18); needs a data-side fix, not a re-trigger. YT Short slot 1 ✅ live (YouTube + IG). YT slot 2 pending/likely-delayed. All later slots not yet due. Git pushes healthy.
