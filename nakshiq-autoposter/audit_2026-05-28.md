# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-28 (Thursday)

Audit run at **23:01 AEST** (13:01 UTC) on Thu 2026-05-28.

## ⚠️ Day-zero dashboard

| Metric | Value |
|---|---|
| Posts logged today (post_log) | **0** |
| State commits on `autoposter-state` today | 2 (07:09 UTC, 08:16 UTC) |
| Workflow runs evidenced | 2 (both failed to publish) |
| Posts logged yesterday (2026-05-27) | 8 |
| 7-day rolling average | ~9.6 posts/day |

**This is the first zero-publish day in the last 7 days.** Both runs that did execute (morning + YT Short #1) failed at content generation — not at scheduling — and produced no posts on IG / FB / YT. The 08:16 UTC run is the most recent state commit; the branch has been silent for ~4.75 hours since.

---

## 📊 GitHub Actions

`gh` CLI is not available from the autonomous-task sandbox, so the audit infers run history from `autoposter-state` branch commits and the embedded `autoposter.log` rather than `gh run list`. Two runs confirmed today via state commits; both wrote log lines but didn't update `post_log` (no successful publish).

---

## 🗓 Scheduled vs actual (per active workflow file)

The task-file schedule (tourist-map, canva-visual, infographic, reel slots) is **out of date** — the **2026-05-16 CADENCE CUT** paused all of those crons. Per `.github/workflows/autoposter.yml`, the live Thursday schedule is:

| Slot | Cron (UTC) | AEST | Status today |
|---|---|---|---|
| Morning post (IG+FB) | 03:17 | 13:17 | 💥 **FAILED** — ran 07:08 UTC (~4h late), 0 posts published |
| YT Short slot 1 (YT only) | 04:17 | 14:17 | 💥 **FAILED** — ran 08:16 UTC (~4h late), 0 posts published |
| YT Short slot 2 (YT only) | 11:47 | 21:47 | ❌ **MISSING** — 75 min past scheduled, no state commit yet |
| Evening post (IG+FB) | 14:17 | 00:17 next day | ⏰ Not yet due |
| Analytics sync (internal) | 18:17 | 04:17 next day | ⏰ Not yet due |
| Engagement pull (internal) | 21:17 | 07:17 next day | ⏰ Not yet due |
| Weekly digest | 22:17 Sun | 08:17 Mon | n/a (Thursday) |

Paused since 2026-05-16 (no longer in cron): **reel**, **tourist-map**, **canva-visual**, **infographic**, **pomelli-visual**, **reel-map**, **flow-story**. The task brief still lists these — the brief needs updating.

---

## 💥 Failure detail

### 1) Morning post — `v2_route_animated_map` skipped both platforms

```
2026-05-28 07:08:38  INFO   Morning format (themed-week-4→discovery): v2_route_animated_map [70% vs target 20%]
2026-05-28 07:08:38  INFO   Formats — IG: v2_route_animated_map  ·  FB: v2_route_animated_map  ·  Story: score_card
2026-05-28 07:08:53  INFO   [csv_formats] endpoint-anchored: v2_route_animated_map → routes (23 candidates)
2026-05-28 07:08:53  INFO   v2_route_animated_map: no endpoint candidate passed eligibility — SKIPPING
2026-05-28 07:08:53  WARNING [instagram/nakshiq] No content generated — skipping.
2026-05-28 07:09:06  INFO   v2_route_animated_map: no endpoint candidate passed eligibility — SKIPPING
2026-05-28 07:09:06  WARNING [facebook/Nakshiq] No content generated — skipping.
```

23 route candidates were considered, none passed eligibility. The selector picked this format because it was "themed-week-4 → discovery" and **way over its 20% target at 70%** — yet eligibility wiped every candidate. Either:

- the eligibility gate for `v2_route_animated_map` is too tight (dedup + 14d-rolling + fingerprint stacking?), or
- the route catalog is exhausted relative to that gate.

This is a **first-time** occurrence in the rotating log. No earlier "no endpoint candidate" failures observed.

### 2) YT Short slot 1 — generator returned `None`

```
2026-05-28 08:16:10  INFO   Nakshiq Autoposter · YT SHORT · 2026-05-28
2026-05-28 08:16:10  INFO   [csv_formats] loaded 38 format specs
2026-05-28 08:16:16  ERROR  YT Short generation returned None.
```

Generator exited with `None`. Also a **first-time** occurrence — no earlier "YT Short generation returned None" lines in the autoposter.log. Possibly related to recent commit `80601cf1 fix(autoposter): csv yt_short brand_stamp opt-in + quarantine Veo-watermarked silent_pov assets` (the silent_pov asset quarantine could be removing every viable candidate for today's chosen format).

### 3) YT Short slot 2 — not run yet

Scheduled 11:47 UTC; current time 13:01 UTC. No state commit and the last 7 days show YT Short #2 typically commits within 30-90 min of its cron, so this is borderline (cron may still fire) but the morning + slot 1 pattern today (`~4h late`) suggests today's GHA scheduling drift could push it to ~15:30 UTC. Re-check after 15:30 UTC.

---

## 🔍 Git push / state-branch health

`autoposter-state` is being written normally (Bot wrote 2 commits today, head is `bae4759f`). No "rejected" / "non-fast-forward" / "failed to push" patterns in the log. The branch is healthy — the suppression is at the **content generation layer**, not the git/push layer.

---

## 🛠 Actions taken / not taken

| Action | Decision |
|---|---|
| Re-trigger morning post via `gh workflow run` | ❌ NOT taken — task brief's mode list excludes `morning`/`evening`; only `yt-short` etc. allowed |
| Re-trigger YT Short slot 1 via `gh workflow run -f mode=yt-short` | ⚠️ NOT taken (autonomous) — failure was a code-level `None` return, not transient; retry is unlikely to fix without a code investigation. Also, `gh` CLI is not available in this autonomous-task sandbox. **Recommend the operator run manually:** `cd ~/Desktop/India\ Travel\ Planner && gh workflow run autoposter.yml -f mode=yt-short` after addressing the regression below. |
| Re-trigger YT Short slot 2 | ❌ NOT taken — only 75 min past scheduled at audit time and historically today's runs are landing ~4h late; let the cron fire. Re-check at 15:30 UTC. |
| Address `v2_route_animated_map` eligibility wipe | ⚠️ Operator action — investigate why all 23 routes failed eligibility (dedup window? asset quarantine? recent route-catalog edits?). Possibly add a fall-through to a backup format when route-anchored content can't materialize. |

---

## 🚨 Recommended next steps (operator)

1. **Investigate today's regression.** Both failures are first-time. The 2026-05-26 commit `80601cf1 fix(autoposter): csv yt_short brand_stamp opt-in + quarantine Veo-watermarked silent_pov assets` is the most recent autoposter touch — high suspicion for the YT Short None return. Check whether the silent_pov quarantine list now excludes all viable candidates for today's chosen format.
2. **Add a fallback when content gen returns 0 candidates.** A "themed-week" pick that wipes all candidates should fall through to a discovery/safe format rather than skip both IG and FB silently.
3. **Manually dispatch** today's missed slots (if you want to recover today's cadence):
   ```bash
   cd ~/Desktop/India\ Travel\ Planner
   gh workflow run autoposter.yml -f mode=yt-short      # for slot 1 retry
   # Morning post has no workflow_dispatch input — would need to run locally:
   # python nakshiq-autoposter/autoposter.py            # (no --force)
   ```
4. **Update this audit task file** (`SKILL.md`) to reflect the post-2026-05-16 CADENCE CUT schedule. The current brief lists 6 paused content types that no longer correspond to any cron.

---

## Summary

⚠️ **2 issues found** (both content-generation regressions, first-time symptoms) + **1 slot pending observation** (YT Short #2). State branch + GHA scheduling appear healthy; failures are upstream in autoposter format selection. No auto re-triggers performed — operator action recommended above.
