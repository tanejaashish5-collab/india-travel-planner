# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-22 (AEST Friday)

Audit run shortly after **06:35 AEST May 22** (≈20:35 UTC May 21). Covers the **AEST May 21 (Thursday) posting day** plus the overnight roll into May 22 — i.e. it picks up exactly where `audit_2026-05-21.md` (run 06:35 AEST May 21) stopped, at commit `c9ade096` (20:34 UTC May 20).

**Sandbox notes:**
- The `gh` CLI is **not installed** in the audit sandbox (same as the last 4 audits). GitHub Actions run conclusions and `event` types (schedule vs `workflow_dispatch`) cannot be queried directly. `gh workflow run` re-triggers are also impossible here.
- Runs are reconstructed from the `origin/autoposter-state` branch — each completed run pushes one `chore: autoposter state YYYY-MM-DD HH:MM UTC` commit. The audit reads the per-commit diffs of `autoposter.log`, `data/post_log.jsonl`, `state.json` and `analytics.json`. A fresh `git fetch` was performed before this report.
- Date basis: the host reports today as **Friday 2026-05-22**; the workspace VM clock is unreliable (jumped ~10h between calls) and was disregarded. Last `origin/main` commit is `2026-05-21 19:46 +1000`, consistent with May 22 AEST being "today".

## 📊 GitHub Actions activity

Audit window: **20:34 UTC May 20 → 20:14 UTC May 21** (06:34 AEST May 21 → 06:14 AEST May 22). **6 state-push commits = 6 completed runs.** No outright crashes (no Python tracebacks; every run that wrote a log ended with `Run complete`).

| # | UTC commit | AEST | Run | Result |
|---|---|---|---|---|
| 1 | `aceef68c` 07:08 | 17:08 May 21 | Post (MORNING) | ⚠️ chopta · `v2_pov_slow_morning` · IG `k1Lah` ✅ — **FB skipped** (`no eligible dest at render time`) |
| 2 | `02761e20` 08:09 | 18:09 May 21 | Post (MORNING) | ✅ chopta · `v2_pov_slow_morning` · FB `WiIvz` — completed the FB half; IG correctly dedup-skipped |
| 3 | `f96c667a` 13:52 | 23:52 May 21 | YT Short | ✅ tungnath · `v2_yt_silent_pov` · **YouTube `ZI89u` + IG `8k3cI`** — a genuine YT Short |
| 4 | `3f590956` 14:29 | 00:29 May 22 | Post (EVENING) | ✅ sissu · `festival_alert` · IG `gJStx` + FB `O2jqv` |
| 5 | `a689ed2d` 16:54 | 02:54 May 22 | Analytics sync | ⚠️ Ran (synced 50 new / 280 tracked) — analytics.json write later clobbered; also reverted `state.json` |
| 6 | `ea87585c` 20:14 | 06:14 May 22 | Stale-checkout run | 💥 No posts — clobbered `analytics.json` (−801 lines) and **emptied `autoposter.log`** |

**6 runs · 4 content-post runs · 1 analytics sync · 1 clobber run.** 6 platform posts landed across 3 content pieces (chopta reel, tungnath short, sissu reel). 0 crashes.

## 📅 Schedule reference

**The SKILL.md schedule narrative is stale** (morning feed 6:30–9 PM, reel slots 1/2/3, tourist map Tue/Thu/Sat, canva, infographic Mon/Wed/Fri, 3 YT Short slots, 2 AM / 3 AM / 6 AM crons). Those slots were **paused on 2026-05-16** as part of the cadence cut (user diagnosis: 215 IG posts → 2 organic followers; sub-1K accounts get algorithm-penalised for posting >2–3×/day). **This is the 3rd consecutive audit to flag the stale task file** — see Issue §5.

Active crons in `.github/workflows/autoposter.yml` (verified against `origin/main` this run):

| Slot | Cron (UTC) | AEST | May 21 status |
|---|---|---|---|
| Morning post (IG+FB) | `17 3 * * *` | 13:17 | ✅ Posted (chopta — across runs 1+2) |
| YT Short slot 1 | `17 4 * * *` | 14:17 | ✅ Covered — one YT Short shipped today |
| YT Short slot 2 | `47 11 * * *` | 21:47 | ✅ Posted (tungnath short) |
| Evening post (IG+FB) | `17 14 * * *` | 00:17 May 22 | ✅ Posted (sissu) |
| Analytics sync | `17 18 * * *` | 04:17 May 22 | ⚠️ Ran 16:54 (≈84 min early — recurring) — write clobbered |
| Engagement pull | `17 21 * * *` | 07:17 May 22 | ⏳ Read-only; cron not yet due at audit time |
| Weekly digest | `17 22 * * 0` | Mon 08:17 | N/A (Sunday only) |
| Reel 1/2/3, Visual, Reel-map, Flow-story, UGC, Infographic, Tourist map, Canva | — | — | ⏸️ Paused 2026-05-16 — **not a miss** |

Run start times drift heavily from cron times (morning cron 03:17 UTC → ran 07:06; +3h49m) — the same scheduler drift / possible manual-dispatch pattern noted in the last 3 audits. Without `gh` the `event` type cannot be confirmed.

## ✅ Cross-reference — scheduled vs actual

- **Morning feed post** → ✅ **Published.** chopta / `v2_pov_slow_morning` reel on IG (`k1Lah`) + FB (`WiIvz`). Delivered across two runs: run 1 published IG but FB skipped on the recurring `v2_pov_slow_morning` render bug; run 2 (one hour later) re-fired, IG correctly dedup-skipped (`Already posted today`), and the FB half went through. Net result is a complete IG+FB pair — **no missing post**, but see Issue §1.
- **YT Short** → ✅ **Published.** tungnath / `v2_yt_silent_pov` to YouTube (`ZI89u`) + Instagram (`8k3cI`); FB skipped by design (YT Shorts post to YT+IG only). **This is a genuine YouTube Short** — a clear improvement over `audit_2026-05-21.md` Issue §2, which flagged zero YouTube Shorts on May 20. One YT-SHORT-mode run was visible; one short/day is the right volume under the cadence cut, so this is treated as ✅ covered, not a partial miss.
- **Evening feed post** → ✅ **Published.** sissu / `festival_alert` reel on IG (`gJStx`) + FB (`O2jqv`). Minor: the Story carousel locked with `no valid images found`, but the primary IG/FB posts succeeded.
- **Analytics sync** → ⚠️ **Ran but did not persist.** Run 5 synced 50 new posts (280 tracked total; 225 published / 46 failed cumulative; platform split IG 132 / FB 80 / YT 59) and appended 802 lines to `analytics.json`. Run 6, ≈3h20m later, **reverted those exact lines.** Branch-HEAD `analytics.json` is still 3,611 lines — unchanged from where the previous audit left it. See Issue §2.
- **Engagement pull** → ⏳ Not yet due at audit time (cron 21:17 UTC; latest commit 20:14 UTC). Read-only — expected absence, not a miss.

**Verdict: no content slot is ❌ Missing or 💥 Failed.** All three active content modes (morning post, YT short, evening post) published cleanly. The 💥 marker on run 6 is a data-integrity clobber, not a missed content slot. The real concerns are a recurring render bug and two recurring branch-clobber regressions — not availability.

## 🔁 Git push check

All 6 runs successfully pushed their state commits to `origin/autoposter-state` — **no `rejected` or `failed to push` errors**; every expected commit is present. The problem is the *inverse* of a push failure: runs 5 and 6 started from **stale checkouts** of the autoposter-state branch and their commit step overwrote newer data (analytics.json, state.json, autoposter.log) instead of merging it. Pushes are landing fine; the autoposter-state commit step is not rebasing/merging these files. See Issues §2 and §3.

## 🚨 Issues Found

**1. `v2_pov_slow_morning` Facebook render skip — recurring, still unfixed.**
Run 1 published the chopta reel to Instagram, then on Facebook logged `v2_pov_slow_morning: no eligible dest at render time — SKIPPING` → `[facebook/Nakshiq] No content generated — skipping`. This is the **exact bug** flagged as Issue §5 in `audit_2026-05-21.md` (the May 20 `7aaf8ca2` run). It self-healed today only because run 2 happened to re-fire an hour later and complete the FB half — that is luck/retry, not a fix, and it cost an extra workflow run. The FB render-eligibility check for this Phase-2 format needs a real fix before it ships on cadence.

**2. `analytics.json` clobbered again — analytics persistence broken 2 days running.**
Run 5 (16:54 analytics sync) appended 802 lines. Run 6 (20:14) removed 801 of them, pushing a stale `analytics.json` checked out before the sync landed. This is identical to `audit_2026-05-21.md` Issue §3. Consequence: branch-HEAD `analytics.json` has been frozen at 3,611 lines for two consecutive days, so every analytics sync re-derives the **same** 280 / 225 / 46 snapshot and never advances. The autoposter-state commit step merges `post_log.jsonl` but still does **not** merge `analytics.json`.

**3. `state.json` rollback recurrence — now the live HEAD state again.**
Run 5's commit also reverted `state.json` to the stale 30 April baseline: `posted_today: {}`, `last_sync: 2026-04-30`, `posted_destinations: []`, and it dropped the evening run's `festival_alert` entries from `posted_formats`. Branch-HEAD `state.json` is once more the April-30 file. This is the same regression as `audit_2026-05-21.md` Issue §4 and `audit_2026-05-20.md` Issue §1 — **third audit running, still unfixed.** **Impact is bounded:** `data/post_log.jsonl` — the authoritative append-only dedup log — is fully intact (67 lines; all 6 May 21 entries verified present), and runtime dedup reads the post-log fingerprint snapshot (`8 dests in 7d · 21 (dest,fmt) pairs in 30d`), not `state.json.posted_today`. So dedup is protected, but `state.json` is effectively dead/stale on the branch.

**4. `autoposter.log` on the branch is empty.**
Run 6's commit also reverted `autoposter.log` to a 0-byte file. The branch log only ever holds the single most-recent run anyway (each run truncates and rewrites it), so this is low-impact for forensics — but it means a future audit that lands between a clobber and the next run would see no log at all. Reconstruction from per-commit diffs (as done here) remains the reliable method.

**5. SKILL.md audit task file is stale — flagged for the 3rd straight audit.**
It still describes the pre-May-16 schedule (morning feed 6:30 PM, reel slots, tourist map, canva, 3 YT Short slots, 2/3/6 AM crons). None match the live workflow YAML. Step 7's re-trigger modes — `tourist-map`, `canva-visual`, `reel`, `infographic` — all target **paused** slots; blindly re-triggering them would reintroduce the exact over-posting the cadence cut fixed. The file should be rewritten to mirror the 6 active crons + an explicit paused list.

## 🛠 Actions Taken

**No re-triggers performed.** Reasons:
1. **Nothing is ❌ Missing** — every active content slot (morning post, YT short, evening post) published a complete post. May 21 shipped 3 content pieces / 6 platform posts, **within** the 2–3/day cadence cap (a clear improvement on May 20's 6-piece over-post).
2. **`gh` CLI is unavailable** in this sandbox — `gh workflow run` cannot be executed here regardless.
3. The analytics sync (Issue §2) did *run*; re-triggering it would only produce another sync at risk of the same clobber, and would not fix the root cause (the missing merge step).
4. SKILL.md step 7's modes (`reel`, `canva-visual`, `infographic`, `tourist-map`) are all **paused** content types — re-triggering them would violate the 2026-05-16 cadence cut.

## ⚠️ Status: 5 issues found, 0 re-triggers

Posting pipeline is **healthy and publishing** — 6 posts landed across IG, FB and YouTube, the morning/evening feed posts and a real YouTube Short all completed, cadence is back within the 2–3/day cap, and no run crashed. The concerns are all **data-hygiene and a render bug**, not availability or missed posts.

**Recommended follow-ups (no autonomous action taken — solo-founder review):**
- **Fix the `analytics.json` + `state.json` clobbers (Issues §2 + §3) — now 3 audits old.** Extend the autoposter-state commit step to rebase/merge `analytics.json` and `state.json` the same way it already merges `post_log.jsonl`, or have the analytics-sync and engagement-pull runs re-checkout the branch tip immediately before committing. The 16:54 sync overlapping the 20:14 run is the direct trigger.
- **Fix `v2_pov_slow_morning` FB render eligibility (Issue §1)** — recurring on every run that picks this format; it should not depend on a lucky retry.
- **Rewrite the SKILL.md audit task file (Issue §5)** to the 6 active crons + explicit paused list — pending for 3 audits running.
- Consider whether `state.json` can be formally retired in favour of `data/post_log.jsonl` now that dedup no longer reads it — that would make the recurring `state.json` rollback a non-event.
