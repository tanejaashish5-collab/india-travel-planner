# 🔍 Nakshiq Autoposter Daily Audit — 2026-06-04

**Generated:** 2026-06-05 09:51 AEST (Fri) · covers the **2026-06-04 AEST posting cycle** (03:17→21:17 UTC Jun 4 crons), which has now fully elapsed.
**Verdict:** 🚨 **1 critical issue — autoposter state pipeline frozen for 24h+. No posting activity recorded for the entire June-4 cycle.**

---

## ⚠️ Read this first — audit ran with reduced access

This audit was designed to run in Claude Code on your Mac with an authenticated `gh` CLI. It actually ran in the Cowork Linux sandbox, which has **no `gh` CLI and no GitHub token**. Consequences:

- **Step 3 (GitHub Actions run history):** could not be queried directly.
- **Step 7 (auto re-trigger missed slots):** could not be performed (no `gh workflow run`).

**Workaround used:** I reconstructed ground truth from the **`autoposter-state` branch** via `git fetch`. This is where GitHub Actions commits the real `autoposter.log` + `state.json` after every run (they're gitignored on `main`). For "what actually posted," this is *more* authoritative than run conclusions — a green run that didn't post still wouldn't update state. The local working copy's `autoposter.log` is a stale gitignored artifact (last entry **2026-05-29**) and was ignored; the local `state.json` is also stale (frozen at 2026-04-30). All findings below are from `origin/autoposter-state`.

---

## 📊 GitHub Actions

**Not directly queryable (no `gh`/token).** Inferred from `autoposter-state` commit history:

- **Last state commit:** `chore: autoposter state 2026-06-03 23:18 UTC` → landed **2026-06-04 09:18 AEST**.
- **Commits since:** **zero**, across ~24h and 6 due daily crons.
- `last_sync` = 2026-06-03 18:37 UTC (04:37 AEST Jun 4); `posted_today` entries all dated **2026-06-03**; `post_log` stuck at **174** entries, last dated 2026-06-03.

---

## Content scheduled today (LIVE workflow schedule)

> **Note:** The schedule in the task file is outdated. The live `autoposter.yml` **paused almost every content slot on 2026-05-16** (the "wait for 1k followers" cadence cut). Auditing against the task-file schedule would raise false alarms for reels/maps/canva/infographics that are *deliberately off*. The table below reflects what the workflow **actually** fires today.

| Slot (live cron) | Due (AEST Jun 4) | Cron (UTC) | Status |
|---|---|---|---|
| Morning post (IG+FB) | 13:17 | `17 3 * * *` | ❌ **Missing** — no log entry, no commit |
| YT Short slot 1 (YT) | 14:17 | `17 4 * * *` | ❌ **Missing** |
| YT Short slot 2 (YT) | 21:47 | `47 11 * * *` | ❌ **Missing** |
| Evening post (IG+FB) | 00:17 (Jun 5) | `17 14 * * *` | ❌ **Missing** |
| Analytics sync (internal) | 04:17 (Jun 5) | `17 18 * * *` | ❌ **Missing** |
| Engagement pull (internal) | 07:17 (Jun 5) | `17 21 * * *` | ❌ **Missing** |
| Weekly digest | Mondays only | `17 22 * * 0` | ⏭️ N/A (Thursday) |

**Paused / not expected to post** (commented out since 2026-05-16, correctly silent — *not* failures): Reel slots, Tourist map, Canva visual, Infographic, Reel-map, Flow story, Pomelli visual, UGC avatar.

---

## Issues found

**🚨 The autoposter has committed no state since 09:18 AEST Jun 4 — all 6 of June 4's daily runs are unaccounted for.**

Evidence (`origin/autoposter-state`):
- `autoposter.log`: **0 lines** dated 2026-06-04 or 2026-06-05 (UTC). Last real entry: 2026-06-03 21:35 UTC (a clean end-of-run analytics summary — the Jun-3 cycle finished fine).
- `state.json`: `last_sync`, `posted_today`, and `post_log` all frozen at 2026-06-03. A fresh day's morning run would reset `posted_today` and append to `post_log`; neither happened.

**Two scenarios — both need you to disambiguate (I can't from here):**

- **(A) Runs aren't executing** (GHA dropped the scheduled runs, the workflow was disabled, or runs crash during checkout/setup before logging). → **Real content gap; nothing posted Jun 4.**
- **(B) Runs execute and posts go out, but the "Commit updated state to autoposter-state" step fails** (e.g., a non-fast-forward / push race on the side branch). The log + state are written in the CI runner but never persisted, so from here it looks identical to (A). → **No content gap, but dedup state is now stale → risk of double-posting on recovery.**

**Candidate root-cause signals in the last healthy logs (not conclusive):**
- `2026-06-03 14:20 UTC ERROR Could not fetch accounts: 500 ... api.outstand.so/v1/social-accounts` — Outstand API 500. (It recovered same day; later Jun-3 runs succeeded.)
- `2026-06-02 08:14 UTC WARNING Facebook 500 "Please reduce the amount of data you're asking for"` — recurring FB publish flakiness.
- `2026-05-28 ERROR YT Short generation returned None` (×3) — intermittent YT-short generation failures.

None of these alone explains a *total* 24h silence with zero log output, which points more toward **(A) runs not starting / failing pre-log**, or **(B) the commit step failing**.

**Git push failures:** Could not scan GHA run output (no access). Given the side-branch commit design (`git fetch … --depth=1` + worktree + merge + push to `autoposter-state`), a push race there is the most likely culprit if scenario (B). Check the latest run's "Commit updated state" step logs.

---

## Actions taken

**None auto-executed — deliberately.**
1. **Re-trigger needs `gh`**, which isn't available in this sandbox.
2. Even if it were, re-firing blind is **unsafe here**: I cannot confirm the slots *genuinely missed* vs. *posted-but-didn't-record* (scenario B). Re-triggering under scenario B would risk the exact duplicate-publish failure the `-force` ban and dedup gates exist to prevent (cf. the 2026-05-25 triple-publish incident).
3. Did **not** attempt GUI/computer-use triggering (unattended run, you're not present) or remote git pushes (not requested; needs auth; working copy is on a feature branch).

---

## Required next actions (run on the Mac, in Claude Code, with authenticated `gh`)

**1. Diagnose — is it scenario A or B?**
```bash
cd ~/Desktop/"India Travel Planner"
gh run list --workflow=autoposter.yml --limit 15 \
  --json status,conclusion,createdAt,event,displayTitle
# Then inspect the most recent run(s):
gh run view --log-failed          # shows the failing step (look at "Run autoposter" vs "Commit updated state")
```
Also confirm whether June-4 posts actually appeared on the IG/FB/YT accounts (Outstand dashboard or the live profiles). **That single check decides A vs B.**

**2a. If scenario B (posts went out, only the commit step failed):** do **not** re-post. Fix the `autoposter-state` push/merge step and re-sync state so dedup is accurate again. Confirm `posted_today` reflects what really went out before the next cron.

**2b. If scenario A (runs genuinely didn't post):** re-trigger the active modes — **plain modes only, never `-force`** (the `-force` variants were removed in Phase C and are hard-blocked anyway). Valid `workflow_dispatch` modes for the currently-active slots:
```bash
gh workflow run autoposter.yml -f mode=yt-short    # YT Short
gh workflow run autoposter.yml -f mode=evening     # Evening IG+FB post
# Morning post has no named input — it's the blank/wall-clock default;
# backfilling a morning post mid-evening is usually not worth it.
gh workflow run autoposter.yml -f mode=analytics   # if you want the analytics sync caught up
```
Wait ~90s and re-check `gh run list` + the next `autoposter-state` commit before declaring recovery.

**3. Check the watchdog.** `autoposter-watchdog.yml` exists to catch exactly this. If the pipeline has been silent 24h+, the watchdog either also failed or isn't covering this case — worth a look:
```bash
gh run list --workflow=autoposter-watchdog.yml --limit 10
```

---

⚠️ **1 critical issue found** — autoposter state pipeline frozen since 09:18 AEST Jun 4; entire June-4 cycle unaccounted for. Auto-recovery not possible from this environment; manual diagnosis required (commands above). Disambiguate scenario A vs B **before** any re-trigger to avoid duplicate posts.
