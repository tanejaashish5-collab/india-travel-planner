# Nakshiq Autoposter Daily Audit — 2026-06-01 (Monday AEST)

Audit run at **23:00 AEST** (13:00 UTC). All times below in AEST (Sydney, UTC+10).

## Methodology note

`gh` CLI is not authenticated in the audit sandbox, so GitHub Actions run list was not directly queryable. Instead, the audit used the `autoposter-state` branch (the autoposter's own canonical record — bot commits state.json + post_log.jsonl + post_outcomes.jsonl after every successful run) plus the `main` branch as ground truth. Each `chore: autoposter state …` commit = one successful workflow run that pushed back to the repo, so missing commits in expected windows = missing runs.

## GitHub Actions activity (autoposter-state branch commits today AEST)

5 successful workflow runs committed state today (i.e. 0 push-failures detected — the branch advanced cleanly):

| AEST | UTC | Commit | What ran |
|---|---|---|---|
| 01:46 | 2026-05-31 15:46 | `3331db41` | Evening post for 2026-05-31 cycle (har-ki-doon + pithoragarh, v2_tourist_trap_split) |
| 05:44 | 2026-05-31 19:44 | `281ff7e6` | Analytics sync |
| 08:18 | 2026-05-31 22:18 | `6fe8a4eb` | Engagement pull (post_engagement.json) |
| 18:52 | 2026-06-01 08:52 | `76a81ed0` | **Morning post 2026-06-01** (yusmarg score_card) |
| 19:42 | 2026-06-01 09:42 | `a170ba65` | **YT Short slot 1** (sissu before_after) |

Plus 1 main-branch commit at 11:03 AEST (`0274ca28`) — autoposter shipping its own bug fix (`fix(cron): prewarm festival sanity count queried non-existent months[] col`). Healthy signal.

## Schedule reconciliation

**⚠️ The SKILL.md schedule is materially out of date.** Reel slots 1/2/3, tourist map, canva visual, and infographic are all **PAUSED** in the actual `.github/workflows/autoposter.yml` (paused 2026-05-16 per comments in the workflow). UGC video is disabled (low HeyGen balance). Auditing against the *actual* active cron table:

| Slot | UTC cron | AEST | Status today | Detail |
|---|---|---|---|---|
| Morning post (IG+FB) | 03:17 daily | ~13:17 + drift | ✅ Published | Fired 18:52 AEST. yusmarg / score_card. IG `lcyDK` `queued_unconfirmed`; FB `e6BYr` published. |
| YT Short slot 1 (YT+IG mirror) | 04:17 daily | ~14:17 + drift | ✅ Published | Fired 19:41 AEST. sissu / yt_short.before_after. YT `R7oUH` published; IG mirror `EizCA` `queued_unconfirmed`. |
| YT Short slot 2 (YT+IG) | 11:47 daily | ~21:47 + drift | ⏳ In drift window | Cron fired 75 min ago at 21:47 AEST. Typical GHA drift today has been 5+ hours; not yet a miss. |
| Evening post (IG+FB) | 14:17 daily | ~00:17 next AEST | ⏳ Future | Cron 1h17m away. |
| Analytics sync | 18:17 daily | ~04:17 next AEST | ⏳ Future | Daily internal. |
| Engagement pull | 21:17 daily | ~07:17 next AEST | ⏳ Future | Daily internal. |
| Weekly digest | 22:17 UTC Sun | ~08:17 Mon AEST | ❓ Inconclusive | Runs only on Sundays UTC. No separate commit visible distinct from 08:18 AEST engagement-pull commit — may have produced no file delta. Not flagged as missing. |

**No `❌ Missing` and no `💥 Failed` slots.** Two `⚠️ queued_unconfirmed` outcomes (see issues).

## Issues Found

1. **Recurring `queued_unconfirmed` on Instagram posts (3rd day in a row).** Today's IG morning (`lcyDK`, yusmarg) and IG YT-short mirror (`EizCA`, sissu) both came back from Outstand without confirmation. Same pattern hit yesterday's evening post (`gkmox`, har-ki-doon). Posts *may* have published anyway — `queued_unconfirmed` means Outstand accepted the queue but didn't return a definitive published status. Worth a manual check of the IG feed at @nakshiq for `yusmarg` (score_card carousel) and `sissu` (before_after reel).
2. **Encoding bug in post_outcomes writer (today, 09:42 UTC).** `WARNING: Could not write post_outcomes entry: 'utf-8' codec can't encode characters in position 98-99: surrogates not allowed`. Caption for the sissu IG mirror contains a surrogate-pair emoji or malformed unicode that breaks the JSONL writer. The post itself went through; only the outcomes telemetry row was dropped. Quick fix: `json.dumps(…, ensure_ascii=True)` or `errors='replace'` on the file write.
3. **SKILL.md for this scheduled task lists 6 paused content types** (reel slots 1/2/3, tourist map, canva visual, infographic). The autoposter workflow has only 7 active crons. Recommend updating `/uploads/SKILL.md` so future audits compare against the right slot list.
4. **No git push failures** detected on `autoposter-state` branch — 5 clean commits landed today.

## Actions Taken

- **No re-triggers fired.** The only slot not yet posted (YT Short slot 2) is still inside its normal drift window (cron fired 21:47 AEST, current time 23:00 AEST — only 75 min late; recent drift has been 1.5–5.5 hr). Re-triggering now risks a duplicate post; will re-evaluate at tomorrow's audit if it's still missing.
- **Did NOT re-trigger** the IG `queued_unconfirmed` posts because they may have published successfully; force-retrigger would risk duplicates. The right next step is a manual feed-check + an Outstand /analytics pull to confirm whether `lcyDK` and `EizCA` actually went live.

## Verification

After this audit completes, a useful spot-check (~30 seconds):
- IG @nakshiq feed: confirm `yusmarg` score_card and `sissu` before_after reel are visible.
- If either is missing: file as a real failure for tomorrow's audit and consider re-trigger with `gh workflow run autoposter.yml -f mode=morning` (or `-f mode=yt-short` — never `-force`).

---

⚠️ **2 issues found** — both soft warnings (queued_unconfirmed Instagram posts + encoding bug). No hard failures, no re-triggers required.
