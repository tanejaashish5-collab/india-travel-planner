# 🔍 Nakshiq Autoposter Daily Audit — 2026-05-22 (AEST Friday)

Audit run **23:05 AEST May 22** (13:05 UTC May 22 · 18:35 IST May 22). Covers the **AEST May 22 (Friday) posting cycle** and picks up where `audit_2026-05-22-am.md` (the earlier 06:35 AEST run) stopped — at commit `ea87585c` (20:14 UTC May 21 / 06:14 AEST May 22).

**Sandbox notes:**
- The `gh` CLI is **not installed** in the audit sandbox (same as the last 5 audits). GitHub Actions run conclusions and `event` types (schedule vs `workflow_dispatch`) cannot be queried, and **`gh workflow run` re-triggers are not possible from here** — the audit runs in an isolated Linux sandbox, not on the Mac where `gh` is authenticated.
- Runs are reconstructed from the `origin/autoposter-state` branch — each completed run pushes one `chore: autoposter state YYYY-MM-DD HH:MM UTC` commit. The audit reads per-commit diffs of `autoposter.log`, `data/post_log.jsonl` and `state.json`. A fresh `git fetch` was performed before this report.
- A pre-existing file named `audit_2026-05-22.md` (written by the earlier 06:35 AEST run, which actually audited the **May 21** cycle) was preserved as **`audit_2026-05-22-am.md`** before this canonical report was written. No data was destroyed.

## 📊 GitHub Actions activity

`gh` unavailable → run count derived from `autoposter-state` state-push commits. **5 completed runs landed during the AEST May 22 day** (00:00 → 23:05 AEST). **5 succeeded, 0 failed, 0 in-progress.** No Python tracebacks, no `ERROR`/`CRITICAL` lines, every run that wrote a log ended with `Run complete`. The first 3 belong to the tail of the May 21 cycle (already in `audit_2026-05-22-am.md`); the last 2 are new to this audit.

| # | UTC commit | AEST | Run mode | Result |
|---|---|---|---|---|
| 1 | `3f590956` 14:29 May 21 | 00:29 May 22 | Post (EVENING, May 21) | ✅ sissu · `festival_alert` · IG `gJStx` + FB `O2jqv` |
| 2 | `a689ed2d` 16:54 May 21 | 02:54 May 22 | Analytics sync | ✅ Ran (analytics summary written) |
| 3 | `ea87585c` 20:14 May 21 | 06:14 May 22 | Engagement pull | ✅ Ran (internal; no main-log output) |
| 4 | `f068b898` 07:04 May 22 | 17:04 May 22 | Post (MORNING, May 22) | ✅ dwarka · `hidden_gem_reveal` · IG `Qc1my` + FB `l5atK` |
| 5 | `72691a3e` 07:58 May 22 | 17:58 May 22 | Post (MORNING, May 22) | ✅ Duplicate cron — both platforms correctly dedup-skipped |

No `rejected` / `failed to push` strings in any committed run log; the `autoposter-state` commit chain is intact, so **no git-push failures detected**. (Raw GHA job logs are not inspectable without `gh`.)

## 📅 Schedule basis — IMPORTANT

The audit task's reference schedule (reel slots 1/2/3, tourist map, canva visual, infographic, 3× YT short) is **stale**. `autoposter.yml` was rewritten on **2026-05-16 ("CADENCE CUT")** — pivoting from 6 IG+FB posts/day to 2 after the user's diagnosis that high posting volume was triggering an algorithm penalty on a sub-1k-follower account. **Reel slot, visual slot (tourist-map / canva / pomelli), reel-map, flow-story and UGC crons are all PAUSED** until the account crosses 1k followers.

Current **live daily schedule = 6 slots** (all crons land on `:17`/`:47` to dodge GHA heavy-minute skips):

| Slot | Cron (UTC) | AEST | Platforms |
|---|---|---|---|
| Morning post | `17 3` | 13:17 | IG + FB |
| YT Short slot 1 | `17 4` | 14:17 | YouTube (+IG) |
| YT Short slot 2 | `47 11` | 21:47 | YouTube (+IG) |
| Evening post | `17 14` | 00:17 (next day) | IG + FB |
| Analytics sync | `17 18` | 04:17 (next day) | internal |
| Engagement pull | `17 21` | 07:17 (next day) | internal |
| Weekly digest | `17 22` Sun | 08:17 (next day) | internal |

Today is **Friday** — under the task's old schedule the infographic (Mon/Wed/Fri) would be due and the tourist map (Tue/Thu/Sat) would not. In reality **neither is scheduled** any more (crons commented out), so they are **not flagged as missing** below.

## Content Published Today (AEST May 22 cycle)

| Slot | Scheduled (AEST) | Status | Detail |
|---|---|---|---|
| Morning post | 13:17 | ✅ Published | dwarka · `hidden_gem_reveal` reel · IG `Qc1my` (platform 18085133603409927) + FB `l5atK` (platform 1564546448362220). Posted 17:03–17:04 AEST. YouTube correctly skipped (feed post, video-only platform). Duplicate 17:58 run dedup-skipped cleanly — **dedup gate working.** |
| YT Short slot 1 | 14:17 | ❌ Missing | No YT short posted. See Issue 1. |
| YT Short slot 2 | 21:47 | ⏳ Overdue | No run committed as of 23:05 AEST. See Issue 2. |
| Evening post | 00:17 (May 23) | ⏳ Not yet due | Cron fires 14:17 UTC. |
| Analytics sync | 04:17 (May 23) | ⏳ Not yet due | Internal. |
| Engagement pull | 07:17 (May 23) | ⏳ Not yet due | Internal. |

**Prior cycle (May 21) closed cleanly:** morning (chopta), YT Short (tungnath → YouTube `ZI89u` + IG `8k3cI`), evening (sissu), analytics sync, engagement pull — all ✅ (full detail in `audit_2026-05-22-am.md`).

## Issues Found

**1. YT Short slot 1 (14:17 AEST) — MISSED.** No YouTube short posted today; `data/post_log.jsonl` has zero `youtube` entries for 2026-05-22 (last YT short was tungnath, May 21). Root cause: **chronic GHA cron drift.** The morning post fired ~3h45m late (03:17 UTC cron → 07:02 UTC), and the autoposter's mode-router classifies runs by wall-clock time. The 04:17 UTC YT-short cron drifted into the same 07:00–08:00 UTC window and was re-routed to **MORNING post mode** — that is the 17:58 AEST run, which then correctly dedup-skipped. The YT-short slot was effectively consumed by drift.

**2. YT Short slot 2 (21:47 AEST) — OVERDUE.** Its cron (11:47 UTC) was due 1h18m before this audit. No `autoposter-state` commit has landed since 07:58 UTC. Given this account's routine 1–3h+ GHA scheduler drift it may still fire late tonight, but as of 23:05 AEST it has not run.

Net result: **0 of 2 YT shorts posted today.** Morning IG+FB post is the only content live for the May 22 cycle so far.

## Actions Taken

**None possible from the audit sandbox.** Re-triggering requires `gh workflow run`, and `gh` is not installed/authenticated here (a known limitation across the last 6 audits). No GitHub token is available either, so the workflow_dispatch API cannot be reached. The audit did **not** attempt a fragile browser-based re-trigger autonomously.

**Recommended manual re-trigger** — from the Mac, in the repo root, run (once per missed slot — the autoposter caps YouTube at 2/day and `mode=yt-short` respects dedup + format rotation, so running it twice is safe):

```
cd ~/Desktop/India\ Travel\ Planner
gh workflow run autoposter.yml -f mode=yt-short
gh workflow run autoposter.yml -f mode=yt-short
```

⚠️ Use the **plain `yt-short`** mode — never `yt-short-force`. The `-force` variant bypasses anti-repetition tracking and causes duplicate content.

## Observations / Recommendations

- **Chronic morning-cron drift** is the systemic culprit. The 03:17 UTC morning cron has drifted 3–4h late on multiple recent days, and when the 04:17 UTC YT-short-1 cron drifts the same amount it lands in the morning/visual time-window and gets misclassified as a feed post. Consider widening the YT-short mode-detection window, decoupling YT-short routing from wall-clock time (e.g. route by the dispatch's own cron identity rather than arrival time), or moving the YT-short-1 cron further from the morning cron.
- The `autoposter-state` chain and dedup gate are healthy — the morning post and its duplicate behaved exactly as designed.
- This audit ran at 23:05 AEST, mid-cycle: the evening post, analytics sync and engagement pull for May 22 were not yet due and will be assessed by the next audit.

## Verdict

⚠️ **2 issues found** — YT Short slot 1 missed, YT Short slot 2 overdue. Manual `gh workflow run autoposter.yml -f mode=yt-short` (×2) recommended. Morning IG+FB feed post published cleanly; no failures, crashes, or git-push problems.
