# This repo is PUBLIC. accounts.json names real Google accounts, so it lives
# only in the live copy at ~/Automation/nakshiq-veo/ and is never mirrored here.
accounts.json
profile/
downloads/
clips/
veo_queue.json
generated.json
node_modules/
