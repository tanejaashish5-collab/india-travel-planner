# NakshIQ — Missed-Opportunities Audit (2026-06-10)

**TLDR:** The product core is genuinely well-covered — data integrity, QA discipline, and zero-fabrication rules held up under adversarial verification. What's missing is last-mile wiring: 1 live prod bug (36 weekend-from URLs serving 404), a distribution machine that ends at a page view (6 email subscribers vs the 2,000 monetisation gate, 0 alerts ever sent, 391 social posts with 1 newsletter mention), and ~a dozen fully-built capabilities never switched on (IndexNow, CI gates, RAG refresh, loop scheduling, GSC OAuth). 39 confirmed findings; most are low-effort re-surfacing of assets already built and paid for.

## Top 10 — ranked

| # | Opportunity | Why it matters (numbers) | Effort | First step |
|---|---|---|---|---|
| 1 | Fix /weekend-from 404s | 18 cities × 2 locales = 36 sitemap URLs 404 on prod; same broken RPC degrades "nearby" on all 525 hubs | Low | One ALTER FUNCTION migration (add `extensions` to search_path) + revalidate |
| 2 | Off-platform DB backup | 3 destructive prod sweeps in 48h; only restore = 7-day Pro window (zero after a downgrade); 453 clips/612MB on one laptop | Low | Weekly pg_dump→R2 script + GHA schedule |
| 3 | Resurrect CI gating | quality-gate.yml: 100/100 runs failed at workflow level — PRs get NO checks; e2e suite red 8 weeks | Low | Fix the invalid secrets-`if` (~1 line), then triage red specs |
| 4 | Feed the email machine | 6 subscribers vs 2,000 gate; 0 alerts ever sent; 391 posts carry 1 newsletter mention; /newsletter page already live | Low | Rotating CTA in captions + all YT descriptions; absolute counts in ops digest |
| 5 | Refresh the RAG index | All 2,513 embeddings frozen 2026-04-13; chat still serves purged fabrications ("NASA Van Allen", "Alexander") | Low | Your OK on the OpenAI re-embed (~cents), then POST /api/embed + monthly cron |
| 6 | GSC automation | OAuth token on disk works (proven live); M2 monitor blind since 05-29; hemkund-sahib is ALREADY consolidated — the 6-week item closes with zero clicks | Med | Close hemkund today; build gsc-inspect-sweep.mjs |
| 7 | /itinerary/[slug] | 491/525 dests have day-structured itineraries buried in the hub; competitors anchor on this query class | Med | New route mirroring /cost pattern + min-content gate |
| 8 | Confidence cards: 34 pilgrimage dests | tirumala, katra, palani render zero cards — core product absent on highest-intent pages | Med | Restructure already-verified row data via pg-bulk |
| 9 | Cross-family internal links | 1,050 /with-kids + 3,334 /vs URLs near-orphaned after the cinematic flip | Med | Restore with-kids link + "Compare with…" row in the cinematic hub |
| 10 | Loop Phase 3 scheduler | Propose cycle built, never runs; headless iMessage proven working on this Mac (live test passed) | Med | launchd plist running `claude -p "/loop-propose"` 3x/week |

## Remaining confirmed gaps (29)

**Product:** 1,661 POIs have zero map links while eateries/hidden-gems on the same page do (~20 LOC). Trip board severed — 1 board ever; seed from saved list + Add-to-trip CTA. Mobile app: see Decisions.

**SEO/AEO:** llms.txt 6 weeks stale ("488" vs 525 dests, omits cost/safari/pilgrimage/festivals). /vs pages (3,334 URLs) emit no FAQPage/Article schema — one file. Sitemap omits india-vs + 7 live families (~20 lines).

**Conversion:** /vs — 4 of top-10 GSC pages, 1.7–5.0% CTR — has zero capture/booking. Cost calculator never offers "email me this budget." 501 festival pages have no date-reminder capture despite fitting alert infra. Airalo (12%) + 12Go (50%) ride the Travelpayouts signup already pending. One-off alert backfill for the orphaned subscriber (~30 lines).

**Data:** destination_costs reviewed_at 0/12,693, permits have no timestamps — ship a "prices recorded" date first. viral_eats: 386 rows fetched on every hub render, never rendered.

**Distribution:** Site links zero social accounts (sameAs = Wikidata only after 391 posts). /social hardcodes dhanaulti while every IG caption says "link in bio." Pinterest: $0 marginal via existing Outstand plan, 1,286 ready images. Telegram channel: ~20 lines, $0. Long-form YT: weak — clips are 6–8s AI loops (impact 2).

**Perf/tech-debt:** React #418 root-caused and reproduced — 365 dest pages break for US-West visitors; add `timeZone:"Asia/Kolkata"`. Zero client error telemetry (sendBeacon→ops_reports, ~25 lines). All media on rate-limited r2.dev (see Decisions). Admin routes: drop `?key=` auth, split the shared secret (~1h).

**Capability/ops:** QA findings never enter the propose pipeline. verify-findings punts GSC/GA4 checks it has working creds for. Road-sweep emails you 7 stale rows (Rohtang, Kunzum) an agent can verify itself. IndexNow fully built, zero callers. MEMORY.md 126KB vs 24.4KB limit — 73% of entries silently dropped every session; compact now.

## Checked and NOT a gap

- **Hindi sidecar content claim — REFUTED.** The ~5,500 Hindi translation rows do render; no wasted translation spend.
- **"Laptop is sole copy of ops tooling" (800 untracked files, 11/19 loop scripts, 36/42 GSC audits) — REFUTED** on verification.
- **GA4 `aio_referral` dimension:** not actually founder-blocked — convertible to a one-time role bump + script.
- **CLAUDE.md pending-items list:** at least 3 items verified already DONE — the task list is partly fiction; prune it.

## Decisions only you can make

1. **Mobile app:** freeze (recommended — PWA live, only 4 commits ever) or ship via EAS (Apple $99/yr + Google $25).
2. **OpenAI re-embed** (#5): paid metered API, ~cents for ~2.5K chunks — confirm before run.
3. **New accounts:** Pinterest business (connect in Outstand) and Telegram channel — 5-min identity steps each.
4. **R2 custom domain:** nakshiq.com runs GoDaddy nameservers — migrate DNS to Cloudflare (free, moves all records) or buy a ~$10/yr media domain.
5. **Travelpayouts signup** (already on your list) — now also unlocks Airalo + 12Go.

---

# Appendix — all 39 confirmed findings (full detail)
> Method: 55-agent workflow (3 mappers, 8 dimension finders, adversarial verifier per finding, 1 synthesizer). Every finding below survived a "prove it wrong" verification pass against the live site, the repo, and the production DB. Counts: 39 confirmed / 2 already-done / 2 refuted / 0 inconclusive.

## 1. 491 destinations have day-structured itineraries in the DB but no standalone /itinerary intent surface — the #1 thing both competitors anchor on
- **Dimension:** product-surfaces · **Impact:** 4/5 · **Effort:** medium
- **Action:** Build /itinerary/[slug] (e.g. /itinerary/jaipur) rendering the existing micro_itineraries one/three/five-day plans, enriched with already-verified sidecar data on the same page (top POIs, 2-3 legendary eateries, stay picks, /cost/[slug] link), TouristTrip + FAQPage JSON-LD, title pattern 'Jaipur itinerary — 1, 3 and 5 days'. Add to sitemap + a link from the hub's Act X. Zero new data needed, zero fabrication risk — it is a re-surfacing of audited content, mirroring the proven /cost pattern. Caveat to handle: some five_days blocks are terse ('Amber deep.') — gate page generation on a minimum-content threshold like the existing data-eligibility patterns.
- **Verifier:** Verified via SQL (491/525 dests have day-structured micro_itineraries; jaipur row has timed one_day blocks + bad_weather_plan + 3/5-day plans), repo ls/grep (no itinerary route; only render is destination-detail-cinematic.tsx:2741; /plan,/trip,/build-route,/routes are different intents), prod curl (404 on /en/itinerary/jaipur), and live Holidify fetch ("Itinerary for Jaipur" anchor section, 19 mentions). Not in .loop ledger or KNOWN OPEN items — genuine gap. Caveat verified: content is compact (median 1224 chars JSON, five_days blocks terse like "Amber deep."), so the proposed sidecar enrichment + min-content gate is mandatory to avoid thin pages.

## 2. All 1,661 POIs render with zero map/navigation link — while eateries (97%) and hidden gems on the SAME page already have them
- **Dimension:** product-surfaces · **Impact:** 3/5 · **Effort:** low
- **Action:** Add a 'Directions' link to each POI card in poi-section.tsx using the same deterministic maps-search URL the hidden-gems section already uses (URL-encoded '<poi name> <destination> India'). No DB write, no fabrication risk (it is a search query, not an asserted address), ~20 LOC + Hindi label parity. Optionally later backfill real google_maps_url per POI, but the search URL ships value now.
- **Verifier:** Verified all three legs: poi-section.tsx has 0 maps links and no maps/coords field in its POI interface; live /en/destination/jaipur renders 13 POI cards with zero maps hrefs while hidden-gems on the same page carry 3 deterministic maps-search links (pattern exists, POISection never got it); SQL confirms points_of_interest google_maps_url = 0/1,661 (and coords only 34, address 15 — so the deterministic search URL is the only viable ship-now fix). Not tracked in .loop/pending-actions.md, OPPORTUNITIES.md, or the known-open list.

## 3. Expo mobile app: zero distribution, never store-shipped, 3+ weeks stale — parity work is a recurring tax with no users
- **Dimension:** product-surfaces · **Impact:** 2/5 · **Effort:** low
- **Action:** Make the explicit founder decision and record it in CLAUDE.md: FREEZE apps/mobile (no new parity work, mark status 'frozen pending traffic gate — PWA is the mobile strategy') OR commit to shipping via EAS (requires founder Apple $99/yr + Google $25 accounts — identity-gated). Given pre-revenue + time-poor + a working PWA, freezing is the defensible default; it stops silent parity drift from consuming future sessions.
- **Verifier:** All facts verified: no eas.json, app.json v1.0.0 with no EAS projectId and placeholder Supabase key; Play Store com.nakshiq.app returns 404 and iTunes lookup resultCount=0 (deterministic, not just WebSearch); last mobile commit a900fbeb 2026-05-16 with zero screens for cost/safari/pilgrimage/arrival; PWA live (manifest.json + sw.js both 200); no freeze/ship decision in CLAUDE.md, and docs/sprint-history.md line 35 marks "Expo parity ✓" which invites future parity work. However the "recurring tax" framing is overstated — only 4 commits EVER touched apps/mobile (none in 3.5 weeks), so this is cheap drift-prevention hygiene (a 2-line CLAUDE.md freeze note), not an active resource bleed.

## 4. Trip board is severed from where intent forms — no 'add to trip' on destination pages and the saved list never seeds the board
- **Dimension:** product-surfaces · **Impact:** 3/5 · **Effort:** medium
- **Action:** Two small wires, no new surface: (1) 'Start your board from your saved list' one-tap seeding on /trip when the board is empty and localStorage saves exist; (2) an 'Add to trip' action on the destination hub next to the existing Save CTA (reuses trip-storage.ts addStop). This completes the only end-to-end plan-a-trip journey the site has; today NakshIQ answers point questions superbly but the planning canvas is an island.
- **Verifier:** Verified all evidence: destination-detail-cinematic.tsx has zero trip-board/addStop references (prod jaipur's only "trip board" string is i18n JSON, not a control); grep of components/trip-board/ for savedIds/nakshiq-saved/useSaved is empty and addStop is only imported inside trip-board/; SQL confirms trip_boards=1, profiles=5. Bonus finding: cold-start.tsx already has a 4-path seed wizard (curated/month/profile/theme) with applySeed plumbing in index.tsx:90 — a "from your saved list" 5th path is even cheaper than proposed (only needs id→slug mapping since saves store IDs, stops are slug-keyed). Not tracked anywhere (.loop/, HANDOFF, sprint-history).

## 5. Owned-audience reality check: 6 newsletter subscribers and 0 destination_alerts rows ever — the peak-alert pipeline has never sent a single alert
- **Dimension:** product-surfaces · **Impact:** 4/5 · **Effort:** low
- **Action:** Add absolute counts (subscribers, active alerts, boards) to the weekly ops digest so percentage metrics can never mask a base of 6 again; then explicitly deprioritize new on-site capture/CRO work in the loop config until organic traffic grows (the scout already disabled cro-leak — extend that reasoning to capture-surface proposals). One genuine product check worth 30 minutes first: send a test save→subscribe→confirm flow end-to-end on prod to prove a destination_alerts row gets created, ruling out a silent prod-only failure (the helper swallows errors by design, console.error only).
- **Verifier:** Live SQL confirms every number: newsletter_subscribers=6 (only 4 confirmed genuine; 1 smoke-test, 1 unconfirmed), destination_alerts=0 ever, trip_boards=1, newsletter_issues=8 — the send-destination-alerts cron has never had a row to send. New sub-finding: the only real savelist subscriber (save-prompt-4, confirmed 2026-05-26, saved kalpa/hemkund-sahib/guptkashi/kaza, all 4 alert-eligible at peak score 5) predates the 2026-06-02 sync wiring, which only fires at confirm/re-save and has no backfill script — so the welcome-email peak-alert promise is unfulfilled for the one person it was made to, and the wired path has never executed in prod (no confirms since 06-02). Mild discount: the "deprioritize CRO" conclusion partially restates the already-disabled cro-leak reasoning; the genuinely new actions are absolute counts in the ops digest, a one-off 4-row alert backfill, and one exercised end-to-end confirm to prove the new path (skipped here — it sends a real email).

## 6. ALL 18 /weekend-from-{city} pages 404 on prod (36 sitemap URLs) — PostGIS RPC broken by a pinned search_path
- **Dimension:** seo-aeo · **Impact:** 4/5 · **Effort:** low
- **Action:** Ship a migration: ALTER FUNCTION public.find_nearby_destinations(double precision,double precision,integer) SET search_path = public, extensions, pg_catalog; same for find_nearby_helpers. Then force-revalidate/redeploy (pages are ISR 6h so the 404s are cached). Add a QA-harness probe that curls one weekend-from URL daily, and a rule: any future advisor search_path pinning must include 'extensions' for PostGIS-using functions.
- **Verifier:** Reproduced everything first-hand: 8/8 curl checks 404 (all 18 cities x 2 locales = 36 URLs still in live sitemap/0.xml); SQL repro returns ERROR 42704 'type geography does not exist' inside find_nearby_destinations; pg_proc shows search_path=public,pg_catalog pinned on both find_nearby_* functions while postgis lives in 'extensions'; weekend-from-view.tsx line 63 calls notFound() on RPC error and the dest-hub fetchNearby silently falls back same-state. Not tracked anywhere — migration 061 pinned only the 2 SOS functions, no migration/script/memory pins find_nearby_*, and qa/findings 2026-06-09/10 (both GREEN) never mention weekend/nearby. Added root-cause context: session_2026_05_28_postgis_relocation_search_path_verification.md green-lit the relocation by checking session-level search_path for API roles, which structurally misses pinned-proconfig functions — pin + relocation = breakage.

## 7. llms.txt stale for 6 weeks — wrong destination count and missing every surface shipped since April
- **Dimension:** seo-aeo · **Impact:** 3/5 · **Effort:** low
- **Action:** Write scripts/gen-llms-txt.mjs that regenerates both files from the cached reference indices (getCachedDestinationsIndex + the same allowlist queries the sitemap uses): live destination count, one section per surface family with 2-3 example deep URLs and the per-family citation guidance ('for trip cost cite /en/cost/{slug}', 'for festival dates cite /en/festivals/{slug}'). Run it in the same flow that busts reference caches so it can't go stale again.
- **Verifier:** Confirmed all evidence: llms.txt last commit d7f9e6e3 (2026-04-30), live file says "488 destinations" 3x while /api/search-index returns 525; grep of llms.txt for cost|safari|pilgrimage|vs|arrival|weekend|festival = 0 hits while all those routes exist in apps/web/src/app/[locale]/; no scripts/*llms* generator exists and the gap is untracked (qa/ mentions are an old, fixed numeric-month-route issue). Minor finder overstatement: llms-full.txt does list /vs, /weekend-from, /guide, /collection route patterns, but still 488 and omits /cost, /cost-index, /safari, /pilgrimage, /festivals, /best, /arrival.

## 8. /vs pair pages (~250 pairs × 2 locales, sitemap priority 0.8) emit NO content schema — only BreadcrumbList
- **Dimension:** seo-aeo · **Impact:** 3/5 · **Effort:** low
- **Action:** In apps/web/src/app/[locale]/vs/[pair]/page.tsx, add a FAQPage built from data already fetched: 'Which is cheaper, X or Y?' (destination_costs), 'Which is better for kids?' (kids_friendly), 'Which is better in {current month}?' (destination_months scores), plus an Article node mirroring the month-page pattern. Zero new data, one file, both locales. Same pattern-gap exists on /india-vs/[country] (15 pages) — fix in the same pass.
- **Verifier:** Verified live + in source: /en/vs/shimla-vs-manali serves only 2 JSON-LD blocks (Organization/WebSite graph + BreadcrumbList); apps/web/src/app/[locale]/vs/[pair]/page.tsx builds only breadcrumbLd while already fetching daily_cost/kids_friendly/destination_months, and the gap is tracked nowhere (no FAQPage commit ever in the vs dir, absent from .loop/OPPORTUNITIES.md). Surface is actually 6.7x bigger than claimed — 1,667 unique pairs / 3,334 URLs in live sitemap chunk 4 — but /india-vs is overstated: it already emits Article (line 51), missing only FAQPage. Impact tempered because Google restricted FAQ rich results to govt/health sites (2023) and AI engines extract mainly from visible text, so schema is a consistency/parse signal, not a guaranteed citation win.

## 9. Sitemap omits live high-value families: india-vs (32 URLs), cost-index, nakshiq-100, sos, gap-year, risk-quiz, explore-by-persona, for/[persona]
- **Dimension:** seo-aeo · **Impact:** 3/5 · **Effort:** low
- **Action:** Add the missing statics to the chunk-0 staticPages array and emit india-vs entries from COUNTRY_LIST (mirror the arrival/[iata] pattern already in chunk 0). ~20 lines in one file.
- **Verifier:** Verified in code (sitemap/[file]/route.ts chunk-0 staticPages lacks all 8 families; india-vs-countries.ts never imported) and live (grep india-vs → 0 in all 5 chunks; cost-index/nakshiq-100/sos/gap-year/risk-quiz/explore-by-persona/for → 0 in chunk 0). All 9 spot-checked URLs return 200 with self-referential canonicals, no noindex, robots.txt allows them — so inclusion is valid and the gap is real. Not tracked in .loop/pending-actions, OPPORTUNITIES.md, or qa/findings (QA only counts sitemap totals). Caveat for the fix: sitemap only the /gap-year hub, never /gap-year/[token] personalized pages.

## 10. Cross-family internal links lost in the cinematic flip: hub pages link to none of /vs, /with-kids (1,050 near-orphan URLs), /pilgrimage
- **Dimension:** seo-aeo · **Impact:** 4/5 · **Effort:** medium
- **Action:** Three small edits: (1) in destination-detail-cinematic.tsx add a 'Compare {name} with…' row rendering links for VS_PAIRS containing this destination, plus restore the '{name} with kids' link (port from the legacy component); (2) in vs/[pair]/page.tsx link each side's /cost/[slug]; (3) where a destination has a pilgrimage_routes row, link it from the hub (mirror the existing safari wiring). All data already loaded on these pages.
- **Verifier:** Confirmed in code and live: destination-detail-cinematic.tsx has 0 links to /vs/, /with-kids/, /pilgrimage/ (cost+safari ARE wired, lines 1574/1623), the dropped links exist only in legacy destination-detail.tsx (lines 427/432), and live shimla/jaipur-december/varanasi HTML carry zero such hrefs while /en/vs/shimla-vs-manali links only the two hubs + /cost-index. Sitemap impact is BIGGER than claimed: 1,050 /with-kids/ + 3,334 /vs/ URLs (~1,667 pairs, not ~500) + 16 /pilgrimage/ are near-orphaned; the only inlink hits in nav/footer are CINEMATIC_PAGE_PREFIXES style lists, and the verdict strip's "vs" CTA goes to /compare?compare= (client-only, non-SEO). Not tracked in .loop/OPPORTUNITIES.md or pending-actions; one fix-detail correction — derive vs pairs from dest.nearbyDestinations (already in scope, matches sitemap generation), there is no VS_PAIRS constant.

## 11. The only fulfilled email promise has 0 recipients — destination_alerts is empty and the one real savelist subscriber is orphaned
- **Dimension:** conversion-monetisation · **Impact:** 2/5 · **Effort:** low
- **Action:** Write a one-time backfill script (scripts pattern: small N, REST fine) that selects confirmed, non-unsubscribed newsletter_subscribers with non-empty saved_destination_ids and runs syncSavedDestinationAlerts for each. ~30 lines, reuses the existing consent-safe helper (double-opt-in already satisfied, peak-month gate, 10-alert cap all built in). Also delete or mark the smoke-test row so list metrics stay honest.
- **Verifier:** Live SQL confirms destination_alerts=0 and the save-prompt-4 subscriber (confirmed 2026-05-26 14:19 UTC, saved IDs kalpa/hemkund-sahib/guptkashi/kaza) predates the 2026-06-02 sync wiring (c8e4225d); syncSavedDestinationAlerts is only called from confirm/route.ts:85 and subscribe/route.ts:90, grep of scripts/ finds no backfill, and the send-destination-alerts cron reads destination_alerts so it has never had a recipient. Gap is real and untracked, but affects exactly 1 genuine subscriber today — the proposed ~30-line consent-safe backfill (plus marking the smoke-test row) is correct and cheap.

## 12. /vs — the site's highest-CTR, fastest-growing page class — has zero conversion surface (no capture, no booking handoff)
- **Dimension:** conversion-monetisation · **Impact:** 3/5 · **Effort:** low
- **Action:** Add to vs/[pair] below the verdict: (a) the existing BookingHandoff for the WINNING destination (component takes destinationName/stateName/destinationId — direct reuse, links auto-tag the moment the pending Travelpayouts/Booking/Agoda env vars land); (b) a save CTA for the winner so the /vs reader enters the existing save→email→peak-alert funnel. Both are existing components; ~1 day including Hindi parity and Playwright check.
- **Verifier:** Verified in code and live: vs/[pair]/page.tsx renders header→VsComparison→CinematicRelatedRail→Footer with zero capture/booking components (grep 0 hits in page + vs-comparison.tsx; BookingHandoff imported only by cost/safari/dest-cinematic/dest-month), and the live kasauli-vs-manali page has no external booking hrefs — the only 'bookingHandoff'/'newsletter' matches are the i18n bundle shipped to every page. GSC audit 2026-06-09 confirms the traffic claim verbatim (4 of top 10 pages, 1.7–5.0% CTR vs ~0.5%), and nothing in .loop tracking, the KNOWN OPEN list, or git history addresses vs-page conversion (audit action #4 is 'build more pairs', not 'convert them'). Tempered: ~31 clicks/wk absolute across the top /vs pages and affiliate links stay untagged until the founder-blocked env vars land, but the save CTA feeds the email-list gate today and the fix is ~1 day of existing-component reuse.

## 13. 501 festival pages (442 with fresh hero video) have no capture and no date-reminder hook, despite destination_alerts infra being a near-exact fit
- **Dimension:** conversion-monetisation · **Impact:** 3/5 · **Effort:** medium
- **Action:** Add a 'Get a heads-up before [festival]' capture on festival detail pages that writes a destination_alerts row targeting the festival's host destination + month (or a thin festival_alerts variant of the same table). Reuses the subscribe/confirm double-opt-in flow and the existing cron; copy must stay honest for the 11 movable-date festivals ('dates vary — we alert on the host destination's month'). Optionally add the host destination's BookingHandoff below the fold.
- **Verifier:** Confirmed: festival detail page (1,022 lines) has zero capture/alert/remind components (grep = 0 hits; live hemis-festival page has no email input, only footer chrome), while destination_alerts + /api/destination-alerts/* + daily send-destination-alerts cron all exist (migration 058, vercel.json) and nothing in .loop docs or the known-open list tracks festival capture. Two corrections: (1) the subscribe API resolves target_month_num server-side via getPeakMonth(destinationId) and ignores client months, so a festival reminder needs a festival-aware subscribe path + new email copy — machinery reuse, not drop-in; (2) the page already links 4x to the host destination, so 'no handoff' is overstated — only capture is missing. Movable-date caveat real (490/501 have month).

## 14. Affiliate map covers only hotels + activities — eSIM, trains/buses, and insurance slots are absent, and two of them ride the SAME Travelpayouts signup already pending
- **Dimension:** conversion-monetisation · **Impact:** 3/5 · **Effort:** medium
- **Action:** Extend affiliate.ts with airalo + 12go partners behind the existing env-var-flip pattern (zero behaviour change until founder's Travelpayouts marker lands — same no-regression design as today). Then add honest, clearly-labelled link slots: Airalo on guide/sim-card + all 12 /arrival/[iata] pages (the inbound traveller's first-hour need); 12Go on guide/book-indian-trains + guide/transport-overview as the 'tourist-friendly alternative to IRCTC' option (only after confirming India rail inventory in the dashboard). This multiplies the payoff of the single founder action already at the top of the pending list.
- **Verifier:** Verified: affiliate.ts has only 5 hotel/experience partners; repo+live-site greps show zero eSIM/trains/insurance link slots (sim-card guide, trains guide irctc/erail/confirmtkt untagged, arrival SIM sections bare); nothing tracked in .loop/ or pending-actions. Travelpayouts offer pages confirmed live: Airalo 12%/eSIM and 12Go 50% rev-share (~$2.9/booking, India covered) both ride the founder's already-pending Travelpayouts signup. One correction: Airalo uses tp.media redirect deeplinks with account-specific trs/p params, not the query-param marker pattern affiliate.ts models — needs a small deeplink builder, not a 2-line record extension.

## 15. /cost/[slug] computes a personalised trip budget but never offers to email it — the strongest natural lead-magnet moment on the site has zero email surface
- **Dimension:** conversion-monetisation · **Impact:** 3/5 · **Effort:** medium
- **Action:** Add an 'Email me this budget' button to the cost calculator result: POST to a small route that renders a react-email budget breakdown via the existing Resend client, with an unchecked 'also send The Window weekly' consent checkbox feeding /api/newsletter/subscribe (source='cost-calculator'). Strictly opt-in, no fabricated urgency. Roll the same pattern to gap-year report-builder output later.
- **Verifier:** Verified in repo + live: cost-calculator.tsx and cost/[slug]/page.tsx have zero capture code AND no save button, so the layout-level SaveListEmailPrompt can never fire there — the cost page is a full capture dead-end despite producing the site's most keepable artifact. All claimed infra exists (subscribe route with source/tags, react-email templates incl. the-window.tsx, Resend send route with sent_at dedup), guide pages (visa/sim-card/first-trip-india/scams) also grep 0 capture, and the idea is tracked nowhere (.loop/pending-actions, OPPORTUNITIES.md, derisk plan all silent on it).

## 16. 34 pilgrimage destinations (tirumala, katra, palani, Ashtavinayak, Panch Prayag) have NO confidence card — the core product is absent on the highest-intent religious-traffic pages
- **Dimension:** data-coverage · **Impact:** 4/5 · **Effort:** medium
- **Action:** Backfill confidence_cards (reach/sleep/fuel/weather_night/network/emergency/safety) + kids_friendly for the 34 dests using the established adversarial-verified workflow. Much of it already exists on the destinations row (cell_network, nearest_railhead, medical_facility, stay_zones) and in the verified emergency_sos rows — so this is largely a restructure of already-verified data, honest-scarcity for the rest. Write via pg-bulk direct Postgres.
- **Verifier:** DB-verified: 525 dests vs 491 confidence_cards/491 kids_friendly rows, 34 missing — slug list matches the pilgrimage cohort (tirumala, katra, palani, 8 Ashtavinayak, 3 Panch Prayag). Live-verified: tirumala has 0 'Safety Rating' (jaipur 1) and katra renders the Act V prose promising cards then shows none; not tracked in .loop/, qa findings, or KNOWN OPEN. Minor corrections: only 14/34 are on the cinematic prose-then-nothing path (tirumala/palani + 18 others use the fallback destination-detail.tsx, cards equally absent at line 1306); Panch Prayag is 3/5 missing not 5; /pilgrimage funnels into 2 of the 34 (katra, astavinayak-circuit).

## 17. Ask-NakshIQ RAG index frozen since 2026-04-13 — chat answers from a stale half-catalog that predates every June fact-correction sweep
- **Dimension:** data-coverage · **Impact:** 4/5 · **Effort:** low
- **Action:** Two steps: (1) re-run POST /api/embed to refresh the existing classes — NOTE it uses OPENAI_API_KEY (paid metered, ~2.5K chunks ≈ cents but needs founder confirm per the rules); (2) extend the embed route to add eateries/festivals/pilgrimage/safari sources and wire a monthly cron (or fold into an existing monthly sweep) so the RAG index can never drift 8 weeks behind verified-fix sweeps again.
- **Verifier:** SQL-verified: all 2,513 document_embeddings rows created 2026-04-13 10:15-10:17 UTC, source_type counts exactly as claimed, no cron in vercel.json hits /api/embed, and the gap is untracked (grepped .loop/, HANDOFF.md, CLAUDE.md, sprint-history). Smoking gun: the index still serves June-purged fabrications — kasar-devi-temple chunk says "NASA-detected Van Allen Belt anomaly" and kangra-fort says "Mentioned in Alexander's war records" while the live points_of_interest rows carry the corrected text, so /api/chat can ground answers in fabrications the zero-fake-data policy already removed everywhere else. Coverage is actually slightly worse than claimed: 260/525 dests, 777/1,661 POIs, 0/3,119 eateries embedded. Mitigation keeping it off 5: live structured queries override RAG for ~50 regex-matched popular dests, and the surface is rate-limited to 10 questions/IP/day.

## 18. Money data has zero freshness loop: destination_costs.reviewed_at is 0/12,693 and the permits table has no timestamp columns at all
- **Dimension:** data-coverage · **Impact:** 3/5 · **Effort:** medium
- **Action:** (1) Cheap first step: surface 'Prices recorded <month year>' on /cost pages from max(recorded_at) — honesty win, zero data work. (2) Add a quarterly cost-review cadence mirroring the SOS smart-cadence pattern (stale view + Monday digest line), prioritising the categories that move (fuel, permits-adjacent, stay bands). (3) Migration to add reviewed_at/updated_at to permits + fold permit fee checks into the existing monthly news-sweep or SOS-style verify script.
- **Verifier:** SQL-verified: destination_costs reviewed_at = 0/12,693 (recorded_at 2026-04-24→2026-06-03) and permits (32 rows, all with cost_inr) has no *_at columns at all. Greps confirmed none of the 14 crons in apps/web/src/app/api/cron/ touch either table (freshness-drift reads only `destinations`), and live /en/cost/manali + /en/permits render no as-of/recorded date (permits "verified" hits were i18n bundle strings). Not in the known-open list — SOS cadence and freshness-drift cover different tables.

## 19. viral_eats (386 rows, 229 dests) is fetched on every destination-hub render but the active cinematic component never renders it
- **Dimension:** data-coverage · **Impact:** 3/5 · **Effort:** low
- **Action:** Either (a) render viral_eats inside the existing cinematic food act (small editorial block beside eateries — content is already verified/honest-review toned), or (b) drop `viral_eats(*)` from the hub select to cut dead payload. (a) is the value play: ~229 dest pages gain a section users and AEO crawlers actually see.
- **Verifier:** Verified: viral_eats(*) fetched at page.tsx:186, cinematic component has 0 references, and ALL 229 viral_eats dests are in the 505-slug cinematic allowlist so none render it; live jaipur HTML shows the rows serialized as dead JSON in the client payload (cinematic is "use client") but never rendered. Two corrections: the autoposter already consumes viral_eats (autoposter.py viral_eats_pick format since 2026-05-10), and 96/386 rows duplicate same-dest local_eateries names already rendered — unique invisible content is 290 rows/206 dests, not 229. Option (a) render-in-food-act remains valid and unshipped; option (b) payload trim is real but small (~1-2KB/page).

## 20. Pinterest is supported by the Outstand API we already pay for, we have ~1,286 'Pinterest-ready' images on disk, and we post zero pins
- **Dimension:** distribution · **Impact:** 3/5 · **Effort:** medium
- **Action:** Founder connects a Pinterest business account in the Outstand dashboard (5-min identity step). Then add a pinterest platform branch to autoposter.py PLATFORM_RULES: 1 pin/day from the existing daily format pick (image + keyword title + deep link with utm_source=pinterest), boards seeded by month ('Where to go in June — India') and state. Zero new content generation — pure redistribution of the existing image library.
- **Verifier:** Verified all finder evidence: 0 pinterest hits in autoposter.py (PLATFORM_VOICE/aliases cover only IG/FB/YT/reels), outstand.so live-confirms Pinterest among its 10 platforms with UNLIMITED connected accounts on the existing $5/mo + 1000-posts plan (so ~30 pins/mo = $0 marginal — stronger than claimed), and asset counts are exact (416+835+35=1,286; dest_image_gen.py line 15 says "Pinterest-ready"). One caveat: SOCIAL_AUDIT_AND_STRATEGY_2026-05.md line 289 already deferred "LinkedIn/X/Pinterest expansion" to Q3 post-100K-MUV — but that deferral assumed content-production cost, while this is zero-new-content redistribution via an API already paid for, which is a new angle that changes the deferral's premise; impact lowered to 3 because Pinterest clicks compound slowly (6-12mo, finder's 5-10K/mo figures are mature-account anecdotes) and it adds a founder identity step to an already-stalled founder queue.

## 21. The website links NONE of its own social accounts — sameAs has only Wikidata, footer has zero social links, despite 391 lifetime posts
- **Dimension:** distribution · **Impact:** 3/5 · **Effort:** low
- **Action:** Set NEXT_PUBLIC_SOCIAL_INSTAGRAM / NEXT_PUBLIC_SOCIAL_YOUTUBE (and FB if a handle var is added) in Vercel + redeploy — per the file's own comment, no code change needed. Claude can do this via vercel CLI/MCP if authed; otherwise it is a 2-minute founder paste. Optionally add the same URLs to the Wikidata item.
- **Verifier:** Verified live: prod sameAs = Wikidata only, 0 social hrefs on homepage; all NEXT_PUBLIC_SOCIAL_* unset while audit_2026-06-10.md confirms active accounts (IG 205 / FB 102 / YT 84 lifetime posts); gap untracked in pending-actions/CLAUDE.md. However the finder's "no code change needed" is half-wrong: getSocialHandles() is only consumed by getSameAsUrls() in layout.tsx:254 — footer.tsx renders NO social links (only the internal /social link-in-bio page, itself with zero outbound account links), and social.ts has no FACEBOOK slot, so the visitor→follower direction needs a small code change plus sourcing the public handles (repo only has Graph API IDs).

## 22. Every IG caption says 'link in bio' but the /social landing page never shows the destination that was just posted (hardcoded dhanaulti) and has no email capture
- **Dimension:** distribution · **Impact:** 3/5 · **Effort:** medium
- **Action:** Make /social close the loop: (1) a 'today on our feed' card row fed from the autoposter's posted-state (the autoposter already commits state.json to the autoposter-state branch every run — a tiny API route or a 5-line POST from the autoposter after publish gives the site the last 3 posted slugs); (2) add the existing save-trip/newsletter capture card to the page. ISR revalidate stays daily.
- **Verifier:** Verified all four legs: page.tsx line 34 hardcodes /destination/dhanaulti/${monthSlug} (en.json subtitle even says "Dhanaulti for {month}" — permanent by design, untouched since cinematic redesign); live curl of /en/social serves the dhanaulti/june card with 0 input elements (Newsletter/Subscribe hits are footer links, not capture); autoposter.py line 639/744 rewrites every IG caption to "link in bio"; audit_2026-06-10.md shows tosamaidan/lamayuru/kedarnath/sinthan-top posted — disjoint from the page. Fix path confirmed viable (origin/autoposter-state carries nakshiq-autoposter/state.json with post_log/posted_destinations) and the gap is untracked (.loop files clean; SOCIAL_AUDIT strategy doc line 174 suggests a rotating bio-link page but never shipped). Impact capped: no GA4 evidence of actual ig-bio click volume, so upside scales with IG growth rather than being immediate.

## 23. Owned email audience is ~6 subscribers and the 391 social posts have never once carried an email CTA — while monetisation is gated on 2K emails
- **Dimension:** distribution · **Impact:** 4/5 · **Effort:** low
- **Action:** Three $0 wires: (1) rotating email-CTA line in 1-in-N captions and ALL YT descriptions ('one email a month: where India is actually worth going → nakshiq.com/newsletter'); (2) newsletter/save card on /social; (3) a data-generated monthly lead magnet that justifies the ask — 'The {Month} Window' PDF/page auto-built from existing month scores (no new data, no fabrication). Measure via existing utm_content plumbing (utm() already supports content tagging, autoposter.py 2554-2595).
- **Verifier:** Live SQL: newsletter_subscribers=6, issues=8, destination_alerts=0, membership_waitlist=0, profiles=4 — total owned emails ~6-10 vs the 2K gate; weekly cron /api/newsletter/send (30 1 * * 0) confirmed in apps/web/vercel.json. Grep of every nakshiq-autoposter/*.py finds exactly one newsletter mention (autoposter.py:4389 Chinese Wall disclosure, not a CTA); _yt_caption_v2/_yt_caption_arrival descriptions carry only dest/home links. Bonus: https://www.nakshiq.com/en/newsletter is live (200) with a working subscribe form, so wire #1 is pure caption text — no build needed; gap is untracked (.loop hits are unrelated bizscout ideas, KNOWN OPEN email items are all on-site funnels).

## 24. 453 B-roll clips + month-scored data never recombined into YouTube long-form — channel is shorts-only, invisible to evergreen 'best places in {month}' search
- **Dimension:** distribution · **Impact:** 2/5 · **Effort:** medium
- **Action:** Add a monthly long-form generator to the autoposter: pick the month's top-10 by score where has_clip, ffmpeg-concat with per-destination narrated segments (reuse _template_spec arcs), chapter markers, description with 10 UTM deep links. Publish 1/month via the existing Outstand YT account (long-form upload supported). Run it first as a one-off for next month and judge search impressions in YT Studio before scheduling.
- **Verifier:** Gap is real: 453 clips on disk confirmed; no compilation/long-form builder in nakshiq-autoposter (all concat is intra-short); the only YT publish path hard-codes isShort:True via Outstand (autoposter.py:6234) and audit_2026-06-10.md confirms 84 lifetime YT posts all shorts; idea untracked in .loop/OPPORTUNITIES.md or any strategy doc. But impact revised down: clips are 6-8s 16:9 AI-generated (Veo) no-people establishing shots (per video-prompts.csv spec), so an 8-15min top-10 means looping each clip ~8-12x — far below the real-footage compilations owning that query — plus zero channel authority, AI-disclosure/inauthentic-content headwinds, unverified Outstand long-form support, and the 453 are festival-slug clips while the month-score picker needs destination-slug clips (same R2 bucket, coverage unproven).

## 25. Zero presence on broadcast channels: a Telegram channel is fully automatable today at $0; WhatsApp Channels is India-dominant but has NO official posting API
- **Dimension:** distribution · **Impact:** 3/5 · **Effort:** low
- **Action:** Founder creates @nakshiq Telegram channel (one-time identity step). Add a telegram_broadcast() step to the autoposter publish loop: rebroadcast each published post's image + caption + the FULL clickable UTM deep link (Telegram doesn't strip URLs, unlike IG). Surface the channel in the site footer via the social-handles env pattern. Skip WhatsApp automation; offer it as an optional manual founder ritual only.
- **Verifier:** Verified: 0 telegram/sendPhoto hits in nakshiq-autoposter/ (publish loop is IG/FB/YT-only via Outstand); t.me/nakshiq shows the generic contact page (no channel exists); idea untracked in SOCIAL_AUDIT/.loop/KNOWN-OPEN. WhatsApp claim holds — Meta's official Cloud API docs have no Channels posting endpoint (whapi.cloud is unofficial, ban-risk). One correction: no "social-handles env pattern" exists — footer.tsx has zero external social links, so the footer step is net-new work; channel also starts at 0 subscribers with weak promotion levers, so impact is compounding-slow, not a traffic spike.

## 26. All site media rides on Cloudflare's rate-limited r2.dev dev subdomain — no CDN caching, throttle risk as traffic grows
- **Dimension:** perf-techdebt · **Impact:** 3/5 · **Effort:** low
- **Action:** Add a custom domain (media.nakshiq.com CNAME) to the nakshiq-images and nakshiq-videos R2 buckets in Cloudflare (domain is already on Cloudflare DNS if nakshiq.com is; otherwise founder adds one DNS record), flip NEXT_PUBLIC_IMAGE_BASE_URL/NEXT_PUBLIC_VIDEO_BASE_URL in Vercel, redeploy. Old r2.dev URLs keep working during transition. Bonus: Cloudflare edge caching then serves repeat image hits without touching the bucket.
- **Verifier:** Confirmed: curl of /en/destination/jaipur shows all media on r2.dev dev subdomains (images pub-d897…, videos pub-bcda… — finder swapped the two), Cloudflare docs confirm r2.dev is rate-limited/dev-only/uncached, live headers show no cf-cache-status (origin hit per unique request), and apps/web/src/lib/image-loader.ts deliberately bypasses Vercel image optimization so there is no intermediate CDN; not tracked in any known-open list. However the proposed fix is overstated: nakshiq.com runs GoDaddy nameservers (ns65/ns66.domaincontrol.com), so attaching media.nakshiq.com requires either a full DNS migration to Cloudflare (free but moves all records incl. Vercel) or a ~$10/yr Cloudflare-registered media domain — not a one-record 30-min change — and there is no current throttling incident at today's low traffic.

## 27. React #418 NEW lead: 7+ timezone-unpinned toLocaleDateString calls in SSR'd client components on destination pages
- **Dimension:** perf-techdebt · **Impact:** 3/5 · **Effort:** low
- **Action:** Add timeZone: "Asia/Kolkata" to every toLocaleDateString/toLocaleString options object in destination-detail-cinematic.tsx and emergency-sos.tsx (and ideally extract a shared formatDateIST() helper in @itp/shared next to currentMonthIST, which already exists for exactly this class of bug). One mechanical PR, no copy changes for IST visitors.
- **Verifier:** Reproduced deterministically on prod: jaipur content_reviewed_at=2026-06-09 03:48 UTC, SSR HTML says "Editorial review 9 JUN", Playwright with timezoneId America/Los_Angeles hydrates "8 JUN" + throws Minified React #418 (text mismatch) on 1/1 load while Asia/Kolkata shows "9 JUN" with 0 errors; SQL shows 365/525 dest pages currently break for US-West visitors (218 US-East, 0 India — but a future bulk stamp at 18:30–24:00 UTC would flip it on for ALL Indian visitors). Corrections: the 3 emergency-sos.tsx sites are click-gated behind expanded=false so they can't cause #418 (pin them anyway for date correctness), the firing site is destination-detail-cinematic.tsx:3456 (day-numeric format; 2864/3184/3332 only fire near month boundaries), and this does NOT explain QA's Canberra-TZ intermittent 4/7 repro (AEST = same day for that stamp), so the tracked #418 may have a second cause.

## 28. Zero client-side error telemetry — production JS errors are only discoverable via the daily QA Playwright console scrape
- **Dimension:** perf-techdebt · **Impact:** 3/5 · **Effort:** low
- **Action:** Cheapest path that fits the $0 constraint: a ~25-line client snippet in the root layout that listens for error + unhandledrejection, dedupes, and navigator.sendBeacon()s {message, stack-head, url, ua, tz} to a new /api/log-client-error route writing to ops_reports (rate-limited like /api/reviews). Surfaces real-user errors (with the TZ field that would have caught #418 immediately) into the existing weekly ops digest. Alternative: Sentry free tier (5K events/mo) — but that adds a vendor + bundle weight for marginal gain.
- **Verifier:** Verified all evidence: zero error-monitoring vendors, zero window error/unhandledrejection listeners, no instrumentation.ts, and display-only error boundaries (error.tsx + useChunkErrorRecovery only auto-reloads chunk errors, reports nothing) across apps/web/src; @vercel/analytics+speed-insights don't capture JS exceptions. The proposed sink genuinely exists (ops_reports written by ~10 crons incl. watchdog with Monday digest; /api/log-bot-visit is an existing beacon-pattern route) and the gap is tracked nowhere (.loop/, qa/findings, CLAUDE.md, sprint-history all clean) — and qa/SKILL.md confirms console errors are only checked on a handful of pages, matching the 3-week #418 localization cost.

## 29. The 15-spec Playwright e2e suite never gates PRs — it runs only AFTER merge, against prod that's already live
- **Dimension:** perf-techdebt · **Impact:** 4/5 · **Effort:** medium
- **Action:** Add a pull_request job that waits for the Vercel preview deployment (deployment_status event or the patrickedqvist/wait-for-vercel-preview action), then runs the functional specs with BASE_URL=<preview URL> and --grep-invert Visual. Keep the post-merge prod run as a smoke check. Separately: drop `--update-snapshots` from the scheduled visual job (snapshot updates should be a manual dispatch) and decide whether lint should actually fail (or at least fail on NEW warnings).
- **Verifier:** Confirmed and worse than claimed. test.yml: push-to-main only, BASE_URL=https://www.nakshiq.com hardcoded, `npm run lint || true`, visual `--update-snapshots` — all verified; PR #25's statusCheckRollup shows ONLY Vercel, zero Actions checks pre-merge. New evidence: (1) quality-gate.yml — the one PR gate the finder credited — has NEVER run a job: 100/100 of its last 100 runs failed at the workflow-file level with 0 jobs ("workflow file issue", almost certainly the invalid `if: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY != '' }}` job-level condition at line 30 — secrets context isn't allowed there), so PRs currently get NO CI gating at all; (2) test.yml's post-merge "safety net" has been red on every run since 2026-04-18 (~8 weeks) — current failures include destination.spec "loads with all key sections", gap-year.spec, conversion.spec peak-alert, axe a11y, and visual (strict-mode: locator('header') resolved to 7 elements), i.e. the suite is broken/ignored, not a working smoke check. Minor finder correction: the visual job CAN fail despite --update-snapshots (locator errors — it's failing now), but snapshot diffs do self-heal. Not tracked in KNOWN OPEN items, .loop/, or docs. Revised action ordering: fix quality-gate's invalid secrets-if first (PR gate resurrect, ~1 line), triage the 8-weeks-red e2e suite (some failures may be stale selectors, some may be real prod bugs), THEN add the preview-URL pull_request job.

## 30. One shared secret (NEWSLETTER_SEND_SECRET) gates every admin write API and is accepted as a URL query param
- **Dimension:** perf-techdebt · **Impact:** 2/5 · **Effort:** low
- **Action:** Remove the query-param branch from isAuthed() in all admin routes (header-only; the admin UI already sends it client-side so switch its fetch to an Authorization header), and split a dedicated ADMIN_API_SECRET from the newsletter-send secret so rotating one doesn't break the other. ~1 hour, no founder action beyond adding one Vercel env var.
- **Verifier:** Verified in code: 6 admin write routes (sos, trip-reports, questions, stay-picks, citations/log, reviews) accept ?key=NEWSLETTER_SEND_SECRET in isAuthed(), and the admin UI GET-list fetches actually use it (admin-sos-client.tsx:74, admin-stay-picks-client.tsx:49) so the shared secret hits Vercel function logs on every list load — real, unshipped, untracked anywhere (.loop/qa/audits/docs/git). But the finder overstated: the revalidate route is header-only (no ?key= branch), and the 'browser history / Referer' leak channels don't apply because the key is entered via a form input, never in the address-bar URL — only same-origin XHR URLs in Vercel logs. Salt fallback 'nakshiq-default-salt' in 5 public routes also confirmed but trivial (IP-hash dedup, not secret-grade). Genuine low-severity hardening, worth the ~1hr fix, but not the medium-severity threat described.

## 31. Phase 3 scheduling is the only missing piece of a fully autonomous propose loop — and the headless pattern is already proven on this Mac
- **Dimension:** capability-leverage · **Impact:** 4/5 · **Effort:** medium
- **Action:** Wire a launchd plist (e.g. com.nakshiq.loop.propose, Tue/Thu/Sat morning) that runs headless `claude -p "/loop-propose"` with NAKSHIQ_LOOP=1 from the repo root. First do a one-shot headless test confirming the iMessage MCP reply tool loads in `claude -p` context (PHASE2.md itself flags this as the open question); if it doesn't, fall back to the Telegram plugin (also installed) or have the headless run send .loop/outbox.txt via the existing Resend ops-email path. No new approval surface — the existing 'go N' /loop-approve flow stays the gate.
- **Verifier:** Verified no scheduler runs loop-propose (no nakshiq launchd plist, crontab only has ga4-daily-audit, nothing in settings/workflows) while Phase 2 is fully built (.loop/PHASE2.md L53-56 defers scheduling to Phase 3, README L71 'not wired', chatId established 2026-06-07, propose-fixes.mjs/verify-findings.mjs exist). I also ran the decisive test PHASE2.md flags as open: a headless `claude -p` run made a real read-only mcp__plugin_imessage_imessage__chat_messages call against the established chat_id and returned TOOL_OK — iMessage MCP works headless on this Mac (Telegram also loads as fallback); one caveat: launchd context won't inherit Terminal's Full Disk Access for chat.db, so the first scheduled run must verify that (finder's one-shot-test-first plan already covers it).

## 32. Daily QA findings never enter the propose pipeline — every AMBER still needs a founder-initiated interactive session
- **Dimension:** capability-leverage · **Impact:** 3/5 · **Effort:** medium
- **Action:** Add a qa-findings adapter to verify-findings.mjs: read the latest qa/findings/<date>.json, map each high/medium finding into the existing finding shape, re-verify with an independent probe (the harness already documents its per-section methods), and queue confirmed ones into .loop/findings-inbox.json so propose-fixes + the iMessage approve flow pick them up unchanged. Combined with the Phase-3 scheduler this closes the full circle: QA detects → loop verifies → founder taps 'go' → PR opens on loop/auto-*.
- **Verifier:** Gap is real and untracked: grep of scripts/_loop/ has zero qa/findings references; propose-fixes.mjs reads only .loop/findings-inbox.json (line 5) and config.json verify.monitoredJobs lists only the 5 M1-M7 audit jobs, while qa/findings/2026-06-10.json is structured+schema-backed and qa/SKILL.md ends at report generators with no loop handoff; pending-actions.md audit comments confirm the 06-08/06-09 AMBERs were triaged in interactive sessions. Impact tempered because the QA run is itself a verifying Claude scheduled task, heterogeneous browser-class findings would need new probe methods (most would land "inconclusive" like the GSC jobs), and Phase 3 auto-PR is not wired so a founder session is still required after the adapter.

## 33. GSC weekly Coverage paste + hemkund-sahib 'is it fixed yet' check are automatable with the OAuth token already sitting in .secrets/
- **Dimension:** capability-leverage · **Impact:** 4/5 · **Effort:** medium
- **Action:** Build scripts/gsc-inspect-sweep.mjs reusing data-pull.mjs's OAuth client: (a) weekly stratified sample of ~1,400 sitemap URLs → write an estimated Indexed/Not-indexed block into the audit md (same format patch-gsc-indexing.mjs produces) so M2 stops silently skipping; (b) a 1-call weekly probe of the hemkund-sahib URLs that iMessages the founder the moment coverageState shows the /en/ canonical consolidated (or confirms it's still stuck). Optional (c): one-time founder login in a persistent Playwright profile to automate the Request-Indexing click.
- **Verifier:** Ran the suggested check live: urlInspection.index.inspect with the existing .secrets/ refresh token (webmasters.readonly) returned full indexStatusResult for sc-domain:nakshiq.com — no scope error — proving both "founder-manual" GSC items are automatable; no inspect script exists in scripts/ and the M2 monitor has been silently skipping (last Indexing Status paste 2026-05-29, route.ts:78 gates on indexed_pages !== null). Bonus: the live probe shows hemkund-sahib is ALREADY consolidated (un-prefixed www URL's googleCanonical = the /en/ URL, /en/ "Submitted and indexed", crawled 2026-06-05) — the 6-week founder-blocked Request-Indexing item can be closed with zero clicks, which itself proves the "nobody knows when Google consolidates" monitoring gap.

## 34. verify-findings.mjs punts GSC/GA4 findings to the founder as 'inconclusive — need OAuth' even though the OAuth creds are on disk
- **Dimension:** capability-leverage · **Impact:** 2/5 · **Effort:** low
- **Action:** Extend verify-findings.mjs with two real re-check methods: for audit-gsc-* findings, re-query the Search Analytics API directly (import the OAuth client factory from data-pull.mjs) with a different window/dimension than the audit used (the layer's own 'different method' rule); for GA4-correlation findings, re-pull via the ga4-reader service account. Keep audit-supabase-advisors inconclusive locally (SUPABASE_PAT is Vercel-only) unless the founder adds the PAT to apps/web/.env.local.
- **Verifier:** Verified all three legs: (1) verify-findings.mjs lines 208-213 hardcode audit-gsc-* to inconclusive "needs OAuth"; (2) live-fired both creds from the same repo — `node scripts/data-pull.mjs gsc top-pages 7d` and `ga4 geo-humans 7d` both returned real data via .secrets/gsc-oauth-client.json+refresh-token and ga4-reader.json; (3) .loop/findings-inbox.json holds an actual audit-gsc-ga4-correlation finding (86 clicks vs 23 sessions, 2026-05-30) parked on the human with that exact reason, and no fix is tracked in .loop/PHASE2.md/README/config. Impact tempered: only 1 such finding in ~10 days, and GA4-side correlation signal is weak anyway (loop disabled cro-leak for bot saturation) — real gap, cheap fix, modest payoff.

## 35. Road-conditions sweep emails the founder to do WebFetch verification that a scheduled agent can do itself
- **Dimension:** capability-leverage · **Impact:** 3/5 · **Effort:** low
- **Action:** Add a weekly scheduled agent step (same launchd/headless vehicle as Phase 3, or a /loop-propose sub-task): query the same three buckets the cron computes, WebFetch each row's source_url + the relevant BRO/PWD page, and write verified status updates or honest-removal proposals into .loop/pending-actions.md for one-tap 'go'. The founder's weekly email then becomes a receipt, not a task list.
- **Verifier:** Read the cron route: it only buckets EXPIRED/STALE/UNSOURCED and emails the founder with manual "WebFetch the source URL" instructions — no verification logic, exactly as claimed. Live DB query: 28 road_reports, 7 currently stale (blocked/risky on Sarchu–Leh, Rohtang, Kunzum, Badrinath access, all last reviewed 2026-05-25 = 16 days ago in peak opening season), so the next Monday run emails a 7-row manual task list; ops_reports shows this nag fired with 13 alerts on 05-18 and 05-25 until an ad-hoc session cleared it. No verifier script exists and nothing in .loop/ (pending-actions, OPPORTUNITIES, findings, proposals, dismissed) tracks this — gap is real, unshipped, and untracked.

## 36. GSC indexed-pages monitor (M2) has been blind for 12 days — the weekly manual paste stopped 2026-05-29 and it skips silently
- **Dimension:** ops-automation · **Impact:** 3/5 · **Effort:** medium
- **Action:** Two-step: (1) low-effort same-day — add an M2.c 'indexing snapshot stale' finding to audit-gsc-alerts (alert when no audit in the window has a non-null indexed_pages for >10 days), so silence becomes a visible alert. (2) kill the ritual — a weekly local Playwright job using a persistent Chrome profile logged into GSC (founder logs in once) scrapes Indexed/Not-indexed from search.google.com/search-console/index and pipes them into patch-gsc-indexing.mjs; this is exactly the 'human-driven Chrome session' that produced the pre-2026-05-27 data, automated.
- **Verifier:** Confirmed but for a deeper reason than claimed: prod ops_reports show audit-gsc-alerts has run with audits_read=1-2 and alerts_count=0/ok:true every day since 2026-05-28 because nearly all gsc-audits/*.md are git-untracked (only 05-28 and 06-02 committed) — Vercel's prebuild snapshot sees 2 audits and evaluate() short-circuits at recent.length<3, so M2.a/M2.b/M3 have never evaluated real data in prod. The finder's specifics are wrong: the parser reads the daily audit's "| Indexed |" rows (non-null almost daily through 06-09, locally), the 05-29 freeze was GSC-side and loudly flagged/resolved in the daily audits (refreshed 06-09 to 16,000), and a weekly Playwright paste alone would not fix prod — the fix is committing the audit MD history (or snapshot) plus an ok:false alarm when audits_read<3 or latest_audit is stale.

## 37. IndexNow is fully built (endpoint live, secret set, key served) but has ZERO automated callers — new pages wait days for Bing/DDG discovery
- **Dimension:** ops-automation · **Impact:** 3/5 · **Effort:** low
- **Action:** Wire 3 cheap call-sites: (1) apply-title-overrides.mjs --revalidate also pipes the changed URLs to indexnow-push.mjs; (2) a weekly Vercel cron (or step in the existing GHA sync job) diffs sitemap.xml against a last-pushed snapshot in the repo/R2 and pushes new+changed URLs; (3) the loop's ship checklist gains 'push new-surface URLs to IndexNow' as a post-deploy step. All free, no founder action.
- **Verifier:** Gap is real: live probes confirm the endpoint works (GET ok, wrong-secret POST → 401 "unauthorized" proving the Vercel secret is set, key file serves 200), yet grep across scripts/, apps/web/src, .github/workflows (4 files), apps/web/vercel.json crons (12+, none indexnow), .loop/ and the autoposter finds zero callers — only the route and indexnow-push.mjs themselves. Not tracked anywhere (sprint-history only logs the 2026-04-24 "key file live" build; QA findings use /api/indexnow only as a GET health probe). Caveat tempering impact: data/bing-webmaster-trend.jsonl (last capture 2026-05-25) shows Bing indexed only 1.7K of 20.8K discovered URLs, so indexing selection — not just crawl discovery — is a bottleneck; IndexNow speeds freshness/recrawl of changed URLs (title overrides, new surfaces) but won't by itself fix the indexed-ratio.

## 38. No off-platform DB backup: a fabrication-purge gone wrong is unrecoverable after 7 days (and after ANY downgrade to free tier)
- **Dimension:** ops-automation · **Impact:** 4/5 · **Effort:** low
- **Action:** One script + one schedule: scripts/backup-content-tables.mjs runs pg_dump over the direct Postgres connection (5432 — does not count toward REST egress) for the ~20 content tables, gzips (~30-50MB), writes to a private R2 'nakshiq-backups' prefix, prunes to 8 weekly copies. Run it from the existing GitHub Actions weekly slot (add SUPABASE_DB_URL as a GHA secret) so it is laptop-independent. Add a convention to pg-bulk's withPgTransaction: dump the touched table to a local snapshot before any bulk UPDATE/purge. For R2 videos: one rclone/aws-cli sync of nakshiq-videos → local archive dir now, refresh quarterly.
- **Verifier:** Verified: zero pg_dump/pg_restore/backup tooling in repo (only CLAUDE.md line 94 mentions pg_dump as an option; watchdog 'backup' strings are posting-retry slots), no backup job in GH workflows or vercel.json crons, gap untracked in .loop/KNOWN-OPEN. Destructive prod sweeps confirmed (eb11033c/6a54b539/33aaf5e9 in 48h) and data/festivals/videos = 453 files/612MB on one laptop with the original 330 clips R2-only since 2026-06-03. One softener: the 2026-06-10 cc-phone purge logs every before→after to a git-committed audit, so that sweep is reversible — but it's per-script discipline, not systemic; the 2026-06-09 SOS purge audit lacks a full restorable pre-image, so the 7-day Pro window (zero after a free-tier downgrade) is the only general restore path.

## 39. MEMORY.md is 126KB against a 24.4KB load limit — every session silently drops part of the project's hard-won rules
- **Dimension:** ops-automation · **Impact:** 3/5 · **Effort:** low
- **Action:** One compaction pass now: rewrite every MEMORY.md entry to a single line ≤200 chars ending in its existing topic-file link (detail already lives there); move entries older than ~45 days into MEMORY-ARCHIVE.md; delete MEMORY.md.bak. Then add one line to the /wrap or capture convention: 'index entry = 1 line ≤200 chars, detail goes in the session file' so it cannot regrow. Target: MEMORY.md < 20KB.
- **Verifier:** Verified byte-exact: MEMORY.md 125,978B vs 24.4KB limit, and this session's own reminder confirms truncation. 94/129 entries (73%, everything older than ~Jun 2) silently drop each load — including the triple-publish postmortem, mobile-parked decision, and spatial_ref_sys guard (egress/scrape/caching rules survive only because CLAUDE.md duplicates them). All 129 entries already link a topic file (sampled files exist), so one-line compaction is lossless; not tracked in pending-actions/HANDOFF/KNOWN-OPEN; the May 22 compaction (.bak/.archive timestamps) regrew 5x in 19 days, so the fix must include enforcing the ≤200-char rule in /wrap, not just a one-off pass.
