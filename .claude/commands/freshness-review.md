---
description: Weekly destination freshness review — re-verify the stalest ~41 destinations so every page's "VERIFIED <month>" is at most a quarter old
---

# Weekly destination freshness review

Every destination page shows `VERIFIED <month>` from `destinations.content_reviewed_at`.
Your job: re-check this week's batch against the live world and report, per destination,
whether what the site says is still true. The wrapper (`scripts/freshness-review-weekly.sh`)
stamps the date AFTER you finish — you never write to the database.

## Inputs
- `$BATCH` — the batch file (path given in your prompt). Each destination carries the
  time-sensitive fields the site renders. **Do not edit this file** — the wrapper checksums it
  and refuses the whole run if it changed.
- Also read the live page if useful: `https://www.nakshiq.com/en/destination/<id>` (Vercel's
  firewall may 403 curl — the batch fields are the source of truth).

## What to check, per destination
1. **Access** — is the destination currently reachable? Road/route closures, landslides,
   floods, bridge collapses, disaster-management advisories in the last 90 days.
   (This replaces the old monthly news-sweep.)
2. **Permits** — `permit_required` / `permit_type` / `permit_lead_days` still match the
   issuing authority (ILP, PAP/RAP, Inner Line, forest/park permits).
3. **Getting there** — `nearest_airport` / `nearest_railhead` still operating (new airport
   opened, station renamed, flights suspended).
4. **Status changes** — park or site closed for the season / by order, entry fees or rules
   changed, destination shut to tourists.
5. **Seasonality** — `best_months` / `avoid_months` not contradicted by an official advisory.

Do NOT re-research prose, eateries, stays or scores. This is a "has anything material
changed" check, not a rewrite.

## Sources
- Prefer official ones: state tourism boards, district (`*.nic.in`) sites, `*.gov.in`,
  NHAI/BRO/state PWD, SDMA/IMD advisories, AAI, Indian Railways, forest departments.
  Reputable news (national/regional dailies) counts for closures.
- **At least 2 sources from 2 different hosts per confirmed destination, and at least ONE of
  them official (`*.gov.in`, `*.nic.in`) or a named news outlet** (the list is `NEWS_HOSTS` in
  `scripts/freshness-review.mjs`). Wikipedia, rome2rio, travel blogs and aggregators may support
  but never carry a verdict alone. nakshiq.com never counts. The wrapper re-opens them; a destination whose sources cannot be opened is not
  stamped.
- These hosts are slow and often need a retry — a timeout is not evidence a page is missing.
- **Never state a fact you did not see on a page.** If you could not find enough to confirm a
  destination, leave it out of the entries file — an unreviewed destination keeps its honest
  older date, which is correct.

## Verdicts
- `confirmed` — nothing material has changed; the rendered facts are still true.
- `needs_correction` — something the site states is now wrong. Give each wrong field with
  `current` (what we show), `proposed` (what's true now) and `source` (URL + a short quote in
  `notes`). These are NOT stamped and NOT applied automatically — a human session applies them.

## Working method
- Split the batch across at most 3 parallel Haiku sub-agents (≈14 destinations each) for the
  searching. Then read their findings and decide each verdict YOURSELF — sub-agents propose,
  you confirm.
- Write the entries file (path in your prompt) in this shape:
  `{"reviews":[{"id","verdict","sources":[…],"notes","corrections":[{"field","current","proposed","source"}]}]}`
- Validate it (no write): `node --env-file=apps/web/.env.local scripts/freshness-review.mjs apply --batch $BATCH --entries $ENTRIES`
  Fix or remove anything it drops, then re-run until it is clean. **Never pass `--commit`.**
- Write the run note (path in your prompt): one table row per destination
  (id · verdict · sources · what you checked), then a **Corrections** section listing every
  proposed fix with its source and quote. Do not commit — the wrapper commits it.

Finish with exactly one final line:
`RESULT confirmed=<n> corrections=<n> skipped=<n>`
